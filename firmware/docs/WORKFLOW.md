# E-SKIN Firmware Workflow

## Purpose

This folder contains the firmware workflow and notes for the STM32 mainboard and Teensy 4.1 bridge.

## Current data path

```text
Sensors -> STM32 mainboard -> Host SPI -> Teensy 4.1 -> USB CDC -> PC backend -> Visualization
```

DAPLink is used for STM32 SWD programming and debugging. The Teensy is programmed separately with Teensy Loader or Arduino CLI.

## STM32G474CET6 CubeMX configuration

The current STM32 project is:

```text
hardware/new/firmware/ESKIN_STM32/
```

The device is an STM32G474CET6 in an LQFP48 package. The project is generated by STM32CubeMX 6.18.0 as a CMake project using STM32Cube FW_G4 V1.6.3.

### Debug and clock

| Function | Configuration |
|---|---|
| SWDIO | PA13, `SYS_JTMS-SWDIO` |
| SWCLK | PA14, `SYS_JTCK-SWCLK` |
| Debug mode | Serial Wire; JTAG is not used |
| Initial system clock | HSI, 16 MHz default generated configuration |
| Toolchain | CMake with GCC/ARM GNU toolchain |

### SPI1: STM32 master to the external MAX11633 ADC

`COL_ADC1` is an external MAX11633 ADC. It is not the STM32 internal ADC1 peripheral.

| Signal | Pin | Configuration |
|---|---|---|
| SCK | PA5 | SPI1 SCK |
| MISO | PA6 | SPI1 MISO |
| MOSI | PA7 | SPI1 MOSI |
| CS1 | PB0 | GPIO output, software-controlled chip select |
| EOC1 | PB10 | GPIO input; may later be changed to EXTI |

SPI1 is configured as full-duplex master, 8-bit, Motorola frame format, MSB first, CPOL low, first edge, and software NSS. The ADC transaction must drive PB0 low only for the transaction and return it high afterwards.

### SPI3: Teensy host link

The Teensy 4.1 is the SPI master and the STM32 is the SPI slave.

| Signal | Pin | Configuration |
|---|---|---|
| NSS / chip select | PA15 | SPI3 hardware NSS input |
| SCK | PB3 | SPI3 SCK, slave mode |
| MISO | PB4 | SPI3 MISO, slave mode |
| MOSI | PB5 | SPI3 MOSI, slave mode |
| HOST_IRQ | PB8 | GPIO output from STM32 to Teensy |
| HOST_SYNC | PB9 | GPIO input from the host side |
| External NRST | PG10 | GPIO output for the external reset net |

SPI3 is configured as full-duplex slave, 8-bit, Motorola frame format, MSB first, CPOL low, first edge, and hardware NSS input. The Teensy must control PA15 and provide the SPI clock.

### MUX1 GPIO

`ROW_MUX1` is a CD74HC4067:

| Signal | Pin | Startup level |
|---|---|---|
| S0 | PA0 | Low |
| S1 | PA1 | Low |
| S2 | PA2 | Low |
| S3 | PA3 | Low |
| EN1 | PA4 | High (disabled until scanning starts) |

### Startup levels

The generated `MX_GPIO_Init()` currently sets the safety levels below:

```text
PG10 / external NRST : High
PB0  / ADC_CS1       : High (ADC deselected)
PA4  / MUX_EN1       : High (MUX disabled)
PB8  / HOST_IRQ      : Low
PA0..PA3 / MUX address: Low
```

These levels prevent the external device from remaining in reset, prevent the ADC from being selected during boot, and keep the multiplexer disabled until the scan routine explicitly enables it. If CubeMX code is regenerated, re-check these output levels in `GPIO Settings` because generated files may be overwritten.

### Current implementation status

The first blocking FSR acquisition implementation is now present in `Core/Src/main.c`. It selects rows 0--15 on MUX1, enables the MUX, requests an AIN0--AIN15 scan from the MAX11633, waits for active-low EOC, reads sixteen 12-bit samples, and stores them in `fsr_frame[16][16]` as `[row][column]`. The implementation uses SPI1 polling and a 1 ms row-settling delay; it is intended for bring-up and is not yet a high-rate solution.

The MAX11633 setup command currently selects internal clock mode 10 and the internal 2.5 V reference (`0x68`). The conversion command (`0xF8`) requests a scan from AIN0 through AIN15. Confirm the command timing and reference choice against the fitted ADC variant before relying on calibrated values. The current code transmits the frame over SPI3 using polling; DMA and higher-rate buffering are still future work.

## Initial STM32--Teensy frame protocol

After a successful FSR scan, STM32 asserts `HOST_IRQ` (PB8) high. The Teensy detects the IRQ, pulls chip-select low, and clocks exactly 515 bytes from the STM32 SPI3 slave:

```text
byte 0        0xA5 frame magic
bytes 1..2    uint16 sequence number, little-endian
bytes 3..514  256 uint16 ADC samples, little-endian, row-major [row][column]
```

The payload is 16 rows × 16 columns; each sample is a 12-bit MAX11633 result stored in a 16-bit word. The Teensy writes the validated binary frame to USB CDC, then waits for `HOST_IRQ` to return low.

## Teensy 4.1 Arduino firmware

The bridge sketch is located at:

```text
hardware/new/firmware/teensy41/ESKIN_SPI_USB_BRIDGE/ESKIN_SPI_USB_BRIDGE.ino
```

It uses only the built-in Arduino `SPI` library and Teensy USB Serial. No third-party Arduino libraries are required. Install the Teensy board support package, select `Teensy 4.1`, and select a USB type that provides `Serial`.

| Teensy 4.1 | STM32 mainboard |
|---|---|
| SCK (13) | PB3 / SPI3_SCK |
| MOSI (11) | PB5 / SPI3_MOSI |
| MISO (12) | PB4 / SPI3_MISO |
| CS (10) | PA15 / SPI3_NSS |
| IRQ (2) | PB8 / HOST_IRQ |
| GND | GND |

The sketch starts at 1 MHz, SPI mode 0, MSB first. Both boards must share ground and use compatible I/O voltage levels.

### Corrected HOST connector pin numbering (2026-08-09)

The connector **pin numbers themselves**, not merely the viewing direction,
were confirmed to be reversed relative to the earlier annotated image. The
authoritative electrical numbering for new-module wiring is:

```text
Mainboard J1 pin 1  = FPGA_3V3_IN -> Teensy 4.1 3.3V pin
Mainboard J1 pin 2  = GND         -> Teensy 4.1 GND pin
Mainboard J1 pin 3  = GND         -> Teensy 4.1 GND pin
Mainboard J1 pin 4  = HOST_SCK    -> Teensy 4.1 digital pin 13 / SCK
Mainboard J1 pin 5  = HOST_MOSI   -> Teensy 4.1 digital pin 11 / MOSI
Mainboard J1 pin 6  = HOST_MISO   -> Teensy 4.1 digital pin 12 / MISO
Mainboard J1 pin 7  = HOST_nCS    -> Teensy 4.1 digital pin 10 / CS
Mainboard J1 pin 8  = HOST_IRQ    -> Teensy 4.1 digital pin 2
Mainboard J1 pin 9  = HOST_SYNC   -> not connected in the current bridge
Mainboard J1 pin 10 = NRST        -> DAPLink nRESET, not Teensy
```

| Correct J1 pin | Signal | Teensy 4.1 pin printed on the board |
|---:|---|---|
| 1 | FPGA_3V3_IN | `3.3V`, only when Teensy is the single power source |
| 2 | GND | `GND` |
| 3 | GND | `GND` |
| 4 | HOST_SCK | digital pin `13` / SCK |
| 5 | HOST_MOSI | digital pin `11` / MOSI |
| 6 | HOST_MISO | digital pin `12` / MISO |
| 7 | HOST_nCS | digital pin `10` / CS |
| 8 | HOST_IRQ | digital pin `2` |
| 9 | HOST_SYNC | Not connected by the current combined bridge |
| 10 | NRST | DAPLink nRESET; not a Teensy data GPIO |

Earlier workflow entries that describe `J1 pin 10` as `FPGA_3V3_IN` or `J1
pin 1` as `NRST` used the reversed annotation and must not be used as the wiring
reference. Signal names and the Teensy GPIO assignments remain unchanged; use
the corrected table above as the authoritative pinout.

### Python frame reader

The binary USB stream can be inspected with:

```text
hardware/new/firmware/teensy41/ESKIN_SPI_USB_BRIDGE/read_teensy_fsr.py
```

Install the dependency and run it with the Teensy COM port:

```powershell
py -m pip install pyserial
py read_teensy_fsr.py --port COM7
```

Use `--print-matrix` to print all 256 samples for each frame:

```powershell
py read_teensy_fsr.py --port COM7 --print-matrix
```

The reader resynchronises on the `0xA5` magic byte, checks frame length, decodes the little-endian sequence number and 16 x 16 uint16 samples, and reports sequence gaps and min/max values.

## Preparation

Install and verify the following tools before creating the STM32 firmware project:

1. Install Visual Studio Code.
2. Install the official `STM32CubeIDE for Visual Studio Code` extension pack.
3. Install the `CMake Tools` and `Cortex-Debug` extensions.
4. Install Python 3 and pyOCD:

   ```powershell
   py -m pip install -U pyocd
   ```

5. Install the STM32G4 CMSIS-Pack:

   ```powershell
   pyocd pack install stm32g474
   ```

6. Verify the available STM32G474 targets:

   ```powershell
   pyocd list --targets
   ```

   The expected target for an STM32G474CE device is usually `stm32g474cetx` or `stm32g474ceux`, depending on the package type.

7. Connect the DAPLink probe to SWDIO, SWCLK, GND, VTref, and preferably NRST, then verify the probe:

   ```powershell
   pyocd list
   ```

8. Create or import the STM32 project as a CMake project. The STM32CubeMX `.ioc` file should generate the CMake files used by Visual Studio Code.

9. Build the project in Visual Studio Code and keep the generated `.elf` and `.hex` files for programming and debugging.

10. Flash the STM32 target with pyOCD, using the exact target name reported by `pyocd list --targets`:

    ```powershell
    pyocd flash -t stm32g474cetx build\\main.hex
    ```

The traditional Eclipse-based STM32CubeIDE is not required for this workflow. It may still be used separately for project generation or debugging, but the Visual Studio Code workflow is based on CMake. Generic DAPLink probes are programmed through pyOCD or OpenOCD; the STM32 Visual Studio Code debugger should not be assumed to support every CMSIS-DAP probe directly.

### Windows build tools

The CMake project requires the following command-line tools:

| Tool | Purpose | Installation |
|---|---|---|
| CMake | Generates the native build files from `CMakeLists.txt` | Install the Windows x64 installer from <https://cmake.org/download/> and add CMake to `PATH` |
| ARM GNU Toolchain | Compiles and links firmware for the Cortex-M4 (`arm-none-eabi-*`) | Download the Windows x86_64 package from <https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads>, extract it, and add its `bin` directory to `PATH` |
| Ninja | Executes the CMake build graph | Download `ninja-win.zip` from <https://github.com/ninja-build/ninja/releases>, extract `ninja.exe`, and add its directory to `PATH` |

On the current Windows installation, the tools are installed under `D:\study\programming` and the PATH entries are:

```text
D:\study\programming\CMake\bin
D:\study\programming\ArmGnuToolchain\bin
D:\study\programming\Ninja
```

After changing PATH, restart VS Code and verify:

```powershell
cmake --version
arm-none-eabi-gcc --version
arm-none-eabi-gdb --version
ninja --version
```

Build the generated project from the `ESKIN_STM32` directory:

```powershell
cmake --preset Debug
cmake --build --preset Debug
```

Expected outputs include `build\Debug\ESKIN_STM32.elf` and `build\Debug\ESKIN_STM32.hex`.

## Development workflow

1. Define and version the STM32--Teensy SPI protocol.
2. Create/build the STM32G474 mainboard firmware.
3. Configure the STM32 host SPI as a slave with hardware NSS and DMA.
4. Acquire sensor data on the STM32 using timer-triggered sampling and double buffers.
5. Signal a ready frame to the Teensy with the IRQ line.
6. Implement the Teensy SPI-master bridge and USB CDC output.
7. Validate frame length, sequence number, endianness, and CRC.
8. Connect the output to the existing PC backend and Hardware Live visualization.
9. Flash/debug the STM32 through DAPLink/SWD.
10. Flash the Teensy bridge separately and run an end-to-end test.

## Recommended first milestone

Use a synthetic STM32 test frame (counter, ramp, or checkerboard) before enabling the real sensors. Confirm:

- SPI mode, clock polarity/phase, chip-select timing, and byte order;
- frame sequence continuity and CRC validation;
- Teensy USB transfer without text logs mixed into the binary stream;
- correct FSR orientation and accelerometer ordering in the PC visualization.

## Data-rate check

For a 16 x 16 two-layer FSR array plus 16 three-axis accelerometers, one frame is approximately 1,132 bytes in the current `ESKN` format. At 700 frames/s this is about 792 kB/s (6.34 Mbit/s) before SPI overhead. Bring up the link at a low rate first, then verify the required SPI clock and buffering at the target rate.

## DAPLink notes

- DAPLink/CMSIS-DAP: STM32 SWD flash, reset, and debug.
- Teensy Loader/Arduino CLI: Teensy 4.1 firmware upload.
- Runtime data path: Teensy USB CDC only.
- Do not assume that DAPLink mass-storage drag-and-drop supports every STM32 target; verify the target flash algorithm or use a CMSIS-DAP-compatible programming tool.

## STM32G474CET6 programming record

This section records the complete programming procedure used for the current `ESKIN_STM32` firmware.

### Hardware connection

The target board was powered externally and connected to the Jixin.pro CMSIS-DAP/DAPLink probe:

| DAPLink signal | STM32 target |
|---|---|
| SWDIO | PA13 |
| SWCLK | PA14 |
| GND | Mainboard GND |
| VTref | Mainboard 3.3 V |
| nRESET | STM32 dedicated NRST, LQFP48 pin 7 |

`PG10` was not used for the DAPLink reset connection; it is a separate ordinary GPIO on the mainboard.

### Build command

The project was built from:

```text
hardware/new/firmware/ESKIN_STM32/
```

The generated image was:

```text
build/Debug/ESKIN_STM32.elf
```

Build command:

```powershell
cmake --build --preset Debug
```

The build completed successfully and linked `ESKIN_STM32.elf`.

### Probe detection

The probe was detected as:

```text
jixin.pro CMSIS-DAP
Unique ID: LU_2022_8888
Target: stm32g474cetx
```

When `Target: n/a` was reported, the board power, VTref, SWD wiring, and dedicated NRST connection had to be checked before continuing.

### Initial programming attempt

The initial command used the default pyOCD SWD speed:

```powershell
pyocd flash -t stm32g474cetx build/Debug/ESKIN_STM32.elf
```

This attempt failed during board uninitialisation with:

```text
SWD/JTAG communication failure (Unexpected ACK '0')
```

This indicates an unstable SWD connection, target reset timing, or target power/clock issue. The ELF file and firmware image were not changed by this failed attempt.

### Successful programming retry

The successful retry used a lower SWD frequency and reset-under-connection:

```powershell
pyocd flash `
  -t stm32g474cetx `
  --frequency 100000 `
  --connect under-reset `
  build/Debug/ESKIN_STM32.elf
```

Successful pyOCD result:

```text
Erased 10240 bytes (5 sectors)
Programmed 9216 bytes (9 pages)
Identical 2048 bytes (2 pages)
```

The updated STM32 firmware containing the initial 16 x 16 FSR scan and SPI3-to-Teensy frame transport is now programmed on the target.

### Post-programming validation

The programming result confirms Flash access only. Runtime validation still requires:

1. Resetting the target and starting a Cortex-Debug session.
2. Confirming that `main()` is reached.
3. Checking `fsr_scan_status` and `fsr_frame[16][16]`.
4. Connecting the Teensy SPI bridge and validating `HOST_IRQ` plus the 515-byte frame.

## Software and integration test record

### Test run: 2026-07-23

| Test | Result | Evidence / interpretation |
|---|---|---|
| STM32 CMake rebuild | PASS | `cmake --build --preset Debug` reported `ninja: no work to do`; the previously built ELF is current. |
| STM32 source compile | PASS | The latest `ESKIN_STM32.elf` linked successfully after the retry-recovery change. |
| Python receiver syntax | PASS | `read_teensy_fsr.py` compiled successfully with Python. |
| Offline 515-byte protocol parser | PASS | A synthetic `0xA5` frame with sequence 7 and values 0..255 decoded correctly into a 16 x 16 matrix. |
| DAPLink target detection | NOT PASS | `pyocd list` detected `jixin.pro CMSIS-DAP_LU`, but currently reported `Target: n/a`; the board must be powered and SWD/VTref wiring rechecked before debug testing. |
| Teensy COM9 open | PASS | Python opened `COM9` successfully, confirming the USB serial port is available. |
| Runtime frame reception | NOT PASS | Zero bytes were received during a 3-second COM9 read. No valid `0xA5` frame was observed. |

### Issues and attempts

1. The first Python receiver attempt failed because COM9 was occupied by Arduino Serial Plotter. Closing the plotter released the port.
2. A receiver process was then left running after a tool timeout; the specific `read_teensy_fsr.py` processes were identified and terminated to release COM9.
3. After COM9 was available, the receiver opened successfully but received no bytes. This indicates that the Teensy USB path is present, but the STM32-to-Teensy frame trigger has not been observed.
4. The current `main.c` retries after ADC/host timeouts instead of stopping permanently. However, no runtime frame can be produced until the STM32 completes the MAX11633 scan and raises `HOST_IRQ`.
5. The latest STM32 image was rebuilt and successfully reflashed with pyOCD at 100 kHz using `under-reset`; this does not by itself prove that the ADC or SPI3 link is working.

### Immediate next diagnostic

Use Cortex-Debug to inspect `fsr_scan_status` and `HOST_IRQ`. If `fsr_scan_status` is `HAL_TIMEOUT`, debug the MAX11633 wiring first (`PB0` CS, `PB10` EOC, `PA5/PA6/PA7` SPI1). If the status is `HAL_OK` but `HOST_IRQ` never rises, debug the SPI3/Teensy wiring (`PB8`, `PA15`, `PB3/PB4/PB5`).

### Follow-up check: 2026-07-23

The latest host-side check found:

```text
pyocd list: Jixin.pro CMSIS-DAP detected, Target: n/a
COM9: Teensy USB serial device detected
COM10: DAPLink USB serial device detected
```

Because the target is currently `n/a`, runtime STM32 variables could not be inspected in this check. The source review confirms that `HOST_SendFrame()` is called only when `FSR_ScanFrame()` returns `HAL_OK`; an ADC EOC timeout prevents any SPI3 frame and therefore produces zero bytes on COM9. Restore target detection first, then inspect `fsr_scan_status` and `HOST_IRQ` with Cortex-Debug.

A further low-speed recovery attempt was made with `pyocd flash -t stm32g474cetx --frequency 100000 --connect under-reset`. It failed during `DebugPortSetup` with `Unexpected ACK '5'`, confirming that the current issue is target-side SWD communication or target power/reset wiring rather than the ELF image.

A final retry at an even lower 10 kHz SWD frequency with `under-reset` also failed during board uninitialisation with `Unexpected ACK '5'`. Further frequency reduction is unlikely to help until target power, VTref, NRST, SWDIO, and SWCLK wiring are physically verified.

### SPI pattern isolation test: 2026-07-24

To isolate the STM32-to-Teensy electrical path without involving the ADC or FSR scan, a new `SPI_PATTERN_TEST` directory was added beside the production firmware. The original STM32 and Teensy files were not modified. The test uses one SPI byte, `0x55` (`01010101`), because SPI transfers complete bytes rather than seven-bit strings. The STM32 test snippet configures the existing SPI3 slave to return `0x55` when the Teensy supplies NSS and clock. The Teensy Arduino sketch acts as SPI master at 100 kHz, clocks one byte every 100 ms, and prints the received byte and PASS/FAIL over USB serial at 115200 baud.

Validation is pending hardware execution. A PASS result (`RX: 0x55 bits=01010101 PASS`) confirms SPI3 wiring, mode, chip-select timing, and Teensy USB output only; it does not validate MAX11633, MUX1, or the production 515-byte frame. The STM32 test file is intentionally an integration snippet and is not included in the production CMake target.

### Power-domain and SCK connector diagnosis: 2026-07-25

#### Resolved fault

The previous condition in which the STM32 HOST_SCK input remained low was traced to a poor connector solder joint. The connector was resoldered, after which the signal at the actual STM32 `PB3/HOST_SCK` pin changed correctly between low and high.

The earlier report called this pin `PD3`. In the mainboard v2.2 schematic and the STM32 firmware it is `PB3` (STM32G474CETx LQFP48 pin 40). The GPIO mirror output is `PB4/HOST_MISO` (pin 41).

#### Circuit review

The v2.2 mainboard schematic shows the following power topology:

- HOST connector `J1` pin 10 is `FPGA_3V3_IN`.
- `FPGA_3V3_IN` passes through `R1 = 0 ohm` directly to the global mainboard `+3.3V` rail.
- SWD header pin 1 is also connected directly to the same global `+3.3V` rail.
- There is no ideal diode, load switch, series isolation diode, or power multiplexer between these two inputs.
- Therefore, connecting both Teensy 3.3 V and DAPLink 3V3 makes two regulator outputs drive the same rail directly.
- The Jixin CMSIS-DAP documentation identifies its `3V3` pin as a 3.3 V power output. It is not required for SWD when the target has its own power; SWD programming requires GND, RST, SWDIO, and SWCLK.

The current SCK mirror test software was also reviewed:

- Teensy GPIO 13 is an output and GPIO 12 is an input.
- STM32 PB3 is a no-pull GPIO input.
- STM32 PB4 is a push-pull GPIO output that copies PB3 every 1 ms.

No software direction conflict was found in this specific mirror test.

#### Recorded power and GPIO observations

| Case | Connections | Measurements and serial result | Interpretation |
|---|---|---|---|
| 1.1 | DAPLink USB and Teensy USB both connected to the PC; DAPLink 3V3 connected to the mainboard | `SCK_LOW->0 SCK_HIGH->0 FAIL`; Teensy 3.3 V approximately 3.0 V; mainboard 3.3 V input stable at approximately 3.0 V; PB3 switches 0-3.0 V; PB4 remains approximately 0.3 V | DAPLink and Teensy 3.3 V regulator outputs are hard-paralleled through the mainboard. The 3.0 V rail and PB4 at 0.3 V are invalid. PB4 is either not being driven because the MCU is not running reliably, or the PB4/MISO path is clamped/open. |
| 1.2 | DAPLink USB and Teensy USB both connected to the PC; DAPLink 3V3 disconnected from the mainboard | `SCK_LOW->0 SCK_HIGH->0 FAIL`; Teensy rail approximately 3.2 V; mainboard 3.3 V input approximately 2.9 V; PB3 switches 0-3.2 V; PB4 remains approximately 0.3 V | Removing the second 3.3 V source improves the Teensy rail, but the 0.3 V drop to the mainboard and PB4 remaining at 0.3 V indicate excessive load, a high-resistance power connection, target brownout/reset, or a separate PB4/MISO connection fault. SWD signal pins can also parasitically feed an inadequately powered target. |
| 2 | Only Teensy USB connected to the PC; the unpowered DAPLink 3V3 wire remains connected to the mainboard | `SCK_LOW->0 SCK_HIGH->1 PASS`; Teensy maximum approximately 2.2 V; mainboard 3.3 V input switches 0-2.1 V; `PB3/HOST_SCK` switches 0 to approximately -0.8 V; `PB4/HOST_MISO` switches 0-0.2 V | This PASS is not valid proof of correct operation. The unpowered DAPLink 3.3 V output is being back-powered and/or loading the rail. A supply rail that follows the test signal is a severe power integrity fault. The STM32 can repeatedly brown out and accidentally cross the Teensy logic threshold, producing an apparent PASS. |
| 3 | Only Teensy USB connected to the PC; DAPLink 3V3 disconnected | `SCK_LOW->1 SCK_HIGH->1 FAIL`; maximum rail approximately 2.57 V; mainboard 3.3 V input switches 0-2.5 V; `PB3/HOST_SCK` switches approximately 0.3 to -0.65 V; `PB4/HOST_MISO` switches 0-2.57 V | The Teensy supply still collapses without DAPLink. This indicates an overloaded/shorted 3.3 V rail, a high-resistance/intermittent 3.3 V connector contact, incorrect connector orientation, or parasitic powering through a GPIO protection diode. A value near 2.5-2.6 V is consistent with a 3.2 V GPIO feeding an unpowered rail through a diode drop. |

The user subsequently confirmed that the measured nodes were PB3 and PB4; the earlier PA3/PA4 wording was only a naming error. These readings therefore apply directly to HOST_SCK and HOST_MISO.

#### Consolidated diagnosis

The original PB3/SCK problem was a connector solder fault and is now resolved. The present primary fault is power distribution, not SPI timing:

1. DAPLink 3V3 and Teensy 3.3 V must not be connected to the mainboard simultaneously.
2. An unpowered DAPLink must not remain attached through its 3V3 output.
3. The mainboard 3.3 V rail is not stable when powered from Teensy. It must not switch with SCK or fall to 2.1-3.0 V.
4. The single PASS in case 2 occurred under an invalid collapsing supply and must be treated as a false/accidental PASS.
5. The negative reported voltage requires oscilloscope verification with the probe ground connected directly to mainboard GND. If it is a real excursion rather than a reference/probe artefact, it indicates undershoot or ground bounce outside the intended 0-3.3 V range.

#### Safe next test

Do not continue the PB3-to-PB4 logic test until the power rail passes the following isolation test:

1. Disconnect DAPLink 3V3 permanently from the mainboard. For SWD, leave only GND, SWDIO, SWCLK, and dedicated NRST connected.
2. Disconnect all Teensy GPIO signal wires from the mainboard.
3. With all power removed, measure resistance from mainboard `+3.3V` to GND and inspect the recently repaired connector, especially pin 10 (`FPGA_3V3_IN`), for a bridge or high-resistance joint.
4. Power the mainboard from one current-limited, regulated 3.3 V source at `FPGA_3V3_IN`. Do not parallel it with either DAPLink or Teensy 3.3 V. Record the current consumption.
5. Confirm a stable 3.3 V at the source, J1 pin 10, and the STM32 VDD pins. The rail must remain stable while no GPIO is connected.
6. If the source remains 3.3 V but J1/VDD is lower, repair the power connector/trace. If the source itself enters current limit, locate the board short or overloaded device before reconnecting Teensy.
7. After stable power is established, connect common GND first, then SCK only, then MISO. Repeat the static 1 s low/high mirror test.
8. Measure PB4 directly at STM32 pin 41 and at the HOST_MISO connector. If pin 41 follows PB3 but the connector does not, repair the PB4-to-connector route. If pin 41 remains low with stable VDD, NRST high, and the mirror image confirmed running, investigate firmware execution or damage/clamping at PB4.

#### Firmware-aware analysis of the image under test

The firmware build and flash history were audited together with the power measurements.

##### Version state

- The actual build working copy is `D:\tmp\ESKIN_STM32_PATTERN`.
- Its archived `main.c` and the working-copy `main.c` are byte-identical.
- The latest ELF was built on 2026-07-25 at 15:09:01.
- Latest ELF SHA-256:

```text
D0AEAFCC181D5C9DBA3927BBC35269AB5E1E82E238BF8CBB2569CCD44D3A9C8C
```

- Disassembly of this ELF confirms that the final machine code configures PB3 as a no-pull input, PB4 as a push-pull output, reads PB3, writes that state to PB4, delays 1 ms, and repeats.
- The latest no-pull ELF was built, but the subsequent pyOCD attempt waited for a probe and did not report a successful flash. The last positively recorded flash was the earlier SCK mirror image. Therefore, until Flash is read back or the latest ELF is reflashed successfully, the MCU should be assumed to contain the earlier mirror variant with the PB3 internal pull-down.
- The pull-down difference does not explain PB4 remaining near 0.3 V. Teensy GPIO 13 actively drives PB3, and PB3 was measured switching between low and high. Both mirror variants configure PB4 as push-pull output and should produce a full logic level after the mirror loop is reached.

Direct read-back was not possible during this review because no CMSIS-DAP probe was detected. The system-default `D:\app\python\Scripts\pyocd.exe` installation also currently lacks the `colorama` and `psutil` dependencies, so its environment must be repaired before a binary read-back comparison.

##### Startup path relevant to PB4

The mirror operation is not the first code executed. The current image performs:

```text
HAL_Init
  -> SystemClock_Config
  -> MX_GPIO_Init
  -> MX_SPI1_Init
  -> MX_SPI3_Init
  -> reconfigure PB3/PB4 as GPIO
  -> PB3-to-PB4 mirror loop
```

`SystemClock_Config`, `MX_SPI1_Init`, and `MX_SPI3_Init` call `Error_Handler()` on failure. `Error_Handler()` disables interrupts and loops forever, with no heartbeat or error output. If the 3.3 V rail collapses, the MCU may repeatedly brown out, reset, or stop in an initialisation failure before PB4 is configured as a push-pull output. In that state PB4 can be high-impedance and a meter may report an invalid residual value such as 0.3 V.

This code path matches the measurements:

- PB3 switching proves that the Teensy signal reaches the STM32 package pin after the connector repair.
- It does not prove that the STM32 reached the mirror loop.
- PB4 at approximately 0.3 V is consistent with the MCU not reaching the PB4 reconfiguration, repeated brownout/reset, an external clamp, or a PB4 route fault.
- The collapsing 3.3 V rail is therefore sufficient to invalidate the GPIO test even though the mirror source code and ELF machine code are correct.

##### Additional reset configuration issue

The current `.ioc` contains:

```text
PG10-NRST.Signal=GPIO_Output
```

Generated code labels PG10-NRST as `NRST`, writes it high, and attempts to initialise it as push-pull output. On STM32G4, the physical PG10-NRST pad mode is selected by the `NRST_MODE` option bytes. With the production-default reset mode, GPIO functionality is unavailable and the external active-low reset remains functional. If `NRST_MODE` is ever changed to GPIO mode, DAPLink nRESET can no longer reset the target and the generated push-pull-high configuration can conflict with an external reset driver.

For reliable DAPLink operation, reserve PG10-NRST as the reset input/output and remove its `GPIO_Output` configuration in the `.ioc`. This is a genuine configuration defect, but it does not by itself explain PB4 at 0.3 V if the option bytes remain in their default reset mode.

##### Code-aware next validation

After the mainboard has one stable 3.3 V source:

1. Restore pyOCD operation and connect the probe without its 3V3 output.
2. Reflash the latest no-pull ELF and record a successful verification, or read back Flash and compare it with the SHA-256-identified ELF.
3. Use a minimal GPIO-only diagnostic that does not initialise SPI1 or SPI3 before the mirror test.
4. Add a separate heartbeat output after PB3/PB4 initialisation. A heartbeat proves that the MCU reached and remains in the mirror loop; absence of heartbeat distinguishes reset/init failure from a PB4 trace fault.
5. Correct `PG10-NRST` in the `.ioc` before regenerating production code.

#### Ideal behaviour with the available Teensy-only supply

The user does not currently have an independent current-limited bench supply. If the Teensy 4.1 must temporarily power the mainboard, DAPLink 3V3 must remain disconnected. The ideal test configuration is:

- Teensy USB connected to the PC.
- Teensy 3.3 V connected to mainboard J1 pin 10 (`FPGA_3V3_IN`).
- Teensy GND connected to mainboard J1 pin 8 or pin 9.
- Teensy GPIO 13 connected to mainboard HOST_SCK/PB3.
- Teensy GPIO 12 connected to mainboard HOST_MISO/PB4.
- DAPLink 3V3 disconnected; preferably disconnect the complete DAPLink during this GPIO-only test.

Expected electrical behaviour:

| Node | Expected low phase | Expected high phase |
|---|---:|---:|
| Teensy 3.3 V | Stable approximately 3.2-3.3 V | Stable approximately 3.2-3.3 V |
| Mainboard J1 pin 10 / STM32 VDD | Stable approximately 3.2-3.3 V | Stable approximately 3.2-3.3 V |
| Teensy GPIO 13 | Approximately 0 V for 1 s | Approximately 3.2-3.3 V for 1 s |
| STM32 PB3/HOST_SCK | Approximately 0 V for 1 s | Approximately 3.2-3.3 V for 1 s |
| STM32 PB4/HOST_MISO | Approximately 0 V | Approximately STM32 VDD, following PB3 within approximately 1 ms |
| Teensy GPIO 12 | Logic 0 | Logic 1 |
| STM32 PG10-NRST | Stable high, close to STM32 VDD | Stable high, close to STM32 VDD |

Expected serial output at 115200 baud:

```text
Teensy HOST_SCK retest: GPIO12 should follow GPIO13
SCK_LOW->0 SCK_HIGH->1  PASS
SCK_LOW->0 SCK_HIGH->1  PASS
...
```

The mainboard 3.3 V rail must not change with SCK. PB3 or PB4 must not become negative. `PB4 approximately 0.3 V`, `SCK_LOW->1`, `SCK_HIGH->0`, a mainboard rail below approximately 3.1 V, or a cycling rail are all invalid results.

The currently compiled no-pull image and the most likely currently flashed pull-down mirror image have the same ideal serial result. The only difference is the weak internal PB3 pull-down; it cannot hold PB3 low against an actively driven Teensy GPIO 13.

Because the Teensy 4.1 external 3.3 V budget is limited, disconnect power immediately if its 3.3 V pin or the mainboard rail falls below approximately 3.1 V, cycles with GPIO activity, or any component becomes warm. Do not reconnect DAPLink 3V3 to raise the voltage.

#### Root-cause analysis of the Teensy 3.3 V voltage drop

A normal PB3/PB4 GPIO transition cannot pull a healthy 3.3 V rail from approximately 3.2 V down to 2.2-2.6 V. The observations indicate a power-path fault or excessive load.

Ranked causes:

1. **Confirmed source contention/back-power when DAPLink 3V3 is attached.** DAPLink 3V3 and Teensy 3.3 V are connected to the same mainboard rail without isolation. When both probes are USB-powered, their regulators can source current into each other. When DAPLink USB is removed but its 3V3 wire remains connected, Teensy can reverse-power the unpowered DAPLink. This matches the worst approximately 2.2 V case.
2. **Probable mainboard overload or partial short.** With DAPLink completely removed, the Teensy rail still fell to approximately 2.57 V. If the voltage measured directly at the Teensy 3.3 V pin also falls, the Teensy regulator is being overloaded or entering current/thermal protection.
3. **Probable high-resistance 3.3 V connection.** A Teensy-side value near 3.2 V and a mainboard-side value near 2.9 V means approximately 0.3 V is being lost in the wire, connector, solder joint, or PCB route. The already-confirmed HOST connector solder fault makes J1 pin 10 another high-priority inspection point.
4. **Possible GPIO parasitic powering.** If J1 pin 10 is open, intermittent, reversed, or highly resistive, a high PB3/CS/MOSI signal can feed the STM32 VDD rail through an input protection diode. A rail around 2.5-2.6 V is compatible with a 3.2 V GPIO minus a diode drop. A mainboard 3.3 V rail that follows SCK is strong evidence for this condition.
5. **Possible USB input or measurement-reference problem.** If Teensy VUSB/VIN also falls, the USB cable, hub, or PC port is limiting current. Negative PB3 measurements can also be exaggerated by a wrong oscilloscope ground or long probe ground lead, but this cannot explain a confirmed drop measured directly on the Teensy 3.3 V pin.

Teensy-only discrimination test:

| Step | Connections | Interpretation |
|---|---|---|
| A | Teensy USB only; nothing connected to 3.3 V or GPIO | Record Teensy 3.3 V and VUSB/VIN. This is the no-load baseline. |
| B | Connect only Teensy GND and 3.3 V to mainboard GND and J1 pin 10; no GPIO and no DAPLink | If Teensy 3.3 V falls, the mainboard load/short is the primary problem. If Teensy stays stable but J1/VDD is lower, the power connector/path has excessive resistance. |
| C | If step B is stable, add GPIO13-to-PB3 only | If the rail now falls or follows GPIO13, the board was not correctly powered through J1 pin 10 or PB3 is shorted/clamped. |
| D | If step C is stable, add PB4-to-GPIO12 | If the rail now falls, inspect the PB4/MISO route and confirm GPIO12 remains configured as input. |

Also measure Teensy VUSB/VIN during step B. Stable approximately 5 V with a collapsing 3.3 V output points to Teensy regulator overload; a collapsing VUSB/VIN points to the USB cable/port or upstream supply.

#### Live boot-source diagnosis: PB4 stuck near 0.3 V (2026-07-25)

Observed condition:

- DAPLink 3.3 V was disconnected from the mainboard.
- The Teensy remained the only board power source.
- PB4/HOST_MISO stayed near 0.3 V instead of mirroring PB3/HOST_SCK.

Live inspection through DAPLink, without initially resetting or flashing the MCU, produced the following results:

- No residual `pyocd`, GDB server, or GDB process was running.
- The CMSIS-DAP probe was detected normally.
- The STM32 core was running, but `PC = 0x1FFF3B26` and `VTOR = 0x1FFF0000`.
- These addresses are inside the STM32 system-memory bootloader, not the user application in internal Flash.
- The internal Flash still contained a valid vector table (`SP = 0x20020000`, reset handler `0x08001C61`).
- GPIOB showed PB3 and PB4 both in alternate-function mode rather than the intended test configuration.
- Therefore PB4 was not being driven by the mirror firmware. The measured approximately 0.3 V was a high-impedance residual/clamped voltage, not a valid GPIO logic-high output.

The option-byte value was read as `FLASH_OPTR = 0xFFEFF8AA`, including:

- `nSWBOOT0 = 1`: the boot source is selected from the physical BOOT0 pin at reset.
- `nBOOT1 = 1`: a high BOOT0 level selects system memory.
- On this board, PB8 is shared with `HOST_IRQ` and the STM32G4 BOOT0 function.

Confirmed cause:

- PB8/HOST_IRQ was sampled high or was insufficiently defined during the earlier power-up/reset sequence.
- The MCU consequently started the ROM system bootloader instead of the user Flash firmware.
- Disconnecting DAPLink 3.3 V removed one power source, but did not by itself change the already sampled boot mode.

Recovery performed:

1. Confirmed PB8 was low at the time of the recovery.
2. Issued one normal STM32 reset through DAPLink; no erase or reprogramming was performed.
3. After reset, the program counter moved to `0x080004F0`, confirming execution from user Flash.
4. GPIO configuration after boot was verified:
   - PB3: input with pull-down.
   - PB4: GPIO output.
5. The target was resumed and left running.

Expected result after recovery:

- Teensy drives SCK/PB3 low: PB4 should read low.
- Teensy drives SCK/PB3 high: PB4 should read high.
- The Teensy test should report `SCK_LOW->0 SCK_HIGH->1 PASS`.

Permanent prevention:

- Preferred hardware solution: fit an approximately 10 kOhm pull-down from PB8/HOST_IRQ/BOOT0 to GND so the boot pin is defined during every power-up. This draws only about 0.33 mA when STM32 later drives HOST_IRQ high.
- Firmware-option solution: program the option bytes so boot from main Flash does not depend on the physical PB8 level (`nSWBOOT0 = 0`, with the software BOOT0 selection set for main Flash). This is a persistent device configuration change and should only be performed deliberately.
- Do not actively drive PB8 low from Teensy after normal startup because PB8 is also an STM32 HOST_IRQ output and active opposing outputs would create contention.

If PB4 still measures approximately 0.3 V after the confirmed Flash boot, the remaining check is hardware-only: disconnect Teensy GPIO12/MISO and compare the voltage directly at STM32 PB4 (package pin 41) with the HOST_MISO connector pad. A low voltage at only one point would distinguish an open connection from an external clamp.

#### CLK resolution and complete SPI pattern restoration (2026-07-25)

Final CLK result:

- The user confirmed that the HOST_SCK/CLK problem was resolved.
- This closes the PB3/HOST_SCK connector-path diagnostic.
- The GPIO-only PB3-to-PB4 mirror programs were removed from the active isolated test sources.

Restored STM32 test:

- Project source: `SPI_PATTERN_TEST/ESKIN_STM32_PATTERN/Core/Src/main.c`.
- SPI3 remains an 8-bit, MSB-first, Mode-0 slave with hardware NSS.
- PA15 = SPI3_NSS, PB3 = SPI3_SCK, PB4 = SPI3_MISO, and PB5 = SPI3_MOSI, all using AF6.
- PB8/HOST_IRQ is low at idle, goes high immediately before the one-byte blocking transfer, and returns low after completion or timeout.
- Each transaction returns `0x55` (`01010101`) through `HAL_SPI_TransmitReceive()`.
- A 1000 ms STM32 timeout, `HAL_SPI_Abort()` recovery, and a 10 ms IRQ-low interval are retained.

Restored Teensy test:

- Sketch: `SPI_PATTERN_TEST/teensy_spi_pattern_receiver/teensy_spi_pattern_receiver.ino`.
- Teensy 4.1 is the SPI master: GPIO10 = CS, GPIO13 = SCK, GPIO12 = MISO, GPIO11 = MOSI, and GPIO2 = HOST_IRQ.
- SPI parameters are 100 kHz, MSB first, and Mode 0.
- GPIO2 is configured as `INPUT_PULLDOWN` to reduce the risk of PB8/BOOT0 floating during an STM32 reset.
- The receiver waits for IRQ high, applies 1 ms setup delays, clocks one byte, waits for IRQ low, and prints the received byte over USB serial.

Build and flashing record:

1. The long-path OneDrive build directory was missing `build.ninja`.
2. Reconfiguration in that directory failed because the generated compiler-test object path exceeded the Windows/CMake safe object-path length.
3. The restored STM32 source was synchronised to the existing short-path working copy at `D:\tmp\ESKIN_STM32_PATTERN`.
4. The short-path CMake/Ninja build passed:
   - RAM used: 1792 bytes.
   - Flash used: 10060 bytes.
   - The unused FSR-production helper warnings are expected because this is an isolated SPI test.
5. The resulting ELF was flashed with pyOCD at 100 kHz using `under-reset`:
   - Erased: 10240 bytes in 5 sectors.
   - Programmed: 10240 bytes in 10 pages.
   - Identical: 0 bytes.
6. A post-flash live check briefly halted and then resumed the target:
   - `PC = 0x08000794`, proving execution from user Flash.
   - GPIOB MODER/AFR confirmed PB3, PB4, and PB5 in SPI3 AF6 mode.
7. The Teensy sketch compiled successfully with `teensy:avr` core 1.62.0 and was uploaded to the detected Teensy 4.1 at `Port_#0004.Hub_#0002`.

Artifact identities:

```text
STM32 main.c SHA-256:
5B49E9174AE3ED018EB09FE24F3CB37FFDF9E8E504B43CEB22D5E6977E217968

STM32 ELF SHA-256:
5ABC30F14BFF51A4FF2CD47AC6A8EA678463BBC9EC14FA6EF086BDD9D2387946

Teensy receiver SHA-256:
5C92CCAC37B33AD41C131CB061164B62B0271157EDEDE3371A89849A880B7155
```

End-to-end result:

COM9 was opened for approximately five seconds after both uploads. The startup line was correct and eight consecutive successful transactions were captured:

```text
Teensy SPI pattern receiver: expecting STM32 0x55
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
RX: 0x55  bits=01010101  PASS
```

Conclusion:

- HOST_IRQ, HOST_nCS, HOST_SCK, HOST_MISO, SPI Mode 0 timing, the STM32 SPI3 slave, the Teensy SPI master, and Teensy USB serial output are now validated together.
- This test does not yet validate SPI1/MAX11633, MUX1 scanning, FSR samples, or the 515-byte production frame.
- PB8/BOOT0 still requires a permanent hardware pull-down or a deliberate option-byte configuration for guaranteed cold-boot selection.

## Update policy

After any firmware, protocol, pin assignment, build, flashing, or PC-parser change, update this file with:

- date and change summary;
- affected board/firmware version;
- protocol changes and compatibility notes;
- validation result and any known issue;
- next action.

## Change log

| Date | Change | Validation | Next action |
|---|---|---|---|
| 2026-07-23 | Created firmware workflow document. | Pending hardware implementation. | Define the first SPI frame specification. |
| 2026-07-23 | Added Visual Studio Code, pyOCD, and STM32G4 CMSIS-Pack preparation steps. | pyOCD 0.45.1 installed; STM32G4xx DFP 2.2.0 installed; no DAPLink probe connected during verification. | Connect DAPLink and verify the STM32G474 target. |
| 2026-07-23 | Corrected `ESKIN_STM32.ioc`: SPI3 is now a hardware-NSS slave for the Teensy host link; both SPI peripherals use 8-bit data; MUX1 and external ADC CS/EOC labels were aligned with the mainboard schematic; HOST_SYNC is configured as a GPIO input. | Configuration file checked; code regeneration and hardware validation pending. | Open the `.ioc` in STM32CubeMX, review the pinout, regenerate code, then build and test with DAPLink. |
| 2026-07-23 | Added a blocking 16 x 16 FSR acquisition prototype using MUX1, SPI1, MAX11633 AIN0--AIN15 scan, and EOC polling. | Source-level review completed; hardware and build validation pending. | Verify MAX11633 command timing, then add SPI3 frame output and DMA. |
| 2026-07-23 | Documented the Windows CMake, ARM GNU Toolchain, and Ninja installation, PATH entries, verification commands, and CMake build commands. | Documentation update completed; local command availability still needs verification. | Install the tools, restart VS Code, and run the four version checks. |
| 2026-07-23 | Flashed `build\Debug\ESKIN_STM32.elf` to the STM32G474CET6 through the Jixin.pro CMSIS-DAP/DAPLink probe using pyOCD. | Erase and programming completed at 100%; pyOCD reported 11 pages programmed with no errors. | Reset the board, start a debug session, and verify the 16 x 16 FSR scan at runtime. |
| 2026-07-23 | Added the first STM32 SPI3-to-Teensy frame transport and created the Teensy 4.1 Arduino bridge sketch. | STM32 CMake build passed; physical SPI and USB validation pending. | Install Teensy board support, upload the bridge sketch, and validate the 515-byte binary frame. |
| 2026-07-23 | Flashed the updated STM32 SPI3/Teensy transport firmware using pyOCD at 100 kHz with `under-reset` connection. | Erased 5 sectors and programmed 9 pages successfully; 2 pages were already identical. | Connect the Teensy bridge and validate the HOST_IRQ/SPI3 frame transfer. |
| 2026-07-23 | Reviewed the STM32 scan/transport code and added retry recovery after ADC or host timeout, then rebuilt and reflashed the image. | CMake build passed; pyOCD erased 5 sectors and programmed 9 pages successfully at 100 kHz under reset. | Debug `fsr_scan_status`, confirm MAX11633 EOC activity, and validate the Teensy frame. |
| 2026-07-24 | Created an isolated STM32 SPI pattern test project and Teensy receiver. The STM32 test returns `0x55` (`01010101`) as an SPI3 slave; the Teensy clocks one byte at 100 kHz and reports PASS/FAIL over USB. | Test project compiled successfully from `D:\tmp\ESKIN_STM32_PATTERN` (RAM 1784 B, Flash 8712 B). Flashing failed at both 100 kHz and 10 kHz with `SWD/JTAG communication failure (No ACK)` during `DebugPortSetup`; no test image was written. | Restore stable DAPLink/SWD communication, then flash `D:\tmp\ESKIN_STM32_PATTERN\build\Debug\ESKIN_STM32.elf` and run the Teensy receiver. |
| 2026-07-24 | Reflashed the isolated SPI pattern firmware after SWD wiring/connection was repaired. | pyOCD at 100 kHz with `under-reset` succeeded: erased 10240 bytes, programmed 9216 bytes (9 pages), with no errors. | Upload the Teensy receiver sketch, open its USB serial monitor at 115200 baud, and check for `0x55 / PASS`. |
| 2026-07-24 | Revised the isolated SPI pattern test to remove the STM32 polling delay and add a HOST_IRQ ready handshake. STM32 raises PB8 before `HAL_SPI_TransmitReceive`; Teensy waits on GPIO 2 before asserting CS and clocking. | Source modification completed; rebuild, reflash, and Teensy runtime validation pending. | Build and flash the revised test, upload the revised Teensy sketch, and check `0x55 / PASS` or an IRQ timeout. |
| 2026-07-24 | Built and reflashed the HOST_IRQ-synchronised SPI pattern test. | CMake build passed (RAM 1784 B, Flash 8664 B); pyOCD at 100 kHz under reset erased 10240 bytes and programmed 9216 bytes successfully. Teensy runtime result pending. | Upload the revised Teensy sketch and check the serial monitor for `RX: 0x55 ... PASS`. |
| 2026-07-24 | Measured the mainboard `HOST_IRQ` line during the synchronised test. | `HOST_IRQ` remained continuously at approximately 3.3 V, consistent with STM32 PB8 running the test firmware and waiting inside the SPI3 receive call. Teensy reported `IRQ timeout`, so the Teensy GPIO 2 input path or connector routing remains unresolved. | Measure Teensy GPIO 2, verify direct continuity from mainboard HOST_IRQ/Pin 3 to GPIO 2, use `pinMode(2, INPUT)`, then perform one SPI transfer while IRQ is high. |
| 2026-07-24 | Replaced the SPI handshake loop in the isolated STM32 test with a GPIO-only HOST_IRQ test. PB8 is driven high continuously and SPI is not used. | Source modified; build and flash pending. This isolates the HOST_IRQ wiring from SPI timing. | Flash the GPIO-only image, run the Teensy digital-input test, and verify GPIO 2 reads `1`. |
| 2026-07-24 | Built and flashed the GPIO-only HOST_IRQ test. | CMake build passed (RAM 1784 B, Flash 6900 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 7168 bytes successfully. | Run the Teensy GPIO 2 digital-input sketch and verify it reads `1`; if it reads `0`, trace the physical HOST_IRQ route. |
| 2026-07-24 | GPIO-only HOST_IRQ test completed, then restored the SPI pattern test source. | Teensy GPIO 2 read `1`, confirming the HOST_IRQ electrical path. SPI rebuild and flash pending. | Build and flash the SPI pattern image, upload the full Teensy SPI receiver, and verify `RX: 0x55 ... PASS`. |
| 2026-07-24 | Rebuilt and reflashed the SPI pattern image after the HOST_IRQ GPIO path passed. | CMake build passed (RAM 1784 B, Flash 8664 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 8192 bytes successfully. | Upload the full Teensy SPI receiver and check whether the STM32 returns `0x55`. |
| 2026-07-24 | Replaced the SPI pattern with a GPIO-only MISO test. STM32 PB4/HOST_MISO is configured as push-pull high; Teensy GPIO 12 reads the level and reports PASS/FAIL. | Source modified; build and flash pending. | Flash the MISO GPIO image, upload the revised Teensy sketch, and record whether GPIO12 reads `1`. |
| 2026-07-24 | Built and flashed the GPIO-only MISO test. STM32 PB4/HOST_MISO is now driven high; Teensy GPIO 12 test sketch reports the input level. | CMake build passed (RAM 1784 B, Flash 6936 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 7168 bytes successfully. Teensy runtime result pending. | Upload the MISO GPIO sketch and check for `GPIO12=1 PASS`. |
| 2026-07-24 | Completed the GPIO-only MISO test. | Teensy reported `GPIO12=1 PASS`, confirming the PB4/HOST_MISO physical route and logic level. | Restore the SPI3 `0x55` slave firmware and the Teensy SPI receiver, then test CS/SCK timing and received data. |
| 2026-07-24 | Restored the SPI3 `0x55` slave firmware and full Teensy SPI receiver after the MISO GPIO test passed. | Source update completed; build, flash, and runtime result pending. | Build and flash the SPI image, upload the Teensy receiver, and capture the serial result. |
| 2026-07-24 | Built and reflashed the restored SPI3 `0x55` test firmware. | CMake build passed (RAM 1784 B, Flash 8664 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 8192 bytes successfully. | Upload the full Teensy SPI receiver and record the actual `PASS`, `FAIL`, or timeout output. |
| 2026-07-24 | For the reported SPI `FAIL` after HOST_IRQ and MISO GPIO tests passed, changed only the isolated test's SPI3 NSS from hardware input to software NSS. | Source modified; build, flash, and runtime result pending. This isolates a possible hardware-NSS handling issue while retaining the external CS wire. | Build and flash the software-NSS image, then rerun the Teensy `0x55` receiver. |
| 2026-07-24 | Built and flashed the software-NSS diagnostic image. | CMake build passed (RAM 1784 B, Flash 8668 B); pyOCD at 100 kHz under reset erased 10240 bytes and programmed 9216 bytes successfully. | Run the unchanged Teensy SPI receiver and report the exact serial line (`PASS`, `FAIL`, or `IRQ timeout`). |
| 2026-07-24 | Because the software-NSS SPI attempt still reported `FAIL` while HOST_IRQ and MISO GPIO tests passed, replaced the isolated firmware with a GPIO-only HOST_nCS test. STM32 reads PA15 and mirrors active-low CS onto PB4; Teensy drives CS and reads GPIO12. | Source modified; build, flash, and runtime result pending. | Flash the CS test image, upload the revised Teensy sketch, and record `CS_LOW -> GPIO12=1 PASS` or `FAIL`. |
| 2026-07-24 | Built the HOST_nCS diagnostic image and attempted to flash it. | Build passed (RAM 1784 B, Flash 7524 B). Flash did not complete because pyOCD returned `Unexpected ACK '5'`/`No ACK` during target discovery and reset; no conclusion about CS can be drawn from this attempt. | Restore stable SWD communication, flash the CS diagnostic, then run the Teensy CS test. |
| 2026-07-24 | Retried flashing the HOST_nCS diagnostic image after SWD reset. | pyOCD at 100 kHz under reset succeeded: erased 8192 bytes and programmed 8192 bytes (8 pages). | Upload the HOST_nCS Teensy test and check `CS_LOW -> GPIO12=1 PASS`. |
| 2026-07-24 | Observed intermittent `CS_LOW -> GPIO12=1 PASS`/`0 FAIL` during the HOST_nCS test. | Root cause identified as test timing: Teensy held CS low for only 100 us while STM32 polled PA15 every 1 ms, so the polling loop often missed the active window. | Updated Teensy CS diagnostic to hold CS low for 20 ms, then repeat the test. |
| 2026-07-24 | Completed the corrected HOST_nCS GPIO test. | Results became stable `CS_LOW -> GPIO12=1 PASS`, confirming the CS route and proving the earlier intermittent result was only a polling-window artifact. | Restore the SPI3 `0x55` test and use the verified CS/SCK wiring and timing. |
| 2026-07-24 | Restored the SPI3 `0x55` STM32 test and Teensy SPI receiver after the HOST_nCS test passed. | Source update completed; build, flash, and runtime validation pending. | Build/flash the SPI image and record the Teensy received byte. |
| 2026-07-24 | Built and flashed the SPI3 `0x55` test using hardware NSS (`PA15`) after the CS GPIO test passed; Teensy receiver was restored. | CMake build passed (RAM 1784 B, Flash 8664 B); pyOCD at 100 kHz under reset erased 10240 bytes and programmed 9216 bytes successfully. | Run the Teensy SPI receiver and record the exact received byte. |
| 2026-07-24 | SPI still reported `FAIL` after HOST_IRQ, MISO, and CS GPIO tests passed. Replaced the isolated firmware with a GPIO-only HOST_SCK test: STM32 mirrors PB3 to PB4, while Teensy toggles GPIO13 and reads GPIO12. | Source modified; build, flash, and runtime result pending. | Flash the SCK diagnostic and record `SCK_LOW->0 SCK_HIGH->1 PASS` or `FAIL`. |
| 2026-07-24 | Built and flashed the HOST_SCK GPIO diagnostic image. | CMake build passed (RAM 1784 B, Flash 7496 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 8192 bytes successfully. | Upload the SCK diagnostic Teensy sketch and record whether GPIO12 follows GPIO13. |
| 2026-07-24 | Completed the HOST_SCK GPIO diagnostic. | Teensy reported `SCK_LOW->0 SCK_HIGH->0 FAIL` repeatedly. STM32 PB3 did not detect the toggling level, so the HOST_SCK route/connector mapping is not reaching PB3; SPI configuration is not yet the primary fault. | Test Teensy GPIO13 with a direct jumper to GPIO12, then use continuity mode to identify the actual mainboard connector pin connected to PB3. |
| 2026-07-24 | Teensy GPIO13-to-GPIO12 self-loop test passed. A multimeter measured approximately 3.0 V on GPIO13 during the fast 5 ms toggle test. | Self-loop confirms Teensy GPIO13/12 and the sketch work; 3.0 V is an averaged meter reading and does not establish a static level. | Changed the Teensy SCK diagnostic to hold each level for 1 s so GPIO13 and the mainboard HOST_SCK connector can be measured directly. |
| 2026-07-24 | HOST_SCK low/high level tests both passed through the mainboard to STM32 PB3. Restored the SPI3 pattern and slowed Teensy SPI to 10 kHz with 1 ms setup delays before CS and transfer. | Source modified; build, flash, and runtime result pending. | Flash the slow-SPI image and record the exact received byte. |
| 2026-07-24 | Built and flashed the slow-timing SPI diagnostic. | CMake build passed (RAM 1784 B, Flash 8664 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 8192 bytes successfully. | Upload the slow SPI Teensy sketch and record the exact received byte. |
| 2026-07-24 | Reviewed the latest serial screenshot. | The Teensy was still running the previous HOST_SCK GPIO diagnostic (`SCK_LOW->0 SCK_HIGH->0 FAIL`), not the slow SPI receiver; therefore no new SPI result was obtained. | Upload the current `Teensy slow SPI test` sketch and verify its startup message before judging SPI. |
| 2026-07-24 | Received the actual SPI receiver output: repeated `RX: 0x00 bits=00000000 FAIL`. With IRQ, MISO, CS, and SCK GPIO tests passed, changed the isolated STM32 slave operation from `HAL_SPI_TransmitReceive()` to transmit-only `HAL_SPI_Transmit()` for a one-byte `0x55` test. | Source modified; build, flash, and runtime result pending. | Build/flash the transmit-only image and rerun the Teensy SPI receiver. |
| 2026-07-24 | Built and reflashed the transmit-only SPI diagnostic. | CMake build passed (RAM 1784 B, Flash 8320 B); pyOCD at 100 kHz under reset erased 10240 bytes and programmed 9216 bytes successfully. | Run the existing slow Teensy SPI receiver and record whether the byte changes from `0x00`. |
| 2026-07-24 | User requested a repeat CLK test after transmit-only SPI still failed. Restored the GPIO-only PB3-to-PB4 SCK mirror and 1 s Teensy GPIO13 toggle test. | Source modified; build, flash, and runtime result pending. | Flash the SCK retest image and record `SCK_LOW->0 SCK_HIGH->1 PASS` or `FAIL`. |
| 2026-07-24 | Built and reflashed the repeated HOST_SCK diagnostic image. | CMake build passed (RAM 1784 B, Flash 7496 B); pyOCD at 100 kHz under reset erased 8192 bytes and programmed 8192 bytes successfully. | Upload the 1 s SCK retest sketch and record the new `SCK_LOW/SCK_HIGH` result. |
| 2026-07-24 | Repeated HOST_SCK test produced one `SCK_LOW->0 SCK_HIGH->1 PASS` followed by repeated `SCK_LOW->0 SCK_HIGH->0 FAIL`. | Because each level is held for 1 s, this is not a polling-window race; it indicates an intermittent/open SCK connection, incorrect connector contact, or the high level being pulled down at the mainboard endpoint. | Measure GPIO13, the HOST_SCK connector pin, and STM32 PB3 during the 1 s high interval; wiggle the cable and verify continuity under movement. |
| 2026-07-24 | Restored the production STM32 firmware after the temporary GPIO/SPI diagnostics. | Flashed the original `ESKIN_STM32\build\Debug\ESKIN_STM32.elf` at 100 kHz under reset successfully: erased 10240 bytes, programmed 10240 bytes (10 pages), 1 page identical. | Upload the production Teensy SPI-to-USB bridge sketch and resume normal FSR/USB testing. |
| 2026-07-24 | Restored the initial isolated SPI test version on request: STM32 sends one `0x55` byte as SPI3 slave with HOST_IRQ ready handshake; Teensy uses SPI Mode 0 at 100 kHz. | CMake build passed (RAM 1784 B, Flash 8664 B); pyOCD at 100 kHz under reset erased 10240 bytes and programmed 9216 bytes successfully. | Upload the matching initial Teensy pattern receiver and run the test. |
| 2026-07-24 | Initial SPI test output changed from repeated `RX: 0x00 FAIL` to `IRQ timeout: STM32 is not ready`. | This indicates STM32 no longer asserts HOST_IRQ or is no longer executing the test loop; it is not a new MISO-only failure. | Measure HOST_IRQ at the mainboard and Teensy during timeout, then check STM32 VDD/NRST and reset/reflash if PB8 is low. |
| 2026-07-24 | Modified the initial Teensy SPI receiver for a timing-controlled retry: SPI reduced to 10 kHz, 1 ms delay after HOST_IRQ, and 1 ms delay after CS assertion. | Source modification completed; Teensy upload and runtime result pending. | Upload this exact sketch and verify the startup message and received byte. |
| 2026-07-24 | Slow SPI retry still reported `0x00 FAIL`; added STM32 HAL status instrumentation. PB9/HOST_SYNC is temporarily an output: high means `HAL_SPI_TransmitReceive()` returned `HAL_OK`, low means an error/timeout. | Source modified; build, flash, and status measurement pending. | Build/flash the instrumented test, run one Teensy transfer, and measure PB9/HOST_SYNC. |
| 2026-07-24 | Built and flashed the HAL-status instrumented SPI test. | CMake build passed (RAM 1784 B, Flash 9288 B); pyOCD at 100 kHz under reset erased 10240 bytes and programmed 10240 bytes successfully. | Run one Teensy SPI transfer and measure PB9/HOST_SYNC: high means HAL_OK, low means HAL error/timeout. |
| 2026-07-24 | PB9 measured `0 V` after the instrumented transfer, proving the HAL transaction did not return `HAL_OK`. Changed PB9 encoding to distinguish timeout from other HAL errors: high=`HAL_TIMEOUT`, low=`HAL_ERROR`/other. | Source modified; rebuild, flash, and measurement pending. | Build/flash the error-classification image and measure PB9 again after one transfer. |
| 2026-07-24 | Built and flashed the HAL error-classification image. | CMake build passed (RAM 1784 B, Flash 9288 B); pyOCD erased 2048 bytes and programmed 2048 bytes successfully (remaining pages identical). | Run one SPI transfer and measure PB9: high indicates timeout, low indicates HAL error/other. |
| 2026-07-24 | Measured PB9 after a Teensy SPI transfer. | PB9 was approximately 3.2 V, identifying `HAL_TIMEOUT`: the STM32 SPI peripheral did not recognise a complete NSS/SCK transaction, despite the GPIO-level tests seeing the signals. | Inspect SPI alternate-function configuration and NSS/SCK peripheral timing; GPIO continuity alone is not sufficient. |
| 2026-07-24 | User requested a return to the SCK test. Restored STM32 PB3-to-PB4 GPIO mirroring and the Teensy 1 s GPIO13 toggle sketch. | Source modified; build, flash, and runtime validation pending. | Build/flash the SCK test and record the new result. |
| 2026-07-24 | Built the restored SCK test but the first reflash attempt failed. | CMake build passed (RAM 1784 B, Flash 7496 B); pyOCD returned `SWD/JTAG communication failure (No ACK)` before programming. | Reset/reconnect DAPLink and retry the SCK image flash. |
| 2026-07-24 | Retried flashing the restored SCK GPIO test. | pyOCD at 100 kHz under reset succeeded: erased 8192 bytes and programmed 8192 bytes (8 pages). | Upload the matching 1 s Teensy SCK test and report `SCK_LOW/SCK_HIGH`. |
| 2026-07-24 | Latest SCK retest reported `FAIL`, and no measurable voltage was found on the expected MISO/SCK test point. | This suggests PB4 is not being driven by the running GPIO test (target not running/reset, wrong physical test point, or firmware/sketch mismatch), rather than a valid logic-low result. | Measure STM32 VDD and NRST first, then measure PB4 directly against STM32 GND while the test is running. |
| 2026-07-24 | Reflashed the SCK GPIO test after confirming VDD and NRST were normal. | pyOCD completed successfully; the image was already identical: erased 0 bytes, programmed 0 bytes, identical 8192 bytes (8 pages). | Release reset, run the matching SCK Teensy sketch, and measure PB4/HOST_MISO directly during the 1 s high phase. |
| 2026-07-24 | Measured NRST high while SCK remained low. | STM32 is out of reset, but HOST_SCK/PB3 is not receiving a high level during the test; this points to the Teensy sketch/pin, connector route, or wrong physical HOST_SCK pin. | Measure Teensy GPIO13 and the mainboard HOST_SCK pin simultaneously during the 1 s high interval. |
| 2026-07-24 | Measured approximately 3.3 V at Teensy GPIO13, HOST_SCK, and STM32 PB3 while the SCK test still reported failure. | SCK input path is now confirmed; the remaining check is STM32 PB4 mirror output and the PB4-to-Teensy GPIO12 path, or whether the flashed image is actually executing its mirror loop. | Measure PB4/HOST_MISO directly during the same high interval and compare it with Teensy GPIO12. |
| 2026-07-24 | SCK test continued to report `SCK_LOW->0 SCK_HIGH->0 FAIL`. | Teensy GPIO12 remains low in both phases, so PB4/HOST_MISO is not observed high or the Teensy is not running the expected mirror-test sketch. | Measure PB4 directly during SCK high; verify the serial startup text is `Teensy HOST_SCK retest: GPIO12 should follow GPIO13`. |
| 2026-07-24 | Measured HOST_MISO at approximately 0.35 V continuously. | This is not a valid STM32 push-pull logic level; it suggests PB4 is high-impedance, the measured point is not PB4, or the HOST_MISO net is externally clamped/loaded. | Measure the STM32 PB4 package pin directly, then compare with the connector HOST_MISO pin; disconnect Teensy temporarily if needed to identify an external clamp. |
| 2026-07-25 | Repeated the STM32 PB3-to-PB4 SCK mirror test. STM32 reads PB3/HOST_SCK as GPIO input and continuously writes that state to PB4/HOST_MISO; Teensy toggles GPIO13 for 1 s low/high and reads GPIO12. | Reflash completed successfully; image was identical (erased 0 bytes, programmed 0 bytes, identical 8192 bytes). Runtime result pending the new Teensy test run. | Upload/run the matching SCK mirror sketch and record `SCK_LOW->0 SCK_HIGH->1 PASS` or `FAIL`. |
| 2026-07-25 | Measured the HOST connector SCK pin high while STM32 PB3 remained low during the mirror test. | This confirms the SCK signal is present at the connector but is not reaching PB3; the remaining fault is the connector-to-MCU PCB net, pin mapping, soldering, or an incorrect PB3 test point—not Teensy GPIO or the mirror code. | With power removed, perform continuity from the actual HOST_SCK connector contact to the STM32 PB3 package pin and inspect the trace/vias/solder joints. |
| 2026-07-25 | User measured continuity between the HOST_SCK connector and PB3, but observed connector high and PB3 low when powered. | Continuity confirms a DC path only; the powered mismatch indicates a pull-down/clamp, another device driving PB3 low, a high-impedance/weak source, or a wrong test point/reference ground. | With power off and Teensy disconnected, measure resistance from PB3 to GND and from HOST_SCK to GND; then measure both nodes with a scope or current-limited supply while driving SCK. |
| 2026-07-25 | Removed the diagnostic GPIO pull-down from PB3 (`GPIO_PULLDOWN` → `GPIO_NOPULL`) to ensure the SCK mirror test does not load the HOST_SCK line. | Source modified; rebuild and flash pending. | Rebuild/flash the no-pull SCK test and repeat the connector/PB3 voltage comparison. |
| 2026-07-25 | Rebuilt the no-pull SCK diagnostic. | Build passed (RAM 1784 B, Flash 7496 B); pyOCD then waited for a debug probe, so flashing did not complete in this attempt. | Reconnect DAPLink and flash the no-pull image before repeating voltage measurements. |
| 2026-07-25 | Hardware observation: A-B and B-C appeared short, while A-C measured relatively high resistance. | No code was changed. In an ideal unpowered wire network this is impossible, so the measurement may involve probe/contact error, powered circuitry, diode/ESD paths, or different non-common routes. | Power off and disconnect Teensy, discharge capacitors, then repeat A-B, B-C, and A-C resistance measurements in both probe polarities while gently moving the connector. |
| 2026-07-25 | Resoldered the HOST connector and restored PB3/HOST_SCK low/high switching. Recorded three DAPLink/Teensy power configurations and reviewed the v2.2 power path plus both mirror-test programs. | PB3 connector fault resolved. The schematic confirms DAPLink 3V3 and Teensy 3.3 V are hard-paralleled through SWD pin 1 and HOST pin 10/R1 when both are connected. All observed 2.1-3.0 V or signal-dependent supply conditions are invalid; the case-2 PASS is not accepted. | Isolate all GPIO, power the board from one current-limited 3.3 V source, record rail current and voltage at source/J1/STM32 VDD, then reconnect GND, SCK, and MISO one at a time. |
| 2026-07-25 | Audited the actual SCK-test working copy, latest ELF, disassembly, flash history, startup sequence, and PG10-NRST configuration against the new power measurements. | Latest ELF contains the correct no-pull PB3-to-PB4 loop, but its successful flash is not recorded; the MCU most likely still contains the earlier pull-down mirror variant. Either version should mirror once reached. Power collapse or an init failure can prevent PB4 setup and leave it high-impedance near 0.3 V. The `.ioc` also incorrectly marks PG10-NRST as a GPIO output. | Establish stable single-source 3.3 V power, repair pyOCD/read back or reflash the identified ELF, use a minimal no-SPI mirror image with a heartbeat, and restore PG10-NRST to reset mode. |
| 2026-07-25 | Confirmed that the previously reported PA3/PA4 measurements were actually taken on PB3/HOST_SCK and PB4/HOST_MISO. | The voltage observations directly describe the HOST SCK/MISO path; no pin-mapping ambiguity remains. | Perform the power-only isolation test before any further SPI or firmware validation. |
| 2026-07-25 | Recorded that no independent current-limited supply is currently available and defined the ideal behaviour for the Teensy-powered PB3-to-PB4 mirror test. | Ideal result is a stable 3.2-3.3 V rail and repeated `SCK_LOW->0 SCK_HIGH->1 PASS`; the compiled no-pull and likely flashed pull-down variants should behave identically when the MCU reaches the mirror loop. | Run only with DAPLink 3V3 disconnected; stop immediately if either 3.3 V rail falls below approximately 3.1 V or follows the GPIO waveform. |
| 2026-07-25 | Analysed the Teensy-powered voltage drop and added a Teensy-only isolation sequence. | DAPLink source contention/back-power is confirmed for the DAPLink-connected cases. With DAPLink removed, the remaining leading causes are mainboard overload/short, a high-resistance J1 pin-10 power path, or GPIO parasitic powering through an inadequately connected 3.3 V input. | Measure the no-load Teensy rail, then connect only mainboard GND and J1 pin 10 before adding PB3 and PB4 one at a time. |
| 2026-07-25 | Diagnosed PB4 stuck near 0.3 V by reading the live STM32 core and GPIO state: the MCU was running from the ROM system bootloader (`PC/VTOR = 0x1FFFxxxx`) rather than user Flash. Identified PB8/HOST_IRQ/BOOT0 as the boot-selection path and issued a normal reset while PB8 was low. | Execution returned to Flash (`PC = 0x080004F0`); PB3 was configured as input with pull-down, PB4 was configured as GPIO output, and the target was resumed. No erase or reprogramming was performed. | Repeat the Teensy mirror test. Add a 10 kOhm PB8/BOOT0 pull-down or deliberately configure option bytes for permanent main-Flash boot selection. |
| 2026-07-25 | User confirmed the HOST_SCK/CLK problem was resolved. Restored the complete STM32/Teensy `0x55` SPI pattern test, rebuilt through the short-path working copy, flashed STM32 at 100 kHz under reset, compiled and uploaded the Teensy receiver, and checked the live serial stream. | PASS: STM32 ran from user Flash at `PC = 0x08000794`; PB3/PB4/PB5 were SPI3 AF6; COM9 produced eight consecutive `RX: 0x55 bits=01010101 PASS` transactions. | Preserve this validated pattern-test baseline. Next isolate SPI1/MAX11633 and then restore the 16 x 16 FSR frame path; add a permanent PB8/BOOT0 pull-down before relying on cold starts. |
| 2026-07-25 | Returned from the isolated `0x55` test to the complete MUX1/MAX11633 16 x 16 acquisition path. Corrected the MUX settle order, added STM32 stage/counter diagnostics, reset and configured the MAX11633 explicitly, hardened the Teensy IRQ/SPI timing, and strengthened the Python frame parser. | PASS after a normal STM32 reset: COM9 delivered 10 consecutive valid 515-byte frames with sequence numbers 417--426 and no gap. All 256 samples per frame were within the 12-bit range; observed limits were 0--3681. | Perform a controlled single-cell press test, confirm the expected matrix row/column changes, then add frame CRC and raise SPI speed only after a longer stability run. |
| 2026-07-25 | Replaced SPI3 `HAL_SPI_TransmitReceive()` with transmit-only `HAL_SPI_Transmit()` because the host link is one-way and the unused receive side was timing out after a valid frame had already reached Teensy. Added aggregate statistics to the PC receiver. | FINAL PASS: 100/100 frames, sequence 281--380, zero gaps, sample range 0--3889, 25.07 frame/s. Live SWD counters later showed 1112 scans, 1112 completed frames, and zero errors. | Run the controlled single-cell press test and verify physical-to-matrix coordinates. |
| 2026-07-25 | Removed the incorrect CubeMX/application GPIO-output configuration for the dedicated PG10-NRST pin, rebuilt, and reflashed the final image. | PASS: 100/100 frames at 25.20 frame/s. After deliberately disrupting SPI with an SWD halt, a normal reset restored 20/20 frames at 25.88 frame/s. | Leave the target running without SWD halts and perform the controlled FSR press/mapping test. |
| 2026-07-25 | Added a live Python FSR heatmap using the existing Teensy 515-byte stream and the hexagonal PCB geometry. | Geometry test passed for all 256 cells; the background reader decoded live COM9 frame 612 as a 16 x 16 matrix with range 0--3585 and no sequence gap. | Run the heatmap during a single-cell press and use hover labels to confirm row/column orientation. |
| 2026-07-25 | Reviewed the first live heatmap showing fixed black vertical bands. | With the default unflipped orientation, the bands correspond approximately to raw columns C2, C3, and C10--C13. The plot is rendering valid near-zero ADC codes; it is not omitting polygons. | Press one cell in each dark column, then check the associated FPC contacts and AIN continuity if the code remains near zero. |
| 2026-07-25 | Performed a code-versus-hardware isolation test for the dark heatmap columns. | Software test filled and rendered all 256 polygons uniformly. A 50-frame live capture had zero sequence gaps but persistent channel-dependent amplitudes: only C1, C4, C6, C7, C9, and C14 produced large values. This rules out the heatmap and transport parser. | Inspect the low-column FPC contacts/traces and press one known cell per column while watching the raw ADC code. |

## 2026-07-25 — Return to complete 16 x 16 FSR acquisition

### Firmware configuration restored

The production acquisition path is active again:

```text
FSR layer 1
  -> MUX1 (PA0..PA3 address, PA4 active-low enable)
  -> MAX11633 ADC1 (PB0 CS, PB10 active-low EOC, SPI1)
  -> STM32 16 x 16 frame
  -> SPI3 slave (PA15 NSS, PB3 SCK, PB4 MISO, PB5 MOSI)
  -> Teensy 4.1 SPI master
  -> Teensy USB CDC
  -> read_teensy_fsr.py on the PC
```

The v2.2 netlist confirms the analog topology: MUX1 selects one row and applies
the 3.3 V excitation through 1 kOhm, while ADC1 simultaneously measures the 16
column nets. Each column has a 10 kOhm pull-down. ADC channel order is reversed
at the column FPC: AIN0 maps to FPC pin 16 and AIN15 maps to FPC pin 1.

The host frame remains 515 bytes:

```text
byte 0       : 0xA5 frame marker
bytes 1..2   : uint16 little-endian sequence number
bytes 3..514 : 256 uint16 little-endian samples in row-major order
```

### Changes made before the test

STM32:

- Added explicit MAX11633 reset (`0x10`) and setup (`0x68`) commands.
- Used scan command `0xF8` for AIN0 through AIN15.
- Corrected each MUX row operation to address -> enable -> 1 ms settle ->
  convert/read -> disable.
- Added volatile scan stage, last-error stage, active-row, completed-scan,
  completed-frame, and error counters for SWD inspection.
- Kept every ADC result masked to the valid 12-bit range.

Teensy:

- Restored `INPUT_PULLDOWN` on GPIO 2 because STM32 PB8 is both HOST_IRQ and
  BOOT0.
- Used SPI Mode 0, MSB first, and 100 kHz for validated bring-up.
- Added 1 ms guard time before and after asserting chip select.
- Added a timeout while waiting for HOST_IRQ to be released.
- Removed the per-frame USB `Serial.flush()` stall.

PC receiver:

- Rejects false frame alignment if any decoded ADC sample exceeds `0x0FFF`.
- Supports finite tests with `--frames` and `--max-time`.
- Reports sequence gaps and can print the complete 16 x 16 matrix.

The MAX11633 data sheet was rechecked after a review suggested using `0xF9`.
For this ADC, conversion-register bit 0 is explicitly "don't care"; it is not a
single-ended/differential selector. Both `0xF8` and `0xF9` therefore select
CHSEL=15 and SCAN=00, but `0xF8` is retained. Scan mode 00 returns one result
for every channel from AIN0 through AIN15.

### Initial runtime failure and diagnosis

The first PC capture returned zero valid frames. Live SWD variables showed that
the ADC/MUX work was completing, but every host transfer was timing out:

```text
fsr_completed_scans  = 333
fsr_completed_frames = 0
fsr_last_error_stage = HOST_TRANSFER
fsr_error_count       = 332
```

Teensy USB diagnostics alternated between `WAIT_IRQ` and `BAD_MAGIC 0x00`.
This isolated the fault to the 515-byte STM32-to-Teensy transaction rather than
the MAX11633 scan.

A normal STM32 reset was then issued while Teensy GPIO 2 held PB8/BOOT0 low.
This returned the MCU to a clean user-Flash/SPI state. No option bytes were
changed.

### Successful end-to-end result

A bounded raw COM9 read contained exactly one diagnostic line followed by one
complete frame:

```text
23 20 ... 0d 0a  a5 94 01 ...
                     ^ sequence 0x0194 = 404
```

The PC receiver then captured 10 consecutive frames:

```text
sequence: 417, 418, 419, 420, 421, 422, 423, 424, 425, 426
valid frames: 10/10
sequence gaps: 0
sample range across the run: 0..3681
invalid samples above 4095: 0
```

A subsequent complete matrix frame was also decoded:

```text
sequence: 484
minimum: 0
maximum: 3665
first sample: 6
last sample: 2
```

### Host-transfer throughput correction

The first apparently successful version was still not acceptable for continuous
operation. A 30-second aggregate run produced only 29 frames:

```text
sequence: 456..484
gaps: 0
sample range: 0..3622
rate: 0.96 frame/s
```

This rate matched the 1000 ms STM32 host timeout. Teensy was receiving all 515
outbound bytes, but `HAL_SPI_TransmitReceive()` still waited for the unused
full-duplex MOSI receive side and frequently timed out.

An interrupt-armed `HAL_SPI_TransmitReceive_IT()` experiment was built and
flashed to test ready-before-IRQ ordering. It was rejected after runtime
validation:

```text
Teensy: BAD_MAGIC 0x00 / BAD_MAGIC 0x01
STM32 scans: 76
STM32 completed frames: 0
STM32 errors: 75
```

The SPI3 TX FIFO did contain a correct `0xA5` frame, but the HAL interrupt/FIFO
path did not sustain this transfer reliably in the current configuration. All
SPI3 interrupt changes were removed.

The final implementation uses blocking transmit-only mode:

```c
HAL_SPI_Transmit(&hspi3, host_tx_frame, HOST_FRAME_BYTES,
                 HOST_SPI_TIMEOUT);
```

This exactly matches the active data direction: STM32 MISO to Teensy MISO.
Teensy MOSI remains physically connected but its dummy bytes are not used by
the STM32 application. HAL clears the receive overrun generated by the unused
two-line receive path when the transmit operation closes.

Final build and flash:

```text
RAM used: 2832 bytes
Flash used: 13288 bytes
pyOCD: 100 kHz, under-reset
erased: 14336 bytes / 7 sectors
programmed: 13312 bytes / 13 pages
flash errors: none
```

Final aggregate PC test:

```text
Captured 100 valid frame(s).
Summary: sequence=281..380 gaps=0 samples=0..3889
elapsed=3.989s rate=25.07 frame/s
```

Final live STM32 counters:

```text
host_sequence        = 1112
fsr_completed_scans  = 1112
fsr_completed_frames = 1112
fsr_error_count      = 0
```

The PC receiver now supports `--summary-only` so long tests produce one
aggregate result instead of hundreds of per-frame lines.

### Final NRST cleanup and runtime state

PG10-NRST was incorrectly listed in the `.ioc` file and generated application
code as a push-pull GPIO output. The pin is now left as the dedicated hardware
reset input:

- removed `PG10-NRST.Signal=GPIO_Output` and its label/lock entries;
- removed PG10 from the configured GPIO pin list;
- removed the generated `NRST_Pin` macros;
- removed the GPIOG clock, output-level write, and GPIO initialisation from
  `MX_GPIO_Init()`.

The final build used 2832 bytes of RAM and 13220 bytes of Flash. pyOCD at
100 kHz under reset erased 10240 bytes, programmed 9216 bytes, and found 4096
bytes already identical.

Final-image validation:

```text
Captured 100 valid frame(s).
Summary: sequence=318..417 gaps=0 samples=0..3680
elapsed=3.969s rate=25.20 frame/s
```

An SWD `halt` was then intentionally observed to interrupt an active
`HOST_TRANSFER`. The following stream fell to approximately 0.96 frame/s even
though its data remained valid. This is a debugger-induced peripheral phase
loss, not an ADC failure. A normal reset restored the final running state:

```text
Captured 20 valid frame(s).
Summary: sequence=293..312 gaps=0 samples=0..3701
elapsed=0.773s rate=25.88 frame/s
```

Do not halt or single-step the MCU while measuring continuous SPI throughput.
If a debug halt occurs during HOST transfer, issue a normal reset before
resuming acquisition.

Final artifact SHA-256 values:

```text
main.c
A82832B917740FE254C729898F18E723AB263C53F165A7D0EC6EA25806517D6E

ESKIN_STM32.ioc
9C9DE09D1B794CF71159C51EE2D88884C8F0B75A3D04381FED5C2AC101342557

ESKIN_STM32.elf
9E42703E5CD0C8DD6737BA12D1E31659AB036945EA31E831519116A3AD09CFAF

ESKIN_SPI_USB_BRIDGE.ino
741D8C15D2D99E71AFCB4F25B90622437422C789A6FE168F2C69880EDDA69D3F

read_teensy_fsr.py
E1FC12BFF8959A64167A3ADA1EAF533073FE93159C5F262F4B59312C7B3C7696
```

Reproduction command:

```powershell
python -u -B .\teensy41\ESKIN_SPI_USB_BRIDGE\read_teensy_fsr.py `
  --port COM9 `
  --baud 2000000 `
  --timeout 0.5 `
  --frames 100 `
  --max-time 15 `
  --summary-only
```

Current conclusion:

- The MUX1/MAX11633 scan is producing plausible 12-bit data.
- STM32 SPI3 now sends complete 515-byte frames.
- Teensy receives the frames and forwards them over USB.
- The PC parser resynchronises and decodes the complete 16 x 16 matrix.
- The validated continuous rate is approximately 25 frame/s at 100 kHz SPI,
  with matching STM32 scan/frame counters and zero recorded transfer errors.
- The next physical validation is a controlled press on one known FSR cell and
  verification that the corresponding matrix coordinate changes
  monotonically and repeatably.
- Keep DAPLink 3.3 V disconnected while the board is powered from Teensy, and
  add a permanent approximately 10 kOhm PB8/BOOT0 pull-down for reliable cold
  boot.

## 2026-07-25 — Live hexagonal FSR heatmap

Created:

```text
teensy41/ESKIN_SPI_USB_BRIDGE/live_fsr_heatmap.py
```

The display reuses the 16 x 16 binary parser and maps its 256 cells into the
same normalized hexagonal outline used by the PCB/frontend geometry. The colour
scale represents the raw 12-bit ADC code from 0 to 4095.

Features:

- dedicated background serial thread so the plot remains responsive;
- latest-frame queue so drawing cannot delay Teensy acquisition;
- frame number, measured frame rate, sequence-gap count, range, and peak cell;
- pointer hover showing raw row, column, ADC code, AIN channel, and column FPC
  pin;
- fixed 0--4095 scale by default, with optional per-frame `--autoscale`;
- optional `--flip-rows` and `--flip-columns` for matching the installed PCB
  orientation without changing raw matrix labels;
- `--demo` mode for checking the geometry without hardware.

Dependencies:

```powershell
python -m pip install pyserial numpy matplotlib
```

Live command:

```powershell
cd .\teensy41\ESKIN_SPI_USB_BRIDGE
python -u .\live_fsr_heatmap.py --port COM9
```

Useful alternatives:

```powershell
python -u .\live_fsr_heatmap.py --port COM9 --autoscale
python -u .\live_fsr_heatmap.py --port COM9 --flip-columns
python -u .\live_fsr_heatmap.py --demo
```

Close Arduino Serial Monitor before starting the heatmap because only one
Windows process can own COM9 at a time.

Validation:

```text
HEATMAP_GEOMETRY_TEST_PASS
polygons: 256
outline vertices: 6

LIVE_FRAME_TEST_PASS
sequence: 612
shape: 16 x 16
sample range: 0..3585
sequence gaps: 0
```

### Fixed dark-column observation

The first hardware heatmap showed two fixed dark bands spanning nearly every
row. Because all polygons and grid edges remain visible, these are valid cells
whose ADC codes are close to zero, not missing graphics.

With the default orientation (no row/column flip), the bands correspond
approximately to:

```text
raw matrix column   ADC channel   column FPC pin
C2                  AIN2          14
C3                  AIN3          13
C10                 AIN10          6
C11                 AIN11          5
C12                 AIN12          4
C13                 AIN13          3
```

Each column net has a 10 kOhm pull-down. Therefore a disconnected FPC contact,
an open FSR column trace, or a column that is not being driven through a pressed
FSR cell naturally reads close to zero and appears black on the fixed 0--4095
colour scale.

Diagnostic interpretation:

- If a dark cell rises when it is pressed, the column is electrically active
  and the black baseline is normal.
- If every row in the column remains near zero while pressing, inspect/reseat
  the listed FPC contacts and check power-off continuity to the matching ADC
  AIN input.
- If the ADC pin voltage rises but its reported code remains zero, investigate
  the ADC/package connection; if the voltage also remains zero, the fault is
  before the ADC.

The screenshot also reported approximately 1 frame/s. This is independent of
the dark-column colour and indicates the previously observed SPI phase loss
after an SWD halt. A normal STM32 reset, without subsequently halting the
target, should restore approximately 25 frame/s.

### Code-versus-hardware isolation result

A uniform synthetic 16 x 16 matrix was passed through the same heatmap geometry
and orientation functions:

```text
SYNTHETIC_ALL_COLUMNS_RENDER_TEST_PASS
cells: 256
input/output value: 2048
```

Therefore the geometry and colour mapper can render every row and column.

After a normal STM32 reset, 50 real frames were captured directly from COM9:

```text
frames: 50
sequence: 422..471
sequence gaps: 0
shape: 50 x 16 x 16
```

Per-column statistics:

```text
column  FPC pin   minimum  maximum    mean
C00       16          0       11       0.8
C01       15          0     3715    1245.3
C02       14          0       60      20.6
C03       13          0        2       1.0
C04       12          0     2321     683.5
C05       11          0       79      23.5
C06       10          0     3804    1294.7
C07        9          0     3849    1316.0
C08        8          0      130      44.5
C09        7          0     2814     951.9
C10        6          0       83      28.6
C11        5          0        5       2.0
C12        4          0        5       1.1
C13        3          0        4       1.0
C14        2          0     1221     401.3
C15        1          0       47      16.1
```

Conclusion:

- the 515-byte transport is aligned and complete;
- the PC parser receives all 16 columns;
- the heatmap creates and colours all 256 polygons;
- the STM32 column loop contains no per-column branch or exclusion;
- the persistent low values originate at or before the ADC inputs.

Only C1, C4, C6, C7, C9, and C14 currently show substantial column voltage.
The leading causes are incomplete FPC contact, open FSR column traces, or a
genuinely high-resistance/unpressed sensor path. If a known press does not raise
one of the low columns, check power-off continuity from its listed FPC pin to
the corresponding MAX11633 AIN pin.

## 2026-07-25 - Per-cell zero/full heatmap calibration

`teensy41/ESKIN_SPI_USB_BRIDGE/live_fsr_heatmap.py` now provides two one-click
calibration controls:

- **Calibrate zero**: remove all pressure and keep the array unloaded while 40
  unique live frames are collected.
- **Calibrate full**: apply the same known full-load condition to every cell
  and hold it while 40 unique live frames are collected.

The median of the 40 samples is stored separately for every one of the 256 FSR
cells. Median acquisition rejects isolated serial/ADC spikes better than a
single-frame reference.

After both maps exist, cell `(r, c)` is displayed as:

```text
pressure[r,c] =
    clip((raw[r,c] - zero[r,c]) / (full[r,c] - zero[r,c]), 0, 1)
```

The calculation deliberately retains the sign of `full - zero`, so it supports
both voltage-divider polarities: ADC codes may either rise or fall with
pressure. The colour bar then changes from raw 12-bit ADC codes to calibrated
pressure from 0% to 100%.

If `abs(full - zero)` is less than 50 ADC codes for a cell, that cell is marked
invalid and shown in grey instead of amplifying noise into a false pressure
reading. The threshold can be changed with:

```powershell
python -u live_fsr_heatmap.py --port COM9 --min-calibration-span 50
```

Hovering over a calibrated cell reports its normalized percentage, current raw
ADC code, zero reference, full reference, ADC input, and FPC pin. The title
reports the peak valid cell and the number of invalid cells.

Calibration is saved automatically beside the script as:

```text
teensy41/ESKIN_SPI_USB_BRIDGE/fsr_calibration.json
```

It is loaded automatically on the next run. A different file or a different
sample count can be selected with:

```powershell
python -u live_fsr_heatmap.py --port COM9 `
  --calibration-file experiment_01.json `
  --calibration-frames 60
```

Operational sequence:

1. Start the heatmap and wait for live frames.
2. Remove every load from the FSR array and click **Calibrate zero**.
3. Do not touch the array until the status reaches 40/40 and confirms the save.
4. Apply a repeatable, spatially uniform full-load reference to all 256 cells.
5. Click **Calibrate full** and hold the load until 40/40 is complete.
6. The heatmap immediately switches to each cell's own 0%--100% range.

The full calibration must be spatially uniform. Pressing the array by hand one
cell at a time while the 40 frames are collected does not create a valid
simultaneous full-load map.

Verification completed:

```text
Python compile check: PASS
Increasing-ADC normalization: zero 100, full 1100, raw 600 -> 0.500 PASS
Decreasing-ADC normalization: zero 1100, full 100, raw 600 -> 0.500 PASS
Invalid-span handling: absolute span 20 at threshold 50 -> NaN/grey PASS
Calibration JSON save/load round trip: PASS
Headless GUI startup: PASS
Visual layout check: PASS
```

No STM32 or Teensy firmware was changed or reflashed for this feature. It is a
PC-side visualization and calibration change only.

## 2026-07-25 - Heatmap refresh synchronized to acquisition

The live heatmap previously checked its frame queue every 40 ms, nominally
25 Hz. The measured hardware rate is approximately 25.1--25.9 frame/s, so the
two independent schedules could slowly change phase and occasionally cause the
one-element display queue to replace a frame before it was drawn.

The default `--refresh-ms` queue-check interval is now 10 ms. This is not an
independent 100 Hz heatmap animation: `update()` returns without drawing when
there is no new hardware frame. Consequently, the visible heatmap changes once
per received STM32 frame and follows the actual hardware acquisition rate,
normally approximately 25 Hz, with no more than approximately 10 ms of GUI
polling latency.

The serial reader remains in its background thread so plotting cannot delay
acquisition. The newest-frame queue is retained to prevent a slow GUI window
from building an increasingly stale backlog.

Verification:

```text
Python compile check: PASS
Default GUI queue check: 10 ms
Redraw condition: new hardware frame only
Expected visible update rate: measured acquisition rate, approximately 25 Hz
STM32 firmware changed: no
Teensy firmware changed: no
```

## 2026-07-25 - One-frame-per-second and false unloaded pressure

The calibrated heatmap was observed at 1.0 frame/s with zero sequence gaps.
This is not a plotting refresh limitation: the GUI now redraws whenever a new
frame arrives, but COM9 was delivering only approximately one frame each
second. The combination of a valid sequential frame and a period close to one
second matches the previously diagnosed STM32 `HOST_SPI_TIMEOUT` recovery path.
A valid 515-byte frame can reach Teensy before the STM32 polling call reports
the failed/incomplete end-of-transaction state and spends its 1000 ms timeout.

A low-frequency normal SWD reset was issued without reflashing:

```powershell
pyocd reset -t stm32g474cetx --frequency 10000 --connect under-reset
```

The command completed successfully. Because the running heatmap owns COM9, an
independent receiver could not open the port to verify the post-reset rate.
The heatmap title is the authoritative immediate check: it should return to
approximately 25 frame/s after the normal reset. If it remains at 1 frame/s,
power-cycle the STM32/Teensy combination while keeping PB8/BOOT0 low and do not
halt the MCU with an SWD debug session.

The saved calibration was also inspected:

```text
zero range:                 0..3492 ADC
full range:                 1..2586 ADC
absolute span median:       1862.25 ADC
span below 50 ADC:          65 cells
span from 50 to 100 ADC:    17 cells
```

For the cell shown in the screenshot:

```text
R0, C7
raw:    2815
zero:   2878.5
full:   2580.5
span:   -298
result: (2815 - 2878.5) / (2580.5 - 2878.5) = 21.3%
```

The displayed pressure is therefore the correct result of the saved electrical
references, but it is not proof of mechanical load. The unloaded raw code moved
63.5 counts toward its full-load reference after zero calibration. Supply or
reference drift, FSR relaxation, crosstalk, unstable contacts, and a long
40-second calibration performed at 1 frame/s can all cause this effect.

The heatmap now labels rates below 10 frame/s as `SLOW LINK: reset STM32`.
Zero/full calibration is blocked below 10 frame/s and an in-progress
calibration is aborted if the link becomes slow. This prevents another 40-frame
reference from being collected over approximately 40 seconds in the timeout
state.

Required recovery order:

1. Confirm approximately 25 frame/s in the title.
2. Leave the array unloaded and redo **Calibrate zero**; it should complete in
   approximately 1.6 seconds.
3. Apply the repeatable uniform reference load and redo **Calibrate full**.
4. Remove the load and inspect raw/zero/full on any remaining non-zero cell.
   A repeatable raw shift then indicates electrical/mechanical baseline drift,
   not a heatmap calculation error.

## 2026-07-26 - Heatmap remained at "Waiting for FSR frames"

The heatmap opened COM9 and loaded `fsr_calibration.json`, but its title remained
`Waiting for FSR frames from COM9...`. This state means the serial port opened
successfully but the parser had not received one complete valid binary frame
beginning with `0xA5`. It is not a valid all-zero pressure frame.

The Teensy bridge prints `# WAIT_IRQ level=0` when STM32 PB8/HOST_IRQ remains
low. The binary PC parser discards this ASCII diagnostic while searching for the
`0xA5` marker, so the GUI can remain on the waiting screen without showing the
underlying IRQ message.

Recovery and inspection performed:

```powershell
pyocd reset -t stm32g474cetx --frequency 10000 --connect under-reset
```

The reset completed. A low-frequency SWD read then showed GPIOB IDR and ODR as
`0xFFFFFEBF`; bit 8 was low at the instant of the read, so HOST_IRQ/PB8 was not
asserted. Because an under-reset debug connection can leave the core halted
during inspection, the target was explicitly resumed:

```powershell
pyocd commander -t stm32g474cetx --frequency 10000 `
  --connect under-reset -c "continue" -c "exit"
```

Result:

```text
Successfully resumed device
```

The running heatmap owns COM9, so a second process cannot independently verify
the first recovered frame. The existing window should begin updating after the
resume. If it remains waiting, the next isolation check is Teensy GPIO2:

- GPIO2 remains low: STM32 is not reaching `HOST_SendFrame()`; inspect the ADC
  scan/EOC path and confirm the MCU booted from Flash.
- GPIO2 goes high: STM32 has a frame but Teensy is not completing the SPI
  transaction; inspect PA15/NSS, PB3/SCK, PB4/MISO, and common ground.

## 2026-07-26 - Non-zero cells immediately after full calibration

The link recovered to 25.0 frame/s with zero sequence gaps. The remaining
coloured cells are therefore not caused by the previous one-frame-per-second
communication fault.

Calibration defines two static electrical reference values; it does not force
future unloaded readings to zero. The screenshot gave the following example:

```text
R15, C15
raw ADC after releasing load: 843
stored zero reference:          1
displayed full reference:    2464
normalized result: (843 - 1) / (2464 - 1) = 34.2%
```

The heatmap calculation is consistent with those inputs. The sensor/electrical
reading had not returned to the previously captured zero reference. Likely
causes are FSR mechanical hysteresis/creep after the full-load press, residual
preload from the stack or fixture, supply/reference movement, and matrix
crosstalk or unstable contacts. A static two-point calibration cannot by itself
distinguish these effects from a real applied pressure.

The newly saved JSON map was inspected:

```text
valid cells (absolute span >= 50 ADC): 187
invalid cells:                          69
zero median:                             1 ADC
full median:                          2529 ADC
full > zero valid cells:               182
full < zero valid cells:                 5
```

Four entire columns account for 64 of the 69 invalid cells:

```text
C2, C3, C12, C13: 16 invalid rows each
```

This matches the previously identified fixed low-response column groups and is
not a heatmap polygon error. The remaining five invalid cells are isolated
low-span channels.

Recommended calibration sequence for the FSR material:

1. Apply and release the reference load several times to precondition the FSR.
2. Release all pressure and allow the raw ADC readings to settle.
3. Click **Calibrate zero** and keep the array untouched for 40 frames.
4. Apply a spatially uniform full-load reference and click **Calibrate full**.
5. Release the load and wait for mechanical recovery.
6. If the unloaded raw codes settle at new offsets, click **Calibrate zero**
   once more. This updates zero while retaining the saved full map.

Large residual readings such as 843 ADC counts should not be hidden with a
software deadband, because doing so would also remove genuine light pressure.
They should first be separated into mechanical preload/hysteresis versus
electrical baseline movement by observing the raw ADC value over time.

## 2026-07-26 - Permanent correction for recurring 1 Hz SPI transfer

The system again fell from 25 frame/s to 1.0 frame/s while still reporting zero
sequence gaps. A live SWD inspection of the original build gave:

```text
fsr_scan_status:       HAL_OK
fsr_stage:             HOST_TRANSFER
fsr_last_error_stage:  HOST_TRANSFER
fsr_completed_scans:   93
fsr_completed_frames:  25
fsr_error_count:       69
host_sequence:         96
```

This proves that ADC/MUX scanning continued, Teensy received sequential valid
frames, but most STM32 host-transfer calls failed after the data had already
been clocked out.

### Root cause

SPI3 is configured as a two-line, full-duplex slave. Every STM32 MISO byte
therefore also clocks a dummy MOSI byte into the STM32 RX FIFO. The current
firmware used:

```c
HAL_SPI_Transmit(&hspi3, host_tx_frame, HOST_FRAME_BYTES,
                 HOST_SPI_TIMEOUT);
```

Inspection of `stm32g4xx_hal_spi.c` showed that `HAL_SPI_Transmit()` calls
`SPI_EndRxTxTransaction()`, which waits for all three conditions:

```text
TX FIFO empty
BSY clear
RX FIFO empty
```

The receive FIFO was not being drained during transmit-only operation. The HAL
could therefore wait for `FRLVL_EMPTY` until the 1000 ms
`HOST_SPI_TIMEOUT`, even though Teensy had already received all 515 valid bytes.
This directly explains the approximately 1 Hz rate with zero PC sequence gaps.

The earlier workflow conclusion that transmit-only mode safely cleared the
overrun was incomplete: HAL clears OVR only after its end-of-transaction checks,
so it can spend the entire timeout waiting for the non-empty receive FIFO
first.

### Firmware correction

STM32 now provides a 515-byte dummy receive buffer and uses full-duplex polling:

```c
static uint8_t host_rx_dummy[HOST_FRAME_BYTES];

HAL_SPI_TransmitReceive(&hspi3, host_tx_frame, host_rx_dummy,
                        HOST_FRAME_BYTES, HOST_SPI_TIMEOUT);
```

This consumes every dummy MOSI byte while the real frame is transmitted on
MISO. Diagnostic variables were retained for future SWD inspection:

```text
host_spi_error_code
host_spi_sr
host_spi_tx_remaining
host_spi_rx_remaining
```

Teensy now holds NSS low for an additional 100 microseconds after its last byte:

```cpp
delayMicroseconds(100);
digitalWrite(STM32_CS_PIN, HIGH);
```

This gives the STM32 time to consume the final receive byte and complete FIFO
checks before the slave transaction closes.

### Build and programming

STM32 build:

```text
RAM:    3360 bytes / 128 KiB (2.56%)
Flash: 13284 bytes / 512 KiB (2.53%)
Build: PASS
```

The STM32 ELF was programmed through DAPLink at 100 kHz under reset. Teensy
4.1 was compiled with Arduino CLI and `teensy:avr:teensy41`; the upload command
completed on COM9.

An initial five-frame measurement before the final STM32 reflash/reset still
showed 1.09 frame/s, consistent with the peripheral remaining in the previous
failed state. After the final corrected STM32 image was programmed, live SWD
diagnostics showed:

```text
fsr_completed_scans:   1109
fsr_completed_frames:  1109
fsr_error_count:       0
host_spi_error_code:   0
host_spi_tx_remaining: 0
host_spi_rx_remaining: 0
```

Final independent COM9 validation:

```text
valid frames: 100/100
sequence: 1722..1821
sequence gaps: 0
elapsed: 3.972 s
rate: 25.17 frame/s
ADC range: 0..3925
```

Result: the recurring 1 Hz mode was reproduced, isolated to SPI3 host transfer,
corrected in both endpoints, rebuilt, flashed, and verified at the expected
continuous frame rate.

## 2026-07-26 - No frames after restart: STM32 booted into system ROM

After the SPI throughput correction, COM9 was still present as the Teensy USB
serial device (`VID:PID 16C0:0483`) and no old heatmap or receiver process held
the port. A direct binary capture nevertheless received no valid frame.

A raw three-second serial read showed:

```text
# WAIT_IRQ level=0
# WAIT_IRQ level=0
# WAIT_IRQ level=0
```

This proved that Teensy was running but STM32 PB8/HOST_IRQ remained low. Live
SWD inspection then identified the exact boot state:

```text
PC:   0x1FFF3B20
VTOR: 0x1FFF0000
```

Addresses in `0x1FFFxxxx` belong to the STM32 system-memory ROM bootloader, not
the application stored in Flash at `0x08000000`. RAM locations corresponding to
the application diagnostic variables also contained unrelated ROM data. The
ADC scan and `HOST_SendFrame()` application were therefore not running.

PB8 is both `HOST_IRQ` and the STM32 BOOT0 sampling path. The Teensy configures
GPIO2 as `INPUT_PULLDOWN`, but this is only a weak internal pull and is absent
or undefined while Teensy itself resets or enters its loader. If STM32 samples
PB8 high during that interval, it starts the ROM bootloader and never produces
an FSR frame.

Recovery performed:

```powershell
pyocd reset -t stm32g474cetx --frequency 10000 `
  --connect under-reset --method hw
```

With the Teensy application already running and holding PB8 low, the hardware
reset returned execution to Flash:

```text
PC: 0x0800164C
target resumed successfully
```

Final independent COM9 validation:

```text
valid frames: 100/100
sequence: 986..1085
sequence gaps: 0
elapsed: 3.986 s
rate: 25.09 frame/s
```

Permanent cold-boot correction remains recommended:

- add an external approximately 10 kOhm resistor from PB8/HOST_IRQ/BOOT0 to
  ground; it draws only approximately 0.33 mA while HOST_IRQ is high, or
- deliberately configure the STM32 boot option bytes so the physical BOOT0 pin
  cannot select system memory.

The resistor is the transparent hardware fix. Option-byte programming is a
persistent target configuration change and must not be performed casually;
retain SWD recovery access and document the exact option values before using it.

## 2026-07-26 - Software-only BOOT0 solution assessment

The user cannot add the recommended external PB8/BOOT0 pull-down. The failure
recurred and was inspected again before changing any persistent configuration:

```text
PC:         0x1FFF3B5A
FLASH_OPTR: 0xFFEFF8AA
```

This was another system-ROM boot, not the corrected SPI link falling back to
1 frame/s. In the current option value:

```text
nSWBOOT0 (bit 26): 1 -> BOOT0 is taken from physical PB8
nBOOT0   (bit 27): 1 -> software BOOT0 value would select main Flash
nBOOT1   (bit 23): 1
```

The STM32G4 reference-manual boot table states that `nSWBOOT0 = 0` and
`nBOOT0 = 1` select main Flash independently of the PB8 pin. Therefore the
software-only permanent correction is to clear only `FLASH_OPTR.nSWBOOT0` while
preserving `nBOOT0`, protection, reset-mode, watchdog, bank, and all other
option fields.

No option byte was changed during this inspection because it is persistent
device configuration and requires explicit approval. STM32CubeProgrammer and
OpenOCD were not installed, so a future update would require either installing
STM32CubeProgrammer or using a carefully verified direct option-programming
sequence.

Temporary recovery was performed again:

```powershell
pyocd reset -t stm32g474cetx --frequency 10000 `
  --connect under-reset --method hw
```

Verification:

```text
PC after reset: 0x08000C7C
valid frames:   20/20
sequence:       723..742
rate:           25.36 frame/s
```

Until the option bit is deliberately changed, the required power-up order is:

1. allow Teensy firmware to start so GPIO2 applies its internal pull-down to
   PB8/HOST_IRQ/BOOT0;
2. then hardware-reset STM32 through NRST.

Any simultaneous cold start, Teensy upload, or Teensy reset can leave PB8
undefined when STM32 samples BOOT0 and can reproduce the ROM-boot condition.

## 2026-07-26 - Permanent software boot selection applied

The user explicitly approved the persistent option-byte correction because the
hardware cannot be modified.

Before programming:

```text
FLASH_OPTR: 0xFFEFF8AA
nSWBOOT0:   1 (physical PB8/BOOT0 pin selected)
nBOOT0:     1
PC during failure: 0x1FFF3B5A (system ROM)
```

A one-time self-check was added to the STM32 application using the official ST
HAL option-byte API. It changes only these two declared user fields:

```c
option_bytes.OptionType = OPTIONBYTE_USER;
option_bytes.USERType = OB_USER_nSWBOOT0 | OB_USER_nBOOT0;
option_bytes.USERConfig = OB_BOOT0_FROM_OB | OB_nBOOT0_SET;
```

This clears `FLASH_OPTR.nSWBOOT0` and explicitly preserves `nBOOT0 = 1`.
Protection, reset mode, watchdog, bank, RDP, BOR, and all unrelated option
fields are untouched. On later starts, the function reads the active option
bytes and immediately returns because the requested state is already present.

The project built successfully:

```text
RAM:    3392 bytes / 128 KiB (2.59%)
Flash: 16672 bytes / 512 KiB (3.18%)
```

The approved image was flashed with DAPLink at 100 kHz under reset. The option
reload reset completed and readback proved the exact one-bit change:

```text
before: FLASH_OPTR = 0xFFEFF8AA
after:  FLASH_OPTR = 0xFBEFF8AA
changed mask:       0x04000000 (nSWBOOT0 only)
PC after reload:    0x0800160C
```

The physical PB8 level can no longer select system ROM; the software nBOOT0
value selects main Flash on every reset.

Final independent transport validation:

```text
valid frames: 100/100
sequence: 952..1051
sequence gaps: 0
rate: 25.16 frame/s
```

The Heatmap display that still showed 1 Hz was an old pre-correction window and
its last measured title. The verified COM9 source was already delivering
25.16 frame/s. A fresh Heatmap process was started:

```text
PID: 35084
command: python -u live_fsr_heatmap.py --port COM9
```

Permanent software boot selection and the SPI RX-FIFO throughput correction
are now both active.

## 2026-07-26 - Full-load calibration removed from Heatmap

The user requested removal of the upper/full-pressure calibration. The Heatmap
now has only one calibration action:

```text
Calibrate zero
```

For each cell, 40 unloaded frames are still collected and their median becomes
that cell's zero reference. The upper endpoint is no longer measured or loaded
from the old `full` JSON field. Twelve-bit ADC full scale is used uniformly:

```text
100% = ADC 4095
```

The displayed value for cell `(r, c)` is now:

```text
pressure[r,c] =
    clip((raw[r,c] - zero[r,c]) / (4095 - zero[r,c]), 0, 1)
```

Consequences:

- every cell has the same electrical upper ceiling of 4095;
- each cell still has its own unloaded offset;
- old full-load calibration values are ignored;
- the **Calibrate full** button and capture path are removed;
- hovering reports raw ADC, per-cell zero, and `100% ADC 4095`.

The existing JSON format remains readable for backward compatibility. The next
zero calibration writes `full: null`, making it explicit that the old endpoint
is no longer used.

Verification:

```text
Python compile check: PASS
zero-only mathematical test:
  zero=100, raw=2097.5, ceiling=4095 -> 50.0% PASS
full calibration button/reference path removed: PASS
visual layout check: PASS
```

During the preceding SWD inspection, the MCU had been halted inside
`SPI_WaitFifoStateUntilTimeout` before any completed scan. A clean hardware
reset was therefore issued without a following debug halt. Pre-launch transport
check:

```text
valid frames: 20/20
sequence: 307..326
rate: 26.22 frame/s
```

The revised visible Heatmap was started on COM9:

```text
PID: 32800
window: Figure 1
status: responding
```

## 2026-07-26 - Root cause of repeated return to 1 frame/s

The link again measured approximately 1 frame/s after it had passed a clean
25 frame/s test. This time the permanent boot option was still correct:

```text
FLASH_OPTR: 0xFBEFF8AA
PC:         0x080011C8 (main Flash)
```

Therefore ROM boot was no longer the cause. A direct five-frame COM9 test,
independent of Heatmap, measured:

```text
valid frames: 5/5
sequence: 432..436
elapsed: 4.268 s
rate: 1.17 frame/s
```

Corrected live SWD diagnostics from the active ELF symbols showed:

```text
fsr_completed_scans:   1151
fsr_completed_frames:  1032
fsr_error_count:       119
fsr_last_error_stage:  HOST_TRANSFER
```

The crucial system-level cause was PC USB backpressure:

1. STM32 finishes one 16 x 16 scan, raises HOST_IRQ, and blocks waiting for the
   Teensy SPI master.
2. Teensy clocks the 515-byte frame, then calls the blocking
   `Serial.write(spiRx, 515)`.
3. When Heatmap/PC serial reading is closed between tests, the USB CDC transmit
   buffer fills after only a few seconds.
4. Teensy blocks inside `Serial.write()` and stops servicing the next
   HOST_IRQ.
5. STM32 waits 1000 ms in its SPI call, records a HOST_TRANSFER error, and
   retries. This creates the observed approximately 1 frame/s mode.
6. Reopening Heatmap does not reliably resynchronize a transaction that is
   already alternating between USB blockage and STM32 timeout.

This explains why every clean reset initially produced 25 frame/s and why the
rate degraded again after the PC reader was closed or restarted.

### Permanent transport correction

The Teensy bridge now treats the PC connection as a lossy newest-frame output,
not as a flow-control dependency for the STM32:

```cpp
if (Serial && Serial.availableForWrite() >=
                  static_cast<int>(FRAME_BYTES)) {
  Serial.write(spiRx, FRAME_BYTES);
} else {
  ++droppedUsbFrames;
}
```

If the PC is absent or slow, Teensy drops only the USB copy of that frame. It
still completes every STM32 SPI transaction immediately, so STM32 acquisition
cannot be stalled by USB.

Teensy compilation and upload completed successfully:

```text
FLASH code: 12548 bytes
RAM1 variables: 5344 bytes
board: teensy:avr:teensy41
port: COM9
```

Controlled validation:

```text
Initial connection:
  valid frames: 50/50
  sequence: 401..450
  gaps: 0
  rate: 25.07 frame/s

COM9 closed with no PC reader for 10 seconds:
  old blocking path would overflow and trigger the 1 Hz failure

Reconnect without resetting STM32 or Teensy:
  valid frames: 50/50
  sequence: 1045..1094
  gaps: 0
  rate: 25.27 frame/s
```

Result: the repeated 1 Hz failure was caused by Teensy USB CDC backpressure,
not Heatmap rendering, ADC timing, or BOOT0 after the option-byte correction.
The bridge now remains synchronized while the Heatmap is closed and resumes at
approximately 25 frame/s immediately when the PC reconnects.

A fresh visible Heatmap was started:

```text
PID: 20184
window: Figure 1
status: responding
```

## 2026-07-26 - Heatmap row and column numbering

The GUI now labels the matrix edges:

```text
columns: C0--C15 on both top and bottom
rows:    R0--R15 on both left and right
```

Labels use raw matrix coordinates and follow `--flip-rows` and
`--flip-columns`, so the displayed label always matches the raw coordinate
reported by hover inspection. Labels are placed outside the sensor cells to
avoid obscuring pressure colours.

Verification:

```text
Python compile check: PASS
labels created: 64
normal orientation mapping: PASS
flipped row/column mapping: PASS
visual overlap/readability check: PASS
```

The updated visible Heatmap was started:

```text
PID: 17860
window: Figure 1
status: responding
```

## 2026-07-26 - Consolidated row/column hardware inspection list

The retained raw-frame and full-calibration evidence was reviewed to separate
repeatable column faults from calibration-only outliers.

Highest-priority repeatable anomalies:

```text
matrix column   ADC input   column FPC pin   affected rows
C2              AIN2        14               R0--R15
C3              AIN3        13               R0--R15
C12             AIN12        4               R0--R15
C13             AIN13        3               R0--R15
```

These four complete columns contributed 64 of the 69 cells whose measured
zero-to-full span was below 50 ADC counts. They also overlap the fixed dark
vertical bands observed in the first Heatmap. They are the primary physical
inspection targets: FPC contacts, trace continuity, solder joints, and
continuity to the named MAX11633 AIN pins.

No complete row failed. The old full-calibration map contained five additional
isolated low-span cells distributed as follows:

```text
row distribution:    R0 = 3 cells, R13 = 1 cell, R14 = 1 cell
column distribution: C1 = 2 cells, C6 = 1 cell, C7 = 2 cells
```

The exact five row/column pairings are no longer recoverable because full-load
calibration was removed and the JSON now stores `full: null`. These isolated
results are lower-confidence: they may reflect a non-uniform full-load press
rather than broken hardware. Re-test the intersections of rows
`R0/R13/R14` with columns `C1/C6/C7` individually before repairing them.

Secondary low-baseline columns from the original 50-frame unloaded run were:

```text
C0, C5, C8, C10, C11, C15
```

These are not confirmed open circuits. Later loading produced valid calibration
span in them, so a low unloaded value is compatible with normal operation.
They should be inspected only if a controlled press fails to raise their raw
ADC codes.

## 2026-07-26 - Protocol-to-physical-cell mapping audit

The complete path was checked against the current STM32 firmware, Teensy
bridge, Python decoder, MAX11633 datasheet, mainboard 2.2 PCB, and FSR-array
2.4 PCB.

The 515-byte frame is:

```text
byte 0:       0xA5 frame marker
bytes 1..2:  uint16 sequence, little-endian
bytes 3..514: 256 uint16 ADC samples, little-endian and row-major
```

For raw matrix coordinate `(Rr, Cc)`, where `r,c = 0..15`:

```text
sample index       = 16*r + c
sample low byte    = 3 + 2*(16*r + c)
sample high byte   = 4 + 2*(16*r + c)
row signal         = MUX1 channel Ir
row connector      = ROW_FPC1 pin r+1
FSR row net        = /out r
column signal      = MAX11633 AINc
column connector   = COL_FPC1 pin c+1
FSR column net     = /ain c
physical cell      = intersection of /out r and /ain c
```

The MAX11633 command `0xF8` selects channel 15 with scan mode `00`, which the
datasheet defines as scanning AIN0 through AIN15. FIFO data is read oldest
first, so the firmware's `C0..C15 = AIN0..AIN15` interpretation is correct.
STM32 packs rows first, Teensy forwards all 515 bytes unchanged, and Python
unpacks 256 little-endian values in row-major order. No transpose or reversal
exists in the transport code.

Current PCB geometry confirms:

```text
R0 / out0 = top physical row
R15/out15 = bottom physical row
C0 / ain0 = left physical column
C15/ain15 = right physical column
```

Correction: the previous GUI and inspection note used `column FPC pin =
16-C`. That was incorrect for the current mainboard 2.2 and FSR-array 2.4
files. Both boards map connector pin 1 to AIN0/ain0 and pin 16 to AIN15/ain15.
The GUI hover text now reports both correct row and column connector pins.
Consequently, the four repeatedly abnormal columns map to:

```text
C2  = AIN2  = COL_FPC1 pin 3
C3  = AIN3  = COL_FPC1 pin 4
C12 = AIN12 = COL_FPC1 pin 13
C13 = AIN13 = COL_FPC1 pin 14
```

A 25-frame live capture on COM9 remained stable at approximately 25 frame/s.
It showed repeatable near-zero rows `R3, R6, R7, R8, R11, R12, R15` and
repeatable low columns `C0, C2, C3, C8, C10, C11, C12, C13, C15` under the
current physical condition. The strongest cells were around `R9/R13/R14` and
`C1/C4/C6/C7`. This capture proves the matrix values are stable, but whether
those values agree with applied pressure must be confirmed by pressing one
known physical cell and comparing the raw `(R,C)` peak.

## 2026-07-26 - Heatmap stuck at "Waiting for FSR frames"

COM9 enumeration was checked and the Teensy remained present as
`VID:PID 16C0:0483`. A hidden Python reader process was found holding COM9 and
was terminated. Opening the low-level reader after releasing the port still
produced no valid 515-byte frames.

Raw USB serial diagnostics for three seconds were:

```text
# WAIT_IRQ level=0
# BAD_MAGIC 0x00
# WAIT_IRQ level=0
# BAD_MAGIC 0x00
# WAIT_IRQ level=0
# BAD_MAGIC 0x01
```

This proves that the GUI was not the cause. Teensy was running and could see
occasional STM32 IRQ assertions, but SPI data did not begin with the required
`0xA5` frame marker.

The active `D:\app\python` pyOCD installation was also found incomplete:
`colorama`, then `psutil` and other dependencies were missing. pyOCD 0.45.1
and its complete dependencies were restored in that environment.

Recovery attempts:

```text
pyocd reset -t stm32g474cetx -f 10k -M under-reset -m hw
result: hardware reset completed

pyocd flash -t stm32g474cetx -f 10k -M under-reset ESKIN_STM32.elf
attempt 1: SWD No ACK
attempt 2: SWD No ACK

pyocd flash -t stm32g474cetx -f 100k -M under-reset ESKIN_STM32.elf
result: SWD No ACK
```

Current conclusion: reopening the Heatmap cannot recover this state. The
Teensy USB interface is alive, but STM32 is neither producing a valid SPI
frame nor responding reliably over SWD. Because the mainboard is powered from
Teensy, the next required recovery step is a real target power cycle: unplug
both Teensy and DAPLink USB, wait at least five seconds, reconnect Teensy first,
then reconnect DAPLink while leaving DAPLink 3.3 V disconnected. After power
returns, repeat the low-frequency under-reset flash and verify five binary
frames before opening the Heatmap.

Correction from the user: DAPLink was not connected to the mainboard during
the flash attempts. Therefore, the three `SWD No ACK` results were expected
and do **not** demonstrate unstable STM32 SWD communication. COM10 only showed
that the DAPLink USB device was enumerated by Windows. The valid diagnosis is
limited to the Teensy-side observation: COM9 and the Teensy firmware were
alive, but SPI transactions returned `0x00/0x01` instead of frame marker
`0xA5`.

With Teensy as the only mainboard power source, the first recovery action is
to unplug Teensy USB for at least five seconds and reconnect it. If valid
frames do not resume, DAPLink must then be physically connected to the
mainboard SWD header before any reset or reflash test is meaningful.

Post-power-cycle result:

```text
capture duration: 3 seconds
USB bytes received: 114
valid 0xA5 markers: 0
diagnostics:
  WAIT_IRQ level=0
  BAD_MAGIC 0x01
  WAIT_IRQ level=0
  BAD_MAGIC 0x00
  WAIT_IRQ level=0
  BAD_MAGIC 0x01
```

The Teensy/mainboard power cycle did not restore the binary protocol. Teensy
is running and performing SPI transactions after IRQ assertions, but the
STM32 response is not the complete-FSR firmware frame. The next meaningful
action is to connect DAPLink to the mainboard and reflash the verified
`ESKIN_STM32.elf`; further Heatmap restarts cannot fix this state.

DAPLink was subsequently connected to the mainboard. Recovery completed as
follows:

```text
1. pyocd reset -t stm32g474cetx -f 10k -M under-reset -m hw
   result: target identified; hardware reset completed

2. pyocd flash -t stm32g474cetx -f 10k -M under-reset ESKIN_STM32.elf
   result: completed successfully

3. Initial post-flash state
   result: Teensy reported WAIT_IRQ level=0

4. Explicit post-flash hardware reset at 10 kHz
   result: valid 0xA5 frames resumed

5. Binary validation
   valid frames: 50/50
   sequence: 360..409
   gaps: 0
   ADC range: 0..25
   rate: 25.15 frame/s
```

The important recovery detail is that a successful flash alone did not start
frame production in this session. An explicit hardware reset after flashing
was required. The corrected Heatmap was then launched on COM9 as PID 21120.

## 2026-07-26 - Requested STM32 reflash and manual procedure

The verified full FSR ELF was reflashed again with DAPLink at a 10 kHz SWD
clock. The first immediate reset attempt reported `already open`. After the
validation reader was terminated and the probe was allowed two seconds to
release, the hardware reset was repeated successfully. The exact holder of
the DAPLink interface was not independently identified, so the COM9 reader
must not be treated as the proven cause; COM9 and CMSIS-DAP are separate USB
interfaces.

Final validation:

```text
firmware: ESKIN_STM32/build/Debug/ESKIN_STM32.elf
flash: completed
post-flash hardware reset: completed
valid frames: 50/50
sequence: 220..269
gaps: 0
ADC range: 0..3154
rate: 25.10 frame/s
Heatmap PID: 18416
```

Manual PowerShell procedure from the `ESKIN_STM32` project directory:

```powershell
pyocd flash `
  -t stm32g474cetx `
  -f 10k `
  -M under-reset `
  ".\build\Debug\ESKIN_STM32.elf"

pyocd reset `
  -t stm32g474cetx `
  -f 10k `
  -M under-reset `
  -m hw
```

The second command is required in this setup even after a successful flash.
Before using pyOCD, stop any previous pyOCD GDB server or Cortex-Debug
session. Close the Heatmap only when running the subsequent COM9 validation.
Wiring is
`GND`, `SWDIO->PA13`, `SWCLK->PA14`, and `nRESET->NRST`; DAPLink 3.3 V remains
disconnected because Teensy supplies the mainboard.

## 2026-07-26 - GUI row/column transpose trial

The user reported that the physical row and column response appeared exchanged
in the Heatmap. A `--transpose` display option was added without altering the
STM32/Teensy transport protocol or stored raw matrix coordinates.

With transpose enabled:

```text
display horizontal axis: raw R0--R15 (MUX I0--I15, /out0--/out15)
display vertical axis:   raw C0--C15 (AIN0--AIN15, /ain0--/ain15)
hover coordinate:        converted back to the original raw (R, C)
calibration arrays:      remain in original raw protocol order
```

All eight combinations of transpose, row flip, and column flip were tested
against a 16x16 index matrix. Displayed values and inverse hover coordinates
matched in every case.

The previous GUI was closed and the transposed trial was launched:

```text
command: live_fsr_heatmap.py --port COM9 --transpose
PID: 29856
status: running
```

This is intentionally an explicit display mode rather than a transport
protocol change. It can be compared against a press on a known physical cell
before deciding whether transpose should become the permanent default.

## 2026-07-26 - Mirrored GUI and one-based visible numbering

The transposed Heatmap was mirrored left-to-right with `--flip-columns`.
Visible row and column identifiers were changed from zero-based `R0..R15` /
`C0..C15` to one-based `R1..R16` / `C1..C16`.

The one-based convention is presentation-only:

```text
GUI R1/C1   = protocol raw row 0 / column 0
GUI R16/C16 = protocol raw row 15 / column 15
```

Edge labels, title peak coordinates, and hover coordinates all use the same
one-based convention. Internal frame unpacking, calibration storage, MUX
channels, and ADC channels remain zero-based and unchanged.

Verification:

```text
leftmost displayed transposed cell maps to raw R15/C0: PASS
inverse hover coordinate after mirror: PASS
top labels after mirror: R16 down to R1: PASS
side labels: C1 through C16: PASS
```

The updated GUI was launched with:

```text
live_fsr_heatmap.py --port COM9 --transpose --flip-columns
PID: 6952
status: running
```

## 2026-07-26 - Full-pressure hardware fault update and corrected axis semantics

The user confirmed the actual hardware meaning:

```text
MUX-selected axis = physical columns
MAX11633 ADC axis = physical rows
```

Therefore the raw protocol matrix remains `matrix[mux_index][ain_index]`, but
its physical cell name is:

```text
physical column = mux_index + 1
physical row    = ain_index + 1
cell            = R(ain_index + 1), C(mux_index + 1)
```

This supersedes earlier notes that described MUX as rows and ADC as columns.
The byte order itself was not wrong; only the physical R/C naming was wrong.

The GUI was corrected consistently:

```text
top/bottom labels after transpose + mirror: C16..C1
left/right labels:                          R1..R16
title peak coordinate:                      R=AIN, C=MUX
hover coordinate:                           R=AIN, C=MUX
MUX connector description:                  column FPC
ADC connector description:                  row FPC
```

A 50-frame median capture was taken while the complete FSR surface was held
under pressure:

```text
sequence: 20600..20649
rate: 24.71 frame/s
global ADC range: 0..2947
```

Confirmed or very strong column fault candidates on the MUX/column side:

```text
physical column   MUX channel   maximum under full press   classification
C3                I2             1                          dead
C4                I3             1                          dead
C8                I7             7                          dead
C9                I8            19                          dead
C12               I11            1                          dead
C13               I12            1                          dead
C16               I15            1                          dead
C1                I0            60                          severely weak
```

Confirmed or very strong row fault candidates on the ADC/row side:

```text
physical row   ADC input   maximum under full press   classification
R1             AIN0         2                          dead
R2             AIN1        19                          dead
R3             AIN2         1                          dead
R4             AIN3         0                          dead
R13            AIN12        6                          dead
R14            AIN13        1                          dead
R16            AIN15       37                          dead
R11            AIN10       65                          severely weak
R12            AIN11      175                          weak
```

Healthy high-response groups in the same capture were:

```text
MUX columns: C2, C5, C6, C7, C10, C11, C14, C15
ADC rows:    R5, R6, R7, R8, R9, R10, R15
```

Because complete columns fail together, inspect the MUX-column FPC,
corresponding MUX inputs, and column electrode continuity first. Because
complete rows fail together, inspect the ADC-row FPC contacts, protection/input
network, and the named MAX11633 AIN pins. The full-pressure capture makes these
fault assignments substantially stronger than the earlier unloaded baseline
analysis.

## 2026-07-27 - Left-right mirror made the GUI default

`flip-columns` is now enabled by default in `live_fsr_heatmap.py`. The normal
physical launch command is reduced to:

```powershell
python live_fsr_heatmap.py --port COM9 --transpose
```

The previous explicit `--flip-columns` argument remains accepted for backward
compatibility but is no longer required. A new opt-out is available:

```powershell
python live_fsr_heatmap.py --port COM9 --transpose --no-flip-columns
```

This changes display orientation only. Raw protocol order, calibration order,
MUX/ADC mapping, and one-based physical labels are unchanged.

Verification:

```text
Python syntax: PASS
--flip-columns help/default path: PASS
--no-flip-columns override exposed: PASS
```

## 2026-07-27 - Horizontal and vertical data axes exchanged

At the user's request, the active GUI orientation was changed back to the
non-transposed matrix view. No transport or calibration data was rewritten.

The resulting physical display is:

```text
horizontal axis: ADC physical rows, R16..R1 after the default mirror
vertical axis:   MUX physical columns, C1..C16
```

The normal launch command is now:

```powershell
python live_fsr_heatmap.py --port COM9
```

`--transpose` remains available if the previous orientation is needed again.

## 2026-07-27 - Transpose restored and made default after physical motion test

The non-transposed trial was rejected by the physical motion test: moving
across the sensor horizontally from 1 to 16 still moved vertically in the GUI.

Root cause:

```text
raw matrix axis 0 = MUX physical column
NumPy axis 0      = display vertical direction
```

Therefore raw MUX column movement appears vertical unless the matrix is
transposed for display. Transpose is now enabled by default, together with the
existing default left-right mirror:

```text
GUI horizontal axis = MUX physical columns, C16..C1
GUI vertical axis   = ADC physical rows, R1..R16
```

Normal command:

```powershell
python live_fsr_heatmap.py --port COM9
```

Optional raw-matrix diagnostic view:

```powershell
python live_fsr_heatmap.py --port COM9 --no-transpose
```

This correction changes display orientation only. Protocol byte order and raw
calibration coordinates remain unchanged.

## 2026-07-27 - Latest 50-frame hardware anomaly classification

A new 50-frame raw capture was taken after the final default orientation was
established:

```text
sequence: 43120..43169
rate: 25.03 frame/s
global median ADC: 1
global maximum ADC: 2780
```

Assuming the user's full-surface pressure was still maintained during this
capture, the latest path-level classification is:

```text
MUX physical columns, dead/near-dead (path maximum <= 10):
C1, C4, C6, C7, C8, C9, C12, C13, C16

MUX physical columns, severely weak:
C2 (max 53), C3 (max 31)

MUX physical columns, strong response:
C5, C10, C11, C14, C15
```

```text
ADC physical rows, dead/near-dead (path maximum <= 10):
R1, R3, R4, R13, R14

ADC physical rows, severely weak:
R2 (max 21), R6 (max 91), R11 (max 57),
R12 (max 146), R16 (max 21)

ADC physical rows, strong response:
R5, R7, R8, R9, R10, R15
```

This capture differs from frame 40327 in the supplied screenshot, especially
for R1/R2. The screenshot showed R1/R2 responding, while the later raw capture
showed them near zero. Therefore R1/R2 may be intermittent contacts or the
applied pressure/contact state changed between the screenshot and capture.
Repeatability under a fixed press is required before replacing components.

The Heatmap was reopened after capture as PID 29208.

## 2026-07-27 - Follow-up raw capture after Figure_1.png

A further 50-frame capture was taken after the user supplied
`Figure_1.png`. COM9 was first released by terminating the stale Python serial
reader (PID 16532).

```text
sequence: 3306..3355
rate: 24.93 frame/s
global ADC range: 0..2450
```

Current MUX-column result:

```text
dead/near-dead: C3, C4, C5, C6, C7, C8, C9, C12, C13, C16
responding:     C1, C2, C10, C11, C14, C15
```

Current ADC-row result:

```text
dead/near-dead: R1, R12, R13, R14
severely weak:  R6, R9, R11, R16
responding:     R2, R3, R4, R5, R7, R8, R10, R15
```

Across repeated captures, the highest-confidence recurring MUX faults remain:

```text
C3, C4, C8, C9, C12, C13, C16
```

`C5/C6/C7` changed between strong and dead across captures, so they are
intermittent rather than confirmed permanently open. The highest-confidence
recurring ADC-side problems are `R12/R13/R14`, with `R11/R16` repeatedly weak
or dead. Other row changes indicate intermittent contact or changed pressure.

The screenshot and later raw capture are not identical (for example R1 appears
active in the screenshot but was near zero in the capture). Fault replacement
decisions must therefore prioritize channels that repeat across captures, not
a single colour image. The Heatmap was reopened as PID 9460.

## 2026-07-27 - Full software-chain re-review after hardware repair

The complete active path was reviewed again under the working assumption that
the connector, power, SPI3 link, MUX address lines, and FSR interconnect are now
electrically sound:

```text
STM32 MUX selection
  -> MAX11633 conversion/FIFO read
  -> STM32 515-byte frame packing
  -> Teensy SPI master and USB forwarding
  -> Python binary decoder
  -> Heatmap orientation and calibration
```

### Verified items

- A clean CMake rebuild of the STM32 Debug target completed successfully with
  `-Wall`; there were no compiler warnings or errors.
- `0x10` correctly resets all MAX11633 registers.
- `0x68` correctly selects clock mode `10`, keeps AIN15 available as an analog
  input, and keeps the internal 2.5 V reference enabled.
- `0xF8` correctly requests one FIFO result from every input AIN0 through
  AIN15.
- EOC polling polarity is correct: EOC is high while the requested operation is
  running and low after the final result is available.
- MAX11633 FIFO results are MSB-first 12-bit values with four leading zero
  bits; the STM32 decoder implements this correctly.
- STM32 stores the raw matrix as
  `frame[mux_column][adc_row]`, packs each sample little-endian, and sends the
  matrix in raw row-major memory order.
- Teensy transfers exactly 515 bytes in SPI mode 0, verifies the `0xA5` marker,
  and forwards the bytes without rearranging the payload.
- A generated 256-value frame passed the Python decoder test, including the
  sequence number, little-endian samples, first cell, last cell, and an interior
  cell.
- All eight combinations of GUI transpose, row flip, and column flip passed an
  exhaustive 16 x 16 display-to-raw inverse-coordinate test.
- The current default display remains physically consistent with the confirmed
  sensor semantics: horizontal is MUX column, vertical is ADC row, with the
  requested left-right mirror.

No software evidence was found for a byte-order error, a one-sample offset, an
accidental matrix transpose in the transport, or a wrong MAX11633 channel-scan
command. Those mechanisms do not explain the recurring low rows and columns.

### Remaining firmware weakness: ADC acquisition settling

The MAX11633 datasheet states that internally timed conversions have a minimum
acquisition time of approximately 1.4 us and that the required acquisition time
increases with analog source impedance:

```text
tACQ = 9 x (RS + 1.5 kOhm) x 24 pF + 1 us
```

The current firmware waits 1 ms after selecting each external MUX column, but
this wait only allows the external FSR/MUX node to settle before the scan. It
does not lengthen the MAX11633 track-and-hold acquisition interval for each
AIN channel. The subsequent `0xF8` command still samples AIN0 through AIN15
using the ADC's internally timed minimum acquisition. An FSR matrix can present
a high and pressure-dependent source impedance, so the present configuration
has not proved that each ADC input settles to 12-bit accuracy. Possible
symptoms are low readings, dependence on the previously converted channel,
crosstalk, and different apparent response among ADC rows.

This is the highest-priority software-side hypothesis remaining. It is not yet
proof that it caused every observed low path. The decisive next firmware A/B
test is to retain the current production image and build a diagnostic variant
that performs repeated conversions/averaging for each AIN result, discards an
initial sample where appropriate, and compares the fixed-pressure raw matrix
against the current single-sample scan.

### Other non-root-cause limitations

- The frame has a magic byte and sequence number but no CRC. A rare payload bit
  error could therefore be accepted silently. This does not match the stable,
  path-shaped patterns seen so far, but a CRC should be added before treating
  the link as production-ready.
- Teensy diagnostic ASCII lines and binary frames share one USB stream. The
  Python decoder deliberately resynchronises on `0xA5`, so this is not the
  matrix-mapping fault, but separate diagnostics or a framed message type would
  make the protocol more robust.
- Zero calibration is a per-cell baseline subtraction followed by scaling to
  ADC code 4095. It does not convert ADC code to force, compensate the nonlinear
  FSR response, or replace a controlled fixed-pressure comparison.

No production source was changed during this diagnostic review. The STM32
clean rebuild did regenerate the verified Debug ELF.

## 2026-07-27 - Targeted R1/R12/R13/R14 end-to-end response test

A new non-destructive PC diagnostic was added:

```text
teensy41/ESKIN_SPI_USB_BRIDGE/test_adc_rows.py
```

It retains the current STM32 and Teensy production images and checks the four
physical ADC rows that were repeatedly weak or inactive:

```text
physical R1  = MAX11633 AIN0  = raw matrix axis 1 index 0
physical R12 = MAX11633 AIN11 = raw matrix axis 1 index 11
physical R13 = MAX11633 AIN12 = raw matrix axis 1 index 12
physical R14 = MAX11633 AIN13 = raw matrix axis 1 index 13
```

The test captures 50 unloaded frames, then captures 75 frames while each target
row is pressed separately. It uses the median raw ADC change across every MUX
column and declares a response when the strongest cell changes by at least
50 ADC counts. Both increases and decreases are accepted because this is an
electrical response test rather than a pressure calibration. For every press it
also reports the strongest responding row anywhere in the matrix. If the
requested row remains unchanged but a different row responds, the result points
to physical numbering or AIN mapping rather than a completely missing signal.

The script distinguishes two different statements:

- receipt of a valid 515-byte frame proves that every row slot is present in
  the protocol;
- a significant raw change proves that the corresponding analog row responds
  to pressure.

An ADC value of zero is valid data and is not treated as a missing byte.
Sequence gaps and measured frame rate are included in every result, and the
complete baseline, pressed matrices, and decisions are saved to a timestamped
JSON file.

Validation performed:

```text
Python AST syntax: PASS
synthetic R12/AIN11 response at C7: PASS
no-response decision for R13: PASS
raw matrix[MUX column][ADC row] indexing: PASS
command-line help: PASS
```

Normal command, after closing the Heatmap and Arduino Serial Monitor:

```powershell
cd "D:\OneDrive - Imperial College London\study\Imperial College\BIOE70025 - MSc Major Individual Project 2025-2026\E-SKIN\hardware\new\firmware\teensy41\ESKIN_SPI_USB_BRIDGE"
python test_adc_rows.py --port COM9
```

At the time the diagnostic was created, Windows enumerated COM5, COM6, COM7,
COM8, and COM10 but not COM9. Consequently the live pressure portion was not
started. Reconnect Teensy and substitute its current COM number if Windows
assigns a different port.

## 2026-07-27 - Analysis of the first targeted row-test result

Result file:

```text
adc_row_test_20260727_150937.json
port: COM9
threshold: 50 ADC counts
targets: R1, R12, R13, R14
```

The unloaded baseline was internally consistent:

```text
frames: 50
sequence: 20158..20207
sequence gaps: 0
elapsed: 1.976 s
accepted rate: 25.30 frame/s
```

The provisional pressure decisions stored by the first test run were:

```text
target   ADC input   strongest target change   provisional result
R1       AIN0        +3137 at C11              response detected
R12      AIN11       +2 at C3                  no target response
R13      AIN12        0                         no target response
R14      AIN13        0                         no target response
```

The individual raw medians reinforce the provisional distinction. During the
R1 press, AIN0 increased by approximately 866--3137 counts on most columns.
During the R12 press, AIN11 stayed between 1 and 3 counts. During the R13 and
R14 presses, every target-row median was exactly 1.

However, the four pressed captures failed the frame-integrity check:

```text
target   accepted frames   sequence range   missing sequence numbers
R1       75                20232..20427     121
R12      75                20452..20792     266
R13      75                20817..21468     577
R14      75                21493..21681     114
```

The R13 sequence advanced by 651 during approximately 2.9 seconds. This is
physically incompatible with the current acquisition loop and 100 kHz,
515-byte Teensy SPI transaction. It cannot be explained by ordinary dropped
USB copies alone.

The likely software mechanism is false frame synchronisation. The protocol has
only a one-byte `0xA5` marker and no length field or CRC. The Python reader
searches for any `0xA5`; an ADC sample whose low byte equals `0xA5` can therefore
be mistaken for a frame start after `reset_input_buffer()` begins reading in
the middle of a continuous binary stream. If the false start remains aligned
to 16-bit sample boundaries, the existing `sample <= 4095` validation can also
pass. This can yield plausible-looking matrices with invalid sequence numbers
and shifted cell coordinates.

Consequently:

- R1 very likely has a real analog response, but its pressed capture was not
  integrity-clean.
- R12, R13, and R14 are strong no-response candidates, but this JSON file is
  not sufficient to confirm them.
- The result must be repeated after strengthening the parser to acquire and
  retain a true 515-byte frame boundary and to reject sequence-discontinuous
  candidates.
- A CRC in the STM32/Teensy frame is the production fix. A short-term PC-only
  fix can require multiple consecutive 515-byte frames with incrementing
  sequence numbers before accepting lock.

## 2026-07-27 - Why one pressed row can appear on multiple rows

The first targeted row test cannot yet be used to measure true row-to-row
crosstalk. Its baseline had zero sequence gaps, but every pressed capture had
large, physically impossible sequence jumps. False synchronisation on an
`0xA5` byte inside the ADC payload can shift the matrix and place values on
unrelated displayed rows. This is the first explanation to eliminate because
it is directly supported by the captured metadata.

After frame locking is corrected, three physical effects can still produce
smaller neighbouring-row responses:

1. Mechanical load spreading. A finger, plate, elastomer, or flexing PCB loads
   more than one FSR intersection. Adjacent rows should respond mainly near the
   pressed column and normally decay with distance from the contact.
2. Passive-matrix ghosting. The FSR sheet is a resistive row/column network
   without a diode at every cell. Current can find additional paths through
   other pressed or partially conductive cells, especially when pressure spans
   multiple columns. This usually forms rectangular or line-shaped patterns.
3. MAX11633 track-and-hold memory. Firmware requests AIN0 through AIN15 as one
   internally timed scan. Each input receives only the ADC's short internal
   acquisition interval. With a high and pressure-dependent FSR source
   impedance, residual charge from the previously sampled AIN channel can
   influence the next result. This normally follows ADC scan order and is
   distinct from symmetric mechanical spreading.

The Heatmap zero calibration can amplify small residual changes visually, but
it does not create raw ADC changes. A correct isolation test must therefore use
raw deltas, not calibrated colours.

The diagnostic pattern should be classified as follows after clean frame lock:

```text
unrelated row + sequence gaps       -> transport/parser misalignment
adjacent rows, same column, decay   -> mechanical pressure spreading
next AIN row in scan order          -> ADC acquisition/charge memory
rectangle across rows and columns   -> passive-matrix ghosting
target row alone                    -> ideal electrical selectivity
```

Use a small rigid probe on one cell rather than pressing an entire row, keep all
other cells unloaded, and require zero sequence gaps before drawing a hardware
conclusion.

## 2026-07-27 - False R8/R8/R15 response parser fix

The one-byte-marker resynchronisation in `read_teensy_fsr.py` was replaced by a
stateful fixed-width `FrameReader`. It no longer accepts the first `0xA5` found
in the stream. A frame boundary is now locked only when three candidates:

1. begin exactly 515 bytes apart;
2. each begin with `0xA5`;
3. each contain 256 valid 12-bit samples; and
4. have strictly consecutive 16-bit sequence numbers.

After acquisition, the reader remains on fixed 515-byte boundaries. A bad
marker, invalid sample, or unexpected sequence causes immediate lock loss and a
fresh three-frame search. This prevents an ADC sample whose low byte is `0xA5`
from moving pressure data onto unrelated displayed rows such as the provisional
R8/R8/R15 results.

The verified reader is now used consistently by:

```text
read_teensy_fsr.py
live_fsr_heatmap.py
test_adc_rows.py
```

`test_adc_rows.py` explicitly clears the reader's internal lock whenever it
flushes the serial input between operator-controlled pressure phases. This
prevents a partial post-flush frame from inheriting the previous boundary.

Offline regression tests:

```text
AST syntax, all three Python files: PASS
many deliberate 0xA5 bytes inside every ADC payload: PASS
expected decoded sequences: 100,101,102,103,104,105
deliberately corrupted frame 203: rejected
lock reacquisition: 200,201,202,204,205,206
matrix[MUX column][ADC row] indexing self-test: PASS
```

Live COM9 validation after the change:

```text
valid frames: 50/50
sequence: 39501..39550
sequence gaps: 0
ADC range: 0..2508
elapsed: 2.087 s
rate: 23.95 frame/s
```

The corrected interactive R1/R12/R13/R14 test was then opened in a new
PowerShell window. Hardware conclusions must use the new timestamped JSON and
require zero sequence gaps in every pressure phase. The earlier
`adc_row_test_20260727_150937.json` remains archived but is not suitable for
final row mapping.

## 2026-07-27 - Corrected targeted-row test result

Corrected result file:

```text
adc_row_test_20260727_152302.json
baseline sequence: 41800..41849
baseline gaps: 0
baseline rate: 23.78 frame/s
```

The new `FrameReader` was active for this run. Every accepted frame therefore
passed the three-consecutive-frame boundary lock before its matrix was decoded.
The sequence gaps reported between accepted groups represent complete frames
that were not delivered successfully; they no longer imply that an arbitrary
payload `0xA5` was accepted as a frame boundary. Accepted matrix coordinates
remain valid after each reacquisition.

Target results:

```text
pressed row   ADC input   target-row maximum change   strongest observed row
R1            AIN0        +3143 at C3                  R1, +3143
R12           AIN11       +2 at C3                     R8, +2753
R13           AIN12        0                           R7, +2632
R14           AIN13        0                           R15, +2950
```

The corrected result confirms:

- R1/AIN0 is responsive. Its response is large and is correctly strongest on
  the requested physical row.
- R12/AIN11 remains at approximately 1--3 ADC counts on all 16 MUX columns.
- R13/AIN12 remains exactly 1 on all 16 MUX columns.
- R14/AIN13 remains exactly 1 on all 16 MUX columns.
- R12, R13, and R14 therefore show no measurable target-channel response under
  the performed pressure test. This is no longer explained by the original
  one-byte frame-marker parser.

Many other ADC rows changed strongly during every pressure phase. The dominant
non-target rows were not a consistent one-to-one permutation:

```text
R12 press -> strongest R8, with R7 also nearly equal
R13 press -> strongest R7, with R8 also nearly equal
R14 press -> strongest R15, with R8/R7 also strong
```

The repeated broad group also included R5, R9, R10, R15, R2, and R3. Because
this is not a stable mapping such as `physical R12 always becomes ADC R8`, it
does not support a simple GUI row permutation. It is more consistent with
large-area mechanical loading, passive-matrix current paths, sensor hysteresis
relative to the single initial baseline, or ADC high-source-impedance
acquisition effects. The current test used one baseline for all four pressure
phases, so residual load or slow recovery can accumulate in later comparisons.

The next isolation firmware test should sample AIN11, AIN12, and AIN13
individually with repeated conversions/averaging and compare them with the
current AIN0--AIN15 scan. That will distinguish an ADC acquisition-time problem
from a row input that is electrically fixed near ground.

## 2026-07-27 - Heatmap display sensitivity reduced to one quarter

The calibrated Heatmap display gain was reduced from `1.0x` to `0.25x` by
default. This is a PC-visualisation change only:

```text
new displayed pressure = old displayed pressure x 0.25
```

Raw MAX11633 ADC codes, STM32 acquisition, SPI/USB frame bytes, matrix
coordinates, zero calibration data, and hardware sensitivity are unchanged.
Peak percentages, cell colours, hover percentages, status text, and the
colorbar now all use and identify the same 0.25x gain.

A new command-line option permits explicit control:

```powershell
# New default, one-quarter display sensitivity
python live_fsr_heatmap.py --port COM9

# Restore the previous display sensitivity
python live_fsr_heatmap.py --port COM9 --sensitivity 1.0
```

Validation:

```text
Python AST syntax: PASS
0.25x result divided by 1.0x result: exactly 0.25
default sensitivity constant: 0.25
command-line help: PASS
```

The revised Heatmap was opened on COM9 as PID 29092.

## 2026-07-27 - Isolated black-cell Heatmap explanation

The supplied screenshot included the decisive hover values:

```text
raw ADC: 2160
stored zero: 2564
display gain: 0.25x
MUX I9: physical C10
```

The calibration file confirms that `zero[9][6] = 2564`, so the hovered cell is
raw matrix `[MUX I9][AIN6]`, physically `C10/R7`. Its current normalized value
is:

```text
((2160 - 2564) / (4095 - 2564)) x 0.25
= -0.06597
clipped to 0.0
```

Inferno colour value zero is black. Therefore this particular black cell is
fully explained by the current per-cell calibration calculation; it is not
evidence of a missing frame byte or a failed complete row/column path.

The current zero file was saved at `2026-07-27T14:30:43Z`. Its C10 calibration
values vary substantially by physical row, including:

```text
C10/R7 zero = 2564.0
C10/R8 zero = 2571.5
```

Only these two cells in the complete 16 x 16 zero map have a stored zero of at
least 2500. A zero capture made with residual pressure, FSR hysteresis, baseline
drift, or an opposite-going raw response at one cell can therefore create an
isolated black cell even when all bytes and coordinates are valid.

Current Heatmap processing order:

```text
verified 515-byte frame
-> raw uint16 matrix[MUX column][ADC row]
-> per-cell zero subtraction
-> divide by per-cell (4095 - zero) span
-> multiply by 0.25 display gain
-> clip each cell independently to 0..1
-> transpose for physical axes
-> left-right flip
-> flatten into 256 PCB polygons
-> inferno colour map
```

Because calibration is independent for every intersection, a single cell can
be black while every other cell on the same physical row and column works.
The 0.25x change makes low positive responses darker but does not cause this
cell's zero result; it was already negative before the gain was applied.

## 2026-07-27 - Two-point per-cell ADC calibration restored

The temporary `0.25x` display-gain approach was removed. The Heatmap now uses
two raw ADC endpoint maps for every FSR intersection:

```text
zero[mux][ain] = median raw ADC with no pressure
full[mux][ain] = median raw ADC at the intended full pressure

calibrated = (current_adc - zero) / (full - zero)
calibrated = clipped to 0..1
```

This transformation does not alter the MAX11633 conversion register or the
515-byte STM32/Teensy frame. The transmitted raw ADC code remains available and
is shown in the hover text. The change replaces display-only gain with a
per-cell ADC-domain correction on the PC.

Both response polarities are supported:

```text
increasing cell: zero 1000, full 3000, current 2000 -> 50%
decreasing cell: zero 3000, full 1000, current 2000 -> 50%
```

This directly fixes the previous assumption that every pressed cell must have
`ADC > zero`. A cell such as C10/R7 can now be calibrated correctly if its full
pressure ADC is below its unloaded ADC.

GUI changes:

- `Calibrate zero` captures 40 unloaded frames and stores their per-cell median.
- A new `Calibrate full` button captures 40 full-pressure frames and stores
  their per-cell median.
- Capturing a new zero map deliberately clears the previous full map.
- Full calibration cannot start before a zero map exists.
- The Heatmap switches to calibrated percentage only when both maps exist.
- Hover text reports raw ADC, zero ADC, full ADC, signed span, and percentage.
- A cell is invalid when `abs(full - zero) < 50` ADC counts. The absolute value
  is required so decreasing-response cells remain valid.
- The calibration JSON continues to store both 16 x 16 raw endpoint arrays.

Validation:

```text
Python AST syntax: PASS
increasing endpoint midpoint: 50%, PASS
decreasing endpoint midpoint: 50%, PASS
absolute span below 50 rejected: PASS
zero-only file treated as incomplete: PASS
```

The older Heatmap PID 29092 was closed. The two-point version was opened on
COM9 as PID 4312 and was confirmed responsive. The existing calibration file
contains a zero map but no full map, so the operator must complete the two
captures before calibrated percentages are displayed.

## 2026-07-27 - Feasibility of moving calibration into STM32

The current calibration JSON was checked again after the two-point GUI run:

```text
updated: 2026-07-27T14:52:15Z
zero map: present, 16 x 16
full map: present, 16 x 16
```

The MAX11633 cannot be programmed with per-cell minimum and maximum values. It
always returns its raw 12-bit result from 0 through 4095; its registers control
scan, reference, timing, reset, and averaging, not 256 independent calibration
endpoints.

Calibration can instead be compiled into STM32 Flash as two signed 16 x 16
tables and applied immediately after each raw conversion:

```text
span = full[mux][ain] - zero[mux][ain]
calibrated = (raw - zero) * 4095 / span
calibrated = clipped to 0..4095
```

Signed 32-bit arithmetic is required because some cells have `full < zero`.
Cells with `abs(span) < 50` require an invalid flag.

Changing STM32 to transmit calibrated values without changing the protocol
would make the PC mistake calibrated codes for raw ADC codes and apply the
calibration a second time. A safe embedded implementation therefore also needs
a protocol version or frame flag, and a decision whether to transmit:

- calibrated values only, retaining the current frame size and rate;
- raw and calibrated values together, approximately doubling the payload and
  reducing the current 100 kHz frame rate; or
- raw values in a diagnostic mode and calibrated values in the normal mode.

No STM32 or protocol source was changed during this feasibility check.

## 2026-07-27 - Reference voltage correction from mainboard 2.2 netlist

The current `mainbord/2.2/mainboard.net` was inspected directly. The fitted
converter is identified as `MAX11633EEGT`. Net code 1 is named `+3.3V`, and the
same network contains all four relevant nodes:

```text
COL_ADC1 pin 17: REF
COL_ADC1 pin 19: VDD
COL_ADC2 pin 17: REF
COL_ADC2 pin 19: VDD
```

Therefore mainboard 2.2 is wired for the 3.3 V rail to act as an external ADC
reference. The earlier assumption that REF only had a bypass capacitor for the
MAX11633 internal 2.5 V reference was incorrect for this board revision.

This reveals a firmware/hardware mismatch:

```text
current setup 0x68:
  CKSEL = 10, internally timed conversion
  REFSEL = 10, internal 2.5 V reference always on

hardware expectation:
  CKSEL = 10, internally timed conversion
  REFSEL = 01, external reference on REF pin

matching setup byte = 0x64
```

With the matching external-reference configuration, ADC code represents the
ratio to the actual REF rail:

```text
code approximately equals Vin / VREF x 4096
VREF is the measured 3.3 V rail, not an assumed exact 3.300 V
```

At exactly 3.3 V, one LSB is approximately 0.806 mV. If the rail is 3.2 V, one
LSB is approximately 0.781 mV. Because the FSR network and ADC reference can be
derived from the same rail, the measurement is partly ratiometric and supply
variation can cancel in the code ratio.

Changing `0x68` to `0x64` will change the conversion scale and invalidates
previous zero/full endpoint maps. Both calibration stages must be repeated
after the corrected STM32 firmware is flashed. No firmware was changed during
this netlist diagnosis.

## 2026-07-27 - External 3.3 V ADC reference firmware change and reflashing

The MAX11633 setup command in `ESKIN_STM32/Core/Src/main.c` was changed from
`0x68` to `0x64`.

```text
old 0x68: CKSEL=10, REFSEL=10, internal 2.5 V reference always on
new 0x64: CKSEL=10, REFSEL=01, external reference on the REF pin
```

The associated source comments now state that mainboard revision 2.2 ties
MAX11633 REF and VDD to the `+3.3V` network. Build, flash, and verification
results are recorded below after each operation.

Build result:

```text
SUCCESS
RAM used:    3392 bytes / 128 KiB (2.59%)
Flash used: 16672 bytes / 512 KiB (3.18%)
Output: ESKIN_STM32/build/Debug/ESKIN_STM32.elf
```

First flash attempt:

```text
pyocd flash -t stm32g474cetx -f 10k -M under-reset -e sector
            build/Debug/ESKIN_STM32.elf

The target was opened and sector erase started, but the command did not return
the required Programming/100% completion result.
```

Immediate verification retry:

```text
Target type is stm32g474cetx
Error: already open
```

This retry result means that the DAPLink interface was still occupied or had
not been released after the interrupted first operation. At this point the
firmware build was valid, but successful programming had not yet been proven.

Final flash retry after the stale interface was released:

```text
command:
  pyocd flash -t stm32g474cetx -f 10k -M under-reset -e sector
              build/Debug/ESKIN_STM32.elf

result:
  Erased 2048 bytes (1 sector)
  Programmed 2048 bytes (2 pages)
  Identical 15360 bytes (15 pages)
  Transfer rate 0.25 kB/s
  Process exit code 0
```

The small programmed-byte count is expected for an incremental firmware
change: pyOCD compared the remaining pages and reported them as identical.

A final hardware reset was then issued:

```text
pyocd reset -t stm32g474cetx -f 10k -M under-reset -m hw
Process exit code 0
```

The corrected firmware was therefore built, programmed, and restarted
successfully. The flashed ELF metadata is:

```text
size:     1226304 bytes (ELF file including debug information)
modified: 2026-07-27 16:17:10 +01:00
SHA-256:  99B2663F32D99DF7B722F247425851D0A4C7534603F1C042E4CB5E89A10612E9
```

Required next measurement step: discard the old per-cell zero/full calibration
and capture both endpoints again because the ADC reference selection changed
from the internal 2.5 V source to the board's external 3.3 V rail.

## 2026-07-27 - Calibration procedure after the 3.3 V reference flash

Calibration and STM32 programming are separate in the current implementation.
The corrected STM32 application has already been programmed. The calibration
maps are captured by the PC Heatmap and saved locally; they are not programmed
into STM32 Flash.

Procedure:

1. Power the Teensy and mainboard, close Arduino Serial Monitor/Plotter, and
   start `live_fsr_heatmap.py --port COM9`.
2. Wait for valid frames at approximately 25 frame/s. Calibration is blocked
   below 10 frame/s.
3. Remove every load from the array, wait for the readings to settle, click
   `Calibrate zero`, and keep all cells unloaded for 40 frames.
4. Apply the intended repeatable full-pressure reference uniformly to all
   256 cells, click `Calibrate full`, and hold the load unchanged for 40
   frames.
5. Confirm that the status reports the zero and full calibration as saved.

Each endpoint is the per-cell median of 40 received frames. Capturing a new
zero endpoint invalidates and removes the previous full endpoint. The result is
saved automatically to:

```text
teensy41/ESKIN_SPI_USB_BRIDGE/fsr_calibration.json
```

The PC displays each valid cell using:

```text
pressure = clamp((raw - zero) / (full - zero), 0, 1)
```

The implementation also supports a decreasing ADC response because the
full-minus-zero span may be negative. A cell is considered invalid if the
absolute endpoint span is below 50 ADC counts.

## 2026-07-27 - Heatmap raw-frame CSV save button

The live Heatmap now includes a third front-end button named
`Save raw frame`. Clicking it writes the most recently received, uncalibrated
STM32 ADC frame to a timestamped CSV file.

The transport matrix is stored as `[MUX column][ADC row]`. The CSV writer
transposes this once so the saved table uses physical coordinates:

```text
first column: R1 ... R16
header row:   C1 ... C16
cell Rn,Cm:   raw 12-bit ADC count for physical row n and column m
```

This export deliberately ignores Heatmap transpose, flip, colour scaling, and
zero/full calibration. It therefore preserves the original STM32 data while
presenting it with the corrected physical row/column naming.

Files are saved by default under:

```text
teensy41/ESKIN_SPI_USB_BRIDGE/raw_frames/
```

Example filename:

```text
fsr_raw_frame_00042_20260727T123456_123456Z.csv
```

The destination can be changed when starting the Heatmap with
`--raw-output-directory PATH`.

Verification:

```text
Python syntax parse: PASS
CSV header R/C labels: PASS
raw [MUX column][ADC row] to physical [row][column] mapping: PASS
```

The deterministic mapping test used a raw matrix containing 0 through 255.
The exported R1 row was `0,16,...,240`, and R16 was `15,31,...,255`, proving
that the transpose was applied exactly once. The first test interpreter
(`D:/app/python`) could not import Matplotlib because its `packaging`
dependency was missing; rerunning the same test with the installed Anaconda
environment passed. No STM32 or Teensy firmware change or reflashing was
required for this PC-only front-end feature.

## 2026-07-27 - FSR1 baseline backup and FSR2 (MUX2/ADC2) test firmware

Before changing the acquisition region, the complete verified STM32 project was
copied from `ESKIN_STM32` to:

```text
hardware/new/firmware/FSR_left
```

The source and backup each contained 192 files. The original and copied
`Core/Src/main.c` files had the same SHA-256:

```text
7B8D0869EDF50D7622668A728B19542A64483689BB4F0784D5FF8F5F7B4B971C
```

The mainboard 2.2 netlist was checked before editing. FSR2 uses:

```text
shared SPI1 SCK/MISO/MOSI: PA5 / PA6 / PA7
shared MUX address S0..S3: PA0 / PA1 / PA2 / PA3
ADC2 chip select CS2:      PB1, active low
ADC2 end of conversion:    PB11, active low
MUX2 enable EN2:           PB2, active low
```

The working `ESKIN_STM32` project was updated to scan only FSR2:

- MUX1 EN1 on PA4 is held high, disabling MUX1.
- ADC1 CS1 on PB0 is held high, deselecting ADC1.
- MUX2 is enabled on PB2 only after each address change and disabled after
  each 16-channel ADC scan.
- Every MAX11633 reset, setup, conversion, EOC wait, and read transaction now
  uses ADC2 CS2/PB1 and EOC2/PB11.
- The error-recovery path also disables both MUX devices, releases both ADC
  chip selects, reinitialises SPI3, and then reinitialises ADC2.
- `ESKIN_STM32.ioc` and `Core/Inc/main.h` now contain the PB1, PB2, and PB11
  definitions, with active-low outputs defaulting high.
- MAX11633 remains configured with setup byte `0x64`, using the external 3.3 V
  reference.

Build result:

```text
SUCCESS
RAM used:    3392 bytes / 128 KiB (2.59%)
Flash used: 16680 bytes / 512 KiB (3.18%)
```

Flash and runtime validation results are recorded below after completion.

Initial FSR2 flash and runtime result:

```text
flash: exit code 0
erased: 12288 bytes
programmed: 11264 bytes
identical: 6144 bytes
hardware reset: exit code 0

COM9: 25 valid frames, sequence 506..530, gaps 0
rate: 24.57 frame/s
raw range: 0..2
```

The initial transport was stable, but the nearly zero global range did not yet
validate the FSR2 analogue response.

## 2026-07-28 - Reverse FSR2 row and column order

The user confirmed that both FSR2 axes run in the opposite direction from
FSR1. The STM32 acquisition was therefore normalised before transmission:

```text
logical MUX index 0  -> physical MUX2 channel 15
logical MUX index 15 -> physical MUX2 channel 0

logical ADC index 0  <- physical ADC2 AIN15
logical ADC index 15 <- physical ADC2 AIN0
```

This is a 180-degree reversal, not an exchange of the two axes. The existing
Teensy frame format and Heatmap orientation remain unchanged. The protected
`FSR_left` baseline was not modified.

Build result:

```text
SUCCESS
RAM used:    3392 bytes / 128 KiB (2.59%)
Flash used: 16768 bytes / 512 KiB (3.20%)
```

Flash and reset result:

```text
pyOCD frequency: 10 kHz
connection mode: under-reset
erased: 12288 bytes (6 sectors)
programmed: 11264 bytes (11 pages)
identical: 6144 bytes (6 pages)
flash exit code: 0
hardware reset exit code: 0
```

Post-flash live validation:

```text
COM9: 25 valid frames
sequence: 206..230
sequence gaps: 0
elapsed: 1.026 s
rate: 24.36 frame/s
raw ADC range: 0..1637
```

The digital link remains stable and FSR2 now produces a non-trivial analogue
range. Direction must be confirmed with a known-corner press, for example
physical R1/C1, because an unlabelled matrix cannot independently prove spatial
orientation.

## 2026-07-28 - Grey cells while displaying FSR2

The grey polygons are the Heatmap's explicit invalid-calibration colour, not
missing polygons or missing USB frame bytes. `live_fsr_heatmap.py` marks a cell
invalid when:

```text
abs(full ADC endpoint - zero ADC endpoint) < 50 counts
```

The normalised value is then `NaN`, and the colormap renders it as RGB
approximately `(0.22, 0.22, 0.22)`.

Both calibration files were inspected:

```text
fsr_calibration.json  (FSR1):
  valid 189, invalid 67
  fully invalid physical rows: R12, R13, R14
  fully invalid physical column: C16

fsr2_calibration.json (FSR2):
  valid 75, invalid 181
  fully invalid physical row: R16
  fully invalid physical columns: C2, C6, C7, C8, C10..C16
```

The grey pattern in the supplied screenshot matches the old FSR1 calibration,
especially the complete R12--R14 horizontal band and C16 edge. The FSR2
Heatmap had therefore been started with its default `fsr_calibration.json`
instead of the separate FSR2 calibration file.

Correct FSR2 launch option:

```text
--calibration-file fsr2_calibration.json
```

FSR2 zero and full endpoints must then be captured again after the new
180-degree firmware mapping. Calibration files captured before that mapping
must not be reused because their cell coordinates no longer refer to the same
physical locations.

## 2026-07-28 - FSR2 axes exchanged after motion-direction test

Observed hardware test:

```text
physical motion: left to right
Heatmap motion:  top to bottom
```

This proves an axis exchange, not merely a reversed direction. Reversing an
index can only change left-to-right into right-to-left; it cannot turn vertical
motion into horizontal motion.

The STM32 FSR2 frame writer was corrected from:

```text
frame[MUX index][ADC index]
```

to:

```text
frame[ADC index][MUX index]
```

Both previously established FSR2 direction reversals remain in place:

```text
MUX logical index = 15 - physical MUX2 channel
ADC logical index = 15 - physical ADC2 channel
```

The resulting transport again has the same semantic layout expected by the
FSR1 Teensy/Heatmap path:

```text
frame[physical column][physical row]
```

Build result:

```text
SUCCESS
RAM used:    3392 bytes / 128 KiB (2.59%)
Flash used: 16768 bytes / 512 KiB (3.20%)
```

Flash/reset result:

```text
10 kHz SWD, under-reset
erased: 2048 bytes
programmed: 2048 bytes
identical: 15360 bytes
flash exit code: 0
hardware reset exit code: 0
```

Post-flash link validation:

```text
COM9 valid frames: 25
sequence: 259..283
gaps: 0
rate: 24.09 frame/s
ADC range: 0..1349
```

The corrected Heatmap was opened as PID 3744 with a fresh coordinate-compatible
calibration file:

```text
fsr2_calibration_v3.json
```

The previous FSR2 calibration files must not be reused because exchanging the
frame axes changes every saved cell coordinate. Final spatial direction now
requires the repeated physical left-to-right slide test. If the Heatmap motion
is horizontal but travels right-to-left, only one direction reversal remains
to be adjusted; the axes must not be exchanged again.

## 2026-07-28 - Correct FSR2 physical left/right direction without GUI mirroring

After the axis exchange, motion was horizontal but its left/right direction
was opposite to the physical motion. This was corrected in the STM32 mapping,
not with the Heatmap `flip-columns` option. Consequently, GUI geometry, R/C
labels, hover coordinates, and CSV coordinates remain unchanged.

Previous horizontal mapping:

```text
ADC2 AIN15 -> logical C1
...
ADC2 AIN0  -> logical C16
```

Corrected horizontal mapping:

```text
ADC2 AIN0  -> logical C1
...
ADC2 AIN15 -> logical C16
```

The vertical MUX2 mapping and the ADC/MUX axis exchange were not changed.

Build result:

```text
SUCCESS
RAM used:    3392 bytes / 128 KiB (2.59%)
Flash used: 16764 bytes / 512 KiB (3.20%)
```

Flash and reset:

```text
10 kHz SWD, under-reset
erased: 12288 bytes
programmed: 11264 bytes
identical: 6144 bytes
flash exit code: 0
hardware reset exit code: 0
```

Post-flash link validation:

```text
COM9 valid frames: 25
sequence: 229..253
gaps: 0
rate: 22.37 frame/s
ADC range: 0..1266
```

A fresh Heatmap was opened as PID 22256 using:

```text
fsr2_calibration_v4.json
```

Earlier FSR2 calibration files must not be reused because the physical
left/right cell correspondence has changed.

## 2026-07-28 - Recurrent `Waiting for FSR frames from COM9`

The Heatmap again opened with no valid frames. Direct diagnostics established:

```text
COM9: Teensy USB serial present (VID:PID 16C0:0483)
COM10: DAPLink present (VID:PID C251:F001)
COM9 open: successful
valid frames during initial 10-second check: 0
```

Therefore this occurrence was not caused by a missing COM port, another
process owning COM9, calibration, or Heatmap colour rendering.

A 10 kHz under-reset STM32 hardware reset returned successfully but did not
immediately restore frames. The stuck serial test process was identified
exactly as PID 27128 and stopped to release COM9.

STM32 live diagnostics were then read from the flashed ELF symbols:

```text
captured PC:            0x080011DA
resolved function:      SPI_WaitFifoStateUntilTimeout()
current/observed stage: HOST_TRANSFER / ADC_READ during retry
last error stage:       HOST_TRANSFER
completed scans:        2399
completed frames:       2181
error count:            218
SPI transfer remaining: 0 TX bytes, 0 RX bytes in the last completed record
```

This localises the recurrent fault to intermittent STM32 SPI3-to-Teensy host
handshake loss. ADC2 acquisition, COM9 enumeration, and the Heatmap parser are
not the root cause. The debugger briefly halted and then continued the core;
the host link subsequently recovered.

Post-recovery direct validation:

```text
COM9 valid frames: 5
sequence: 2760..2764
gaps: 0
ADC range: 0..1782
```

The FSR2 Heatmap was reopened with `fsr2_calibration_v4.json` as PID 3976.
After three seconds the process remained running and responsive.

Likely trigger: STM32-only reset/reflash while Teensy remains running can leave
the SPI slave/master handshake temporarily out of phase. If this repeats,
resetting/power-cycling STM32 and Teensy together is the immediate recovery.
A permanent firmware change should target the HOST_IRQ/SPI3 recovery handshake,
not ADC sampling or GUI calibration.

## 2026-07-28 - Automatic generated-data folder classification

The Python tools were updated so generated measurement files no longer appear
beside the `.py` and `.ino` source files.

New structure:

```text
ESKIN_SPI_USB_BRIDGE/
├── data/
│   ├── README.md
│   ├── calibration/
│   │   ├── fsr1.json
│   │   ├── fsr2.json
│   │   └── archive/
│   ├── raw_frames/
│   │   ├── fsr1/
│   │   └── fsr2/
│   └── row_tests/
├── ESKIN_SPI_USB_BRIDGE.ino
├── live_fsr_heatmap.py
├── read_teensy_fsr.py
└── test_adc_rows.py
```

`live_fsr_heatmap.py` now accepts:

```text
--region fsr1
--region fsr2
```

The selected region automatically controls both defaults:

```text
data/calibration/<region>.json
data/raw_frames/<region>/
```

Explicit `--calibration-file` and `--raw-output-directory` options still
override the automatic paths.

`test_adc_rows.py` now saves its default timestamped JSON output under:

```text
data/row_tests/
```

Its explicit `--output` option remains available and the destination parent
directory is created automatically.

Existing files were preserved and migrated:

```text
three old calibration JSON files -> data/calibration/archive/
two row-test JSON files          -> data/row_tests/
two FSR1 raw-frame CSV files     -> data/raw_frames/fsr1/
one FSR2 raw-frame CSV file      -> data/raw_frames/fsr2/
```

Old calibration files were deliberately archived rather than automatically
assigned to the new canonical `fsr1.json`/`fsr2.json` names because several
were captured before later axis and left/right firmware changes. This prevents
stale coordinate maps from being silently reused.

Verification:

```text
Python syntax: PASS
FSR1 automatic calibration path: PASS
FSR1 automatic raw-frame path: PASS
FSR2 automatic calibration path: PASS
FSR2 automatic raw-frame path: PASS
root-level JSON/CSV count: 0
```

The FSR2 Heatmap was restarted using `--region fsr2 --autoscale` as PID 7480
and remained running and responsive after startup.

## 2026-07-28 - Restore FSR1 and archive the current FSR2 firmware

The active FSR2 STM32 project was preserved as:

```text
firmware/FSR_right/
```

The previously preserved FSR1 project was copied back to the active project:

```text
firmware/FSR_left/      -> preserved FSR1 reference copy
firmware/ESKIN_STM32/   -> active FSR1 project
firmware/FSR_right/     -> preserved FSR2 project
```

The active source was checked for the expected FSR1 functions:

```text
MUX1_SelectRow()
MAX11633_Read16()
FSR_ScanFrame() -> frame[row]
```

It does not contain the FSR2-only `MUX2_SelectRow()` or
`MAX11633_ADC2_Init()` scan path.

The normal in-project build initially failed because Windows could not create
the very long `.su` object paths under the OneDrive project directory. This
was a build-path limitation, not a C source error. The same active source was
therefore rebuilt from the short directory:

```text
D:/study/programming/builds/ESKIN_STM32_FSR1/
```

Build result:

```text
PASS
FLASH used: 16756 bytes / 512 KiB
RAM used:   3392 bytes / 128 KiB
ELF: D:/study/programming/builds/ESKIN_STM32_FSR1/ESKIN_STM32.elf
```

The ELF was programmed through DAPLink with:

```powershell
pyocd flash -t stm32g474cetx -f 10k --connect under-reset `
  "D:\study\programming\builds\ESKIN_STM32_FSR1\ESKIN_STM32.elf"
```

Programming completed successfully with pyOCD exit code 0. The first reset
attempt reported `already open`; no residual pyOCD process was found, and the
second 10 kHz hardware reset completed successfully:

```powershell
pyocd reset -t stm32g474cetx -f 10k --connect under-reset -m hw
```

## 2026-07-28 - COM9 waiting after restoring FSR1

Symptom:

```text
Waiting for FSR frames from COM9...
```

COM9 enumeration was verified:

```text
VID:PID = 16C0:0483
serial  = 14220980
device  = Teensy USB serial
```

The original Teensy bridge emitted only:

```text
# WAIT_IRQ level=0
```

An STM32 diagnostic read showed that ADC1 scanning was not the cause:

```text
fsr_completed_scans  = 87
fsr_completed_frames = 86
fsr_error_count      = 0
fsr_stage            = FSR_STAGE_HOST_TRANSFER
```

This proves that STM32 had completed FSR1 scans and successful SPI3 transfers.

The Teensy bridge contained a USB forwarding defect. It required
`Serial.availableForWrite()` to report at least the complete 515-byte frame
before calling `Serial.write()`. On a USB CDC buffer reporting less than 515
immediately available bytes, every valid frame was silently dropped. The
periodic `WAIT_IRQ` ASCII message could also be inserted between binary frames
and force the PC parser to reacquire alignment.

The bridge was changed to:

```text
1. call Serial.write(spiRx, 515) whenever USB Serial is connected;
2. allow Serial.write() to wait after the STM32 SPI transaction is complete;
3. stop inserting periodic WAIT_IRQ text into the binary frame stream.
```

The modified Teensy 4.1 program compiled successfully and was uploaded through
the Teensy Arduino core 1.62.0.

During the SWD diagnostic, an invalid resume sequence left the STM32 Cortex-M4
in `Lockup`. Both hardware reset and SYSRESETREQ were attempted, but the target
continued to report `Lockup`; the external NRST path did not clear it. COM9
therefore remained silent after the Teensy fix. A full removal of all possible
power/back-power sources is required:

```text
disconnect Teensy USB + DAPLink USB -> wait 5 seconds -> reconnect Teensy only
```

After that physical power cycle, the repaired bridge must be retested for
consecutive 515-byte frames before reopening the Heatmap.

### Resolution after the physical power cycle

The target still entered Cortex-M4 `Lockup` immediately after power-up. Flash
inspection identified the direct cause:

```text
vector table reset address: 0x0800406D
expected Reset_Handler:      0x0800406C
actual Flash at 0x0800406C:  0xFFFFFFFF
fault:                       UsageFault UNDEFINSTR -> HardFault Lockup
```

The earlier 10 kHz programming commands were being interrupted before the
complete image was written. Flash pages were populated only up to approximately
`0x080027FF`, while `Reset_Handler` was located later at `0x0800406C`. The
partial command output had shown erasing/programming progress, but it had not
reached a completed programming summary.

The FSR1 ELF was programmed again with a full chip erase at 100 kHz:

```powershell
pyocd load -t stm32g474cetx -f 100k -M under-reset -e chip --format elf `
  "D:\study\programming\builds\ESKIN_STM32_FSR1\ESKIN_STM32.elf"
```

Verified result:

```text
erased chip
programmed 17408 bytes / 17 pages
Reset_Handler bytes at 0x0800406C match the ELF
Cortex-M4 state: Running
```

COM9 verification over five seconds:

```text
received bytes:          64375
consecutive frame pairs: 123
first sequences:         981, 982, 983, 984, 985
effective rate:          approximately 25 fps
```

The FSR1 Heatmap was then started successfully with:

```powershell
python -u live_fsr_heatmap.py --port COM9 --region fsr1 --autoscale
```

The visible Python process was PID 27928 and its window was responsive.

## 2026-07-28 - FSR1 fixed-MUX individual ADC channel test

Purpose:

```text
fix one MUX1 position
read AIN0 100 times
read AIN1 100 times
...
read AIN15 100 times
send all unmodified 12-bit ADC codes to the PC
```

The working FSR1 firmware was preserved unchanged. A separate STM32 test
project was created:

```text
firmware/FSR1_ADC_CHANNEL_TEST/
```

The selected MUX position is controlled by:

```c
#define TEST_MUX_CHANNEL 0U
```

The initial test therefore fixes MUX1 at physical channel 0 / logical R1.

Each MAX11633 input is requested separately with conversion scan mode `11`
(one conversion from the selected AIN). Samples are stored channel-major:

```text
samples[AIN channel][repeat index]
16 channels x 100 repeats = 1600 raw samples
```

A separate Teensy test bridge and PC reader were created:

```text
teensy41/FSR1_ADC_CHANNEL_TEST_BRIDGE/
|-- FSR1_ADC_CHANNEL_TEST_BRIDGE.ino
`-- read_adc_channel_test.py
```

The dedicated binary test frame is 3206 bytes:

```text
byte 0       0xA6 test-frame magic
bytes 1-2   uint16 sequence, little-endian
byte 3       fixed MUX1 channel
byte 4       AIN channel count = 16
byte 5       repeat count = 100
bytes 6-3205 1600 uint16 raw ADC codes, little-endian, channel-major
```

No calibration, averaging, filtering, pressure conversion, normalisation, or
coordinate transformation is applied.

The STM32, Teensy, and Python sources compiled successfully. The Teensy test
bridge was uploaded first. Several pyOCD attempts stalled because the DAPLink
CMSIS-DAP HID endpoint remained occupied; all confirmed residual processes were
terminated. After physically reconnecting DAPLink, the STM32 test firmware was
successfully programmed at 100 kHz:

```text
chip erase complete
programmed 17408 bytes / 17 pages
```

The first STM32 test revision timed out at `FSR_STAGE_ADC_EOC_WAIT`. A
single-channel MAX11633 conversion completes in only a few microseconds, so its
EOC-low interval could finish before the blocking HAL SPI transmit returned and
GPIO polling began. The test firmware was corrected to wait a conservative
1 ms after every single-channel command before reading its two result bytes.
This changes test duration only and does not change the raw ADC result.

The corrected build and 100 kHz programming both passed. One complete test
frame was captured:

```text
sequence:       2
fixed MUX1:     channel 0
data rows:      100
CSV columns:    repeat + AIN0..AIN15
raw ADC values: 1600
```

Per-channel summary:

```text
AIN00 mean  181.68, range 178..186
AIN01 mean 1660.80, range 1658..1662
AIN02 mean 1491.92, range 1490..1494
AIN03 mean 1470.03, range 1465..1474
AIN04 mean 1586.98, range 1584..1592
AIN05 mean 1365.61, range 1360..1372
AIN06 mean 1580.23, range 1574..1586
AIN07 mean 1650.71, range 1648..1656
AIN08 mean 1532.10, range 1527..1537
AIN09 mean 1282.80, range 1279..1286
AIN10 mean    1.39, range 0..36
AIN11 mean   34.34, range 0..122
AIN12 mean    1.00, range 0..2
AIN13 mean    1.04, range 0..4
AIN14 mean 1315.66, range 1307..1321
AIN15 mean  283.85, range 280..313
```

AIN10-AIN13 are the clear low-response group at this fixed MUX position.
AIN0 and AIN15 have measurable signals but are substantially below
AIN1-AIN9 and AIN14.

The complete raw data was saved to:

```text
teensy41/FSR1_ADC_CHANNEL_TEST_BRIDGE/data/channel_tests/fsr1/
fsr1_mux00_adc_test_seq00002_20260728T143847_893162Z.csv
```

The STM32 and Teensy are left running the dedicated channel-test firmware.
The normal 515-byte Heatmap protocol is not active until both standard
firmwares are restored.

## 2026-07-28 - Live AIN10 monitor across all MUX1 positions

The fixed-MUX 1600-sample test was replaced by a direct serial-monitor test.
The purpose is to isolate AIN10 while checking every MUX1 input without saving
another CSV file.

Current STM32 behaviour:

```text
select MUX1 channel 0
wait 2 ms for settling
read MAX11633 AIN10 once
send one compact frame to Teensy
wait 1 second
repeat for MUX1 channels 1..15
wrap from channel 15 back to channel 0
```

The STM32 test source remains in:

```text
firmware/FSR1_ADC_CHANNEL_TEST/Core/Src/main.c
```

The matching Teensy source remains in:

```text
firmware/teensy41/FSR1_ADC_CHANNEL_TEST_BRIDGE/
FSR1_ADC_CHANNEL_TEST_BRIDGE.ino
```

The new eight-byte SPI frame is:

```text
byte 0      0xA7 live-frame magic
bytes 1-2  uint16 sequence, little-endian
byte 3      MUX1 channel, 0..15
byte 4      MAX11633 AIN channel, fixed at 10
bytes 5-6  raw 12-bit ADC code, little-endian
byte 7      XOR checksum of bytes 0..6
```

The Teensy validates the magic byte, MUX range, AIN number, and checksum, then
prints a human-readable line directly through USB Serial:

```text
seq=42  MUX=9 (position 10/16)  AIN10  ADC=1234/4095  V=0.9944
```

The displayed voltage is an estimate calculated as:

```text
voltage = raw ADC code * 3.3 / 4095
```

No calibration, filtering, averaging, pressure conversion, file logging, or
coordinate transformation is applied. The Python CSV reader is not required
for this test. Open the Arduino IDE Serial Monitor at 2,000,000 baud after both
matching firmwares have been programmed.

### Build, programming, and live verification

Both modified sources compiled successfully. The STM32 build used a short path
because the OneDrive project path exceeded the ARM compiler's object-file path
limit:

```text
D:\study\programming\builds\FSR1_AIN10_LIVE\ESKIN_STM32.elf
FLASH used: 16676 bytes
RAM used:    1864 bytes
```

The matching Teensy build was generated in:

```text
D:\study\programming\builds\TEENSY41_AIN10_LIVE
```

Teensy 4.1 upload completed successfully. The STM32 was then programmed through
DAPLink at 100 kHz with connect-under-reset and a chip erase:

```text
erased chip
programmed 17408 bytes / 17 pages
```

COM9 was monitored directly for 20 seconds at 2,000,000 baud. Sequence numbers
were continuous, MUX1 advanced through channels 4..15, wrapped to 0, and
continued through channel 8 with approximately one line per second. Example:

```text
seq=62  MUX=14 (position 15/16)  AIN10  ADC=3/4095  V=0.0024
seq=63  MUX=15 (position 16/16)  AIN10  ADC=3/4095  V=0.0024
seq=64  MUX=0  (position 1/16)   AIN10  ADC=3/4095  V=0.0024
seq=65  MUX=1  (position 2/16)   AIN10  ADC=3/4095  V=0.0024
```

All 16 MUX1 positions returned approximately 1..4 ADC counts during this
unloaded observation. The `errors=8` counter was already present after the
firmware/upload startup transition; it remained constant throughout the
captured valid sequence and no new frame errors occurred during verification.

## 2026-07-28 - Restored normal FSR1 scanning and Heatmap

After the live AIN10 diagnostic, both processors were restored to the normal
FSR1 acquisition chain.

The Teensy 4.1 was programmed with:

```text
firmware/teensy41/ESKIN_SPI_USB_BRIDGE/ESKIN_SPI_USB_BRIDGE.ino
```

This restores the validated `0xA5`, 515-byte binary bridge protocol. Arduino's
`teensy-monitor` process initially held COM9 and prevented automatic reboot; it
was stopped, after which the normal bridge upload completed.

The STM32 normal FSR1 firmware was rebuilt from the preserved backup:

```text
firmware/FSR_left/
```

Short-path build output:

```text
D:\study\programming\builds\ESKIN_STM32_FSR1_RESTORE\ESKIN_STM32.elf
FLASH used: 16756 bytes
RAM used:    3392 bytes
```

DAPLink programming at 100 kHz with connect-under-reset and chip erase
completed successfully:

```text
erased chip
programmed 17408 bytes / 17 pages
```

Before opening the GUI, ten COM9 frames were validated:

```text
frame sequences: 1422..1431
sequence gaps:   0
sample range:    0..2453
frame rate:      25.27 fps
```

The FSR1 Heatmap was then started directly with:

```powershell
python -u live_fsr_heatmap.py --port COM9 --region fsr1 --autoscale
```

### Heatmap returned to `Waiting for FSR frames from COM9`

The waiting Heatmap process was closed and COM9 was tested directly with the
fixed-frame reader. Five valid frames were received immediately:

```text
sequences: 1775..1779
gaps:      0
samples:   0..2099
rate:      23.76 fps
```

This ruled out STM32 acquisition failure, Teensy firmware mismatch, and a
broken SPI/USB link for this occurrence. The waiting state was caused by the
GUI process failing to obtain its required consecutive-frame lock during
startup. Releasing COM9, validating the binary stream, and restarting the
Heatmap restored operation.

### Permanent GUI waiting-state fix

The Heatmap later remained on `Waiting for FSR frames from COM9` even after a
restart. Two independent checks proved that acquisition was still healthy:

```text
standalone fixed-frame reader: valid consecutive frames
Heatmap SerialReader thread:   frame 2387, 16 x 16, gaps 0
```

The remaining fault was therefore the GUI refresh mechanism. The script used
`figure.canvas.new_timer()` and started that backend timer before
`matplotlib.pyplot.show()`. On the active Windows Matplotlib backend, the timer
could fail to begin dispatching callbacks after the window event loop started.
The serial thread continued receiving frames, but the GUI never removed its
initial waiting message.

`live_fsr_heatmap.py` was changed to use `matplotlib.animation.FuncAnimation`,
whose event source starts after the canvas is drawn. A one-time terminal
confirmation was also added:

```text
Heatmap synchronized on COM9: frame 4120, 16x16
```

Python syntax validation passed and the corrected Heatmap remained running
after synchronizing. No STM32 or Teensy firmware change was required.

## 2026-07-28 - R13 and R14 remain high resistance under full pressure

Physical resistance measurements found that rows R13 and R14 remained much
higher resistance than the other rows even when the affected FSR area was
fully compressed.

For the current software convention, one-based GUI rows map to zero-based ADC
inputs:

```text
R13 -> ADC1 AIN12
R14 -> ADC1 AIN13
```

In the FSR-array 2.4 PCB, these are adjacent `/ain12` and `/ain13` nets on FPC
tail pads 13 and 14. In the mainboard 2.2 PCB, the corresponding adjacent nets
connect the FPC to `COL_ADC1-AIN12` and `COL_ADC1-AIN13`. Both nets have routed
objects in the KiCad PCB files, so the design files do not indicate a simple
completely missing net. This does not rule out a fabrication crack, connector
fault, assembly fault, or mismatch between the design revision and the
fabricated hardware.

Because two complete adjacent rows are affected, the leading fault classes are:

```text
1. adjacent FPC contacts 13/14 not making reliable contact
2. a cracked or delaminated flex/tail region affecting both row conductors
3. common mechanical stack-up preventing the R13/R14 active region from closing
4. printed conductive ink/electrode discontinuity across the two rows
5. mainboard AIN12/AIN13 routing, soldering, or ADC input damage
6. an invalid in-circuit resistance measurement through powered/connected ICs
```

The decisive isolation test is to power everything off, disconnect both FPC
tails from the mainboard, and compare R12/R13/R14/R15 directly on the bare FSR
tail using the same known-good column and a repeatable pressure fixture. If
R13/R14 remain high on the isolated sensor, firmware, Teensy, SPI, USB, the
MAX11633, and mainboard circuitry are excluded.

## 2026-07-28 - Switched normal acquisition from FSR1 to FSR2

The FSR1 Heatmap was stopped and the preserved `FSR_right` STM32 project was
checked before programming. It uses:

```text
MUX2 enable and shared MUX address lines
ADC2 chip select and ADC2 EOC
MAX11633 ADC2 channels 0..15
standard 0xA5 / 515-byte host frame
```

The FSR2 firmware was rebuilt at:

```text
D:\study\programming\builds\ESKIN_STM32_FSR2_NORMAL\ESKIN_STM32.elf
FLASH used: 16848 bytes
RAM used:    3392 bytes
```

DAPLink programming at 100 kHz with connect-under-reset and a chip erase
completed successfully:

```text
erased chip
programmed 17408 bytes / 17 pages
```

The Teensy remained on the standard `ESKIN_SPI_USB_BRIDGE` firmware because the
FSR1 and FSR2 STM32 images intentionally share the same binary transport
protocol.

Ten FSR2 frames were validated before starting the GUI:

```text
sequences: 526..535
gaps:      0
samples:   0..1786
rate:      26.37 fps
```

The corrected Heatmap synchronized successfully in FSR2 orientation:

```text
Heatmap synchronized on COM9: frame 956, 16x16
```

Launch command:

```powershell
python -u live_fsr_heatmap.py --port COM9 --region fsr2 --autoscale
```

### FSR2 raw-frame line-fault analysis

The saved FSR2 frame
`fsr_raw_frame_07387_20260728T203307_577881Z.csv` was analysed using a raw-code
threshold of 100 counts. It contains 146/256 low cells (57.0%), compared with
48/256 (18.8%) in the selected FSR1 comparison frame. This comparison is valid
as a sensor-quality comparison only if both frames were captured under the same
mechanical load.

The FSR2 failures are structured as complete lines:

```text
complete low rows:    R3, R4, R5, R7, R12, R13
complete low columns: C1, C3, C8, C13, C15
```

With the saved-CSV physical convention:

```text
rows    -> logical MUX2 selections
columns -> ADC2 inputs
```

Therefore the implicated shared channels are:

```text
MUX2 logical rows: R3, R4, R5, R7, R12, R13
MUX2 physical inputs (reverse selection): I13, I12, I11, I9, I4, I3
ADC2: AIN0, AIN2, AIN7, AIN12, AIN14
```

This mapping follows the actual two-stage storage path:

```text
STM32: frame[adc_index][mux_index] = adc2_samples[adc_index]
CSV:   physical_matrix = matrix.T
```

Consequently, a CSV column that remains low for every row identifies an ADC2
input path, not one MUX2 input. The earlier row/column interpretation in this
section was reversed and has been corrected here.

The union of six failed rows and five failed columns explains 146 apparent bad
cells:

```text
6 x 16 + 5 x (16 - 6) = 146
```

This pattern does not support 146 independent FSR-cell failures. It points to
multiple common line paths on both FSR2 FPC interfaces, their connectors, flex
traces, or the corresponding ADC2/MUX2 board paths. A single stuck MUX address
bit is also unlikely because the affected MUX indices do not follow one binary
address-bit pattern.

### FSR2 C5 and C12 fixed-low investigation

A later raw table showed C5 and C12 remaining at approximately 23--28 ADC
counts while the other active columns were approximately 1450--1600. Under the
corrected saved-CSV mapping:

```text
C5  -> adc_index 4  -> ADC2 AIN4
C12 -> adc_index 11 -> ADC2 AIN11
```

The two values remain low across every active MUX2 row. This makes the MUX2
address logic and individual FSR intersections unlikely causes. The first
inspection targets are the two ADC-side signal paths: FSR/FPC pin mapping,
connector contacts, PCB continuity and solder joints up to ADC2 AIN4/AIN11,
possible loading or clamping to ground, and finally the two ADC input channels.

The raw codes correspond to only about 19--23 mV for a 3.3 V, 12-bit scale.
Because these are uncalibrated raw values, the Heatmap calibration cannot create
this fault. If the user-confirmed physical FSR columns work, the physical names
being tested may not be the same lines as CSV C5/C12; a one-line-at-a-time
stimulus is required to establish the connector-to-CSV permutation.

### COM9 `PermissionError(13)` while opening another FSR2 Heatmap

A second Heatmap instance attempted to open COM9 while the existing FSR2
Heatmap still owned the serial port. Windows serial ports permit only one
process to open the port, so the new instance reported:

```text
Serial error on COM9: could not open port 'COM9': PermissionError(13)
```

All old `live_fsr_heatmap.py` processes and Arduino's `teensy-monitor` process
were closed. One FSR2 Heatmap was then started and synchronized normally:

```text
Heatmap synchronized on COM9: frame 1799, 16x16
```

This error indicates port ownership, not an STM32, ADC2, MUX2, SPI, or sensor
failure.

### New horizontal FSR: user-reported R3, R6, and R9 at zero

After replacing the horizontal FSR, the user reported that complete logical
rows R3, R6, and R9 read zero. No new matching raw CSV had yet been saved, so
this entry records the live observation rather than an independently parsed
capture.

In the current FSR2 firmware the logical MUX order is reversed:

```text
logical row   mux_index   selected physical MUX2 input
R3            2           I13
R6            5           I10
R9            8           I7
```

The selected input numbers are 13 (`1101`), 10 (`1010`), and 7 (`0111`).
They do not share one fixed binary address bit. A single stuck MUX address line
would create repeated or missing channel groups, rather than only these three
equally spaced logical rows.

The previous saved FSR2 frame from 2026-07-28 22:18 showed complete low rows
R3, R4, R5, and R13. Because replacement changed most of the failed row
positions, a fixed firmware indexing fault is unlikely. Current priority is:

1. horizontal-FSR tail alignment and connector contact pressure;
2. continuity/resistance of the replacement FSR conductors associated with the
   three observed rows;
3. connector-to-MUX2 continuity for I13, I10, and I7;
4. MUX2 output/input damage only if the same physical MUX paths fail under a
   fixed-channel electrical test.

R3 is the only row repeated between the old saved frame and the new user
observation, so its board-side connector/MUX2 path deserves separate inspection.
R6 and R9 are more likely specific to the replacement FSR or its seating unless
a fixed-channel test proves otherwise.

### FSR2 reflash attempt after replacing the horizontal FSR

The current FSR2 ELF was selected for programming:

```text
FSR_right/build/Debug/ESKIN_STM32.elf
target: STM32G474CETx
probe:  jixin.pro CMSIS-DAP_LU, UID LU_2022_8888
mode:   under-reset
```

Three programming attempts were made at progressively lower SWD clocks:

```text
10 kHz -> DebugPortSetup: SWD/JTAG communication failure (No ACK)
 1 kHz -> DebugPortSetup: SWD/JTAG communication failure (No ACK)
100 Hz -> DebugPortSetup: SWD/JTAG communication failure (No ACK)
```

`pyocd list --probes --no-config -v` completed normally and detected the
DAPLink probe, but its target remained `n/a`. Therefore the USB probe and pyOCD
installation are available, while the STM32 is not currently returning an SWD
acknowledgement. Flash was not erased or rewritten during these failed connect
attempts.

Before retrying, verify target VDD, a shared DAPLink/board ground, SWDIO,
SWCLK, and DAPLink nRESET to the STM32 NRST net. NRST should normally be high
and must be pulled low by the probe during an under-reset connection. Since the
failure persists at 100 Hz, further clock reduction is not the next action.

### FSR2 full-matrix irregular jumping after the failed SWD connection

The live Heatmap subsequently showed a rapidly changing checkerboard-like
matrix with raw codes spanning approximately 0--3845. The transport status was
still stable at about 25.1 frame/s with `gaps 0`.

This status proves that complete 515-byte STM32-to-Teensy frames are arriving in
sequence. It does **not** validate the separate MAX11633 ADC2-to-STM32 SPI
transaction contained inside each frame. The Heatmap displays the received
12-bit samples directly and does not generate random per-cell values.

The leading causes, in priority order, are:

1. unstable mainboard/ADC2 VDD, ground, or external 3.3 V reference;
2. intermittent ADC2 CS2, EOC2, SCK, MISO, or ground connection, causing
   corrupted or floating 12-bit conversion words;
3. floating/high-impedance ADC inputs due to a poorly seated replacement FSR
   tail, allowing sample-and-hold carryover and environmental noise;
4. unstable mechanical contact within the replacement FSR stack;
5. software frame alignment, which is unlikely because the frame sequence has
   no gaps and the parser remains synchronized.

The simultaneous inability to obtain any SWD acknowledgement at 10 kHz, 1 kHz,
or 100 Hz increases the priority of target power and shared-ground inspection.
The decisive isolation test is to leave only the known power arrangement,
select one MUX2 channel for one second, and repeatedly sample a known ADC input:
a grounded input must remain near code 0 and a known reference input must remain
stable. Stable fixed inputs implicate the FSR/FPC; unstable fixed inputs
implicate ADC2 power/reference/SPI rather than the sensor.

### Heatmap waits after every FSR reinsertion

The repeated trigger is reinserting the FSR/FPC while the Teensy-powered
mainboard remains energised. During insertion, connector contacts do not mate
simultaneously. Power, ground, MUX, and ADC inputs can be briefly shorted,
floated, or exposed to ESD. This can brown out or reset STM32/ADC2 while the
Teensy continues running. The two ends of the HOST_IRQ/SPI3 handshake then
start in different states, so COM9 remains present but no valid `0xA5`
515-byte frames reach the Heatmap.

The mandatory reinsertion procedure is:

```text
1. close Heatmap and all COM9 serial monitors;
2. unplug Teensy USB and DAPLink USB;
3. wait at least five seconds for the mainboard rails to discharge;
4. insert and lock both FPC tails while unpowered;
5. reconnect Teensy first and wait about two seconds;
6. if debugging is required, reconnect DAPLink afterward with its 3.3 V supply
   wire left disconnected;
7. launch exactly one Heatmap process.
```

If the Heatmap still waits, remove both USB connections again and repeat the
full power cycle. Restarting only the GUI cannot repair a stopped STM32 or an
out-of-phase STM32/Teensy SPI transaction. Resetting only STM32 can also
reproduce the mismatch; if DAPLink is used, Teensy must already be running
before the STM32 hardware reset.

The durable hardware-assisted solution is to connect a spare Teensy GPIO to
STM32 NRST through an appropriate open-drain/reset interface and let the Teensy
perform a coordinated STM32 reset at bridge startup or after repeated IRQ
timeouts. Until that reset connection is implemented, an unpowered FPC
reinsertion followed by a complete power cycle is the reliable procedure.

### Cutting the unused tail from the two-sided FSR FPC

The FSR FPC artwork contains two complete overlapping copper networks: one
network and tail on each copper side. The unused tail is therefore not merely
an unconnected plastic tab. Cutting it can affect the assembled array even
when continuity of every retained-side trace appears correct.

Important mechanisms are:

1. Scissors or side cutters plastically smear copper across the thin polyimide
   cut edge. Copper from the two sides, or adjacent fingers on the removed
   network, can become bridged.
2. The cut exposes copper ends. Conductive debris, carbon/pressure-sensitive
   film, adhesive residue, moisture, or a metal support can create leakage from
   the retained network to the nominally unused network.
3. Cutting and bending at the narrow Y-junction can delaminate copper or create
   microcracks in the retained fan-out. A continuity beeper can still pass
   while resistance changes under bending or pressure.
4. The remaining unconnected copper grid becomes a floating electrode across
   the complete sensing area. It can retain charge and capacitively couple MUX
   switching edges into the high-impedance ADC network, producing ghost cells
   and irregular jumping.
5. Removing one tail changes local thickness, stiffness, and strain relief.
   The two FSR sheets can then sit with nonuniform spacing or preload, producing
   apparently electrical row/column faults.

Required unpowered checks are stronger than a simple continuity test:

```text
retained pad -> its electrode: low and stable resistance while gently flexing
retained pad -> every other retained pad: open/high resistance
retained pad -> every cut-edge/opposite-side conductor: open, preferably > MOhm
cut unused conductors -> each other: no unintended bridges
```

Inspect the cut edge under magnification for copper burrs, folded copper,
delamination, and conductive debris. Clean it and fully isolate it with a thin
polyimide/Kapton cover after confirming no shorts.

For a future revision, use separate one-axis FPC parts or provide a declared
copper-free cut neck. Keep all copper back from the mechanical cut line by the
FPC manufacturer's required fabrication clearance, have the board house form
the outline, and avoid manually cutting through a copper-bearing flex tail.

### Requested switch back to the FSR1 firmware

The protected `FSR_left` source was selected. Its copied `build/Debug` cache
still referenced the original `ESKIN_STM32` source directory, so it was not
trusted. A clean build was first attempted inside the long OneDrive path, but
Windows' object-path limit prevented GCC from creating HAL `.su` files.

A new short build directory was therefore used:

```text
source: D:/.../firmware/FSR_left
build:  D:/study/programming/builds/ESKIN_FSR1
```

The clean build completed successfully:

```text
firmware paths: MUX1 + ADC1
RAM:            3392 bytes / 128 KiB (2.59%)
Flash:         16756 bytes / 512 KiB (3.20%)
ELF:            D:/study/programming/builds/ESKIN_FSR1/ESKIN_STM32.elf
SHA-256:        A577B57B0C8C9FE11EE16137958FEC9DD76875800CA0D47C3DA65E5D4418A01F
```

DAPLink `LU_2022_8888` was visible over USB, but two programming attempts
failed before Flash erase:

```text
10 kHz, under-reset -> DebugPortSetup SWD No ACK
 1 kHz, under-reset -> DebugPortSetup SWD No ACK
```

FSR1 has therefore been built but has **not yet been programmed**. Reconnect
DAPLink GND, SWDIO, SWCLK, and nRESET to the powered mainboard, then rerun the
10 kHz under-reset flash command. DAPLink 3.3 V must not be used as a second
power source while the Teensy supplies the mainboard.

After the user reported that SWD had been reconnected, three further target
connection attempts were made:

```text
10 kHz, under-reset -> SWD No ACK
100 kHz, under-reset -> SWD No ACK
100 kHz, attach      -> SWD No ACK
```

The `attach` attempt does not depend on the external NRST connection. Its
failure proves that NRST alone is not the blocker. The mainboard must be
powered from the Teensy during programming; with the DAPLink 3.3 V output left
disconnected, Teensy USB and the Teensy-to-mainboard 3.3 V/GND connection must
remain present. Next checks are target VDD at 3.2--3.3 V, shared ground, and
continuity/correct ordering of DAPLink SWDIO to PA13 and SWCLK to PA14.
No Flash erase occurred in any of these attempts, so the previously programmed
FSR2 image remains intact.

Root cause confirmed by the user: the SWD wires had been inserted incorrectly.
This explains the repeated `DebugPortSetup: No ACK` results in both
`under-reset` and `attach` modes. Those failures were not caused by the FSR1
ELF, pyOCD, the CMSIS-Pack, target clock frequency, or STM32 option bytes.
The wiring was corrected before the next programming attempt.

After correcting the wiring, the FSR1 image was programmed successfully at
10 kHz in under-reset mode:

```text
DP IDR:          0x2BA01477
core:            Cortex-M4 r0p1
erase:           12288 bytes / 6 sectors
programmed:      11264 bytes / 11 pages
identical:        6144 bytes / 6 pages
pyOCD exit code: 0
image SHA-256:   A577B57B0C8C9FE11EE16137958FEC9DD76875800CA0D47C3DA65E5D4418A01F
```

The STM32 is now running the `FSR_left` MUX1/ADC1 firmware. A separate
five-frame COM9 verification reader could not open the port because another
process already owned COM9 (`PermissionError 13`). This is a serial-port
ownership condition, not a programming failure; the complete pyOCD exit code 0
confirms that erase and programming finished.

## 2026-07-29 - FSR1 calibration embedded into the STM32 image

The requested calibration architecture was implemented as a two-stage flashing
workflow. Calibration cannot be measured automatically by the programmer
because it cannot know when the sensor is unloaded or under the intended full
pressure. The raw firmware is therefore used to acquire both physical states,
then a helper embeds the resulting 256-cell endpoint maps into the final STM32
image.

New and modified files:

```text
FSR_left/Core/Inc/fsr_mcu_calibration.h
FSR_left/Core/Src/main.c
teensy41/ESKIN_SPI_USB_BRIDGE/build_flash_normalized_fsr1.py
teensy41/ESKIN_SPI_USB_BRIDGE/live_fsr_heatmap.py
```

For every cell, the STM32 final image calculates:

```text
increasing response: (raw - zero) * 4095 / (full - zero)
decreasing response: (zero - raw) * 4095 / (zero - full)
```

The result is rounded and clamped to `0..4095`. A 12-bit value has 4096
possible codes but its maximum numeric value is 4095. Cells whose absolute
`full-zero` span is below 50 ADC counts transmit zero, preventing division by a
noise-only calibration span.

The generated header currently has:

```text
FSR_MCU_CALIBRATION_VALID = 0
```

so the source continues to produce raw MAX11633 values until a complete
calibration is generated. The current `data/calibration/fsr1.json`, updated at
2026-07-28 23:35:20 UTC, contains `zero` but `full` is null. The generation
tool correctly refuses to produce a final calibration image in this state.

Required procedure:

```text
1. Run the raw FSR1 firmware.
2. Leave all cells unloaded and click Calibrate zero.
3. Apply the intended full pressure uniformly and click Calibrate full.
4. Release the pressure and close Heatmap so COM9 is free.
5. Keep the DAPLink SWD connection and stable Teensy mainboard power.
6. Run:

   python build_flash_normalized_fsr1.py --flash

7. After programming, launch:

   python live_fsr_heatmap.py --port COM9 --region fsr1 --mcu-normalized
```

`build_flash_normalized_fsr1.py` validates JSON dimensions and raw values,
generates both C endpoint arrays, reports the calibration-span range and invalid
cell count, uses the short Windows build directory
`D:/study/programming/builds/ESKIN_FSR1_NORMALIZED`, builds the Release image,
and flashes it at 10 kHz under reset. `--require-all-valid` may be added to
refuse programming if even one cell has a span below the threshold.

The Heatmap `--mcu-normalized` mode disables PC-side zero/full calibration,
uses a fixed `0..4095` scale labelled as pressure, and shows normalized
percentages. This prevents the embedded calibration from being applied a
second time.

Validation completed:

```text
Python AST syntax:                 passed
missing-full calibration refusal: passed
raw/fallback STM32 build:          passed, Flash 16880 bytes
enabled-normalization STM32 build: passed, Flash 18072 bytes
```

No normalized firmware was flashed yet because the full-pressure endpoint has
not been captured.

### False pressure after programming the first MCU-normalized FSR1 image

The user completed both endpoints, generated the header, programmed the
normalized image, and launched the Heatmap with `--mcu-normalized`. With no
intentional touch, the displayed frame still contained many nonzero cells and
reported:

```text
MCU range: 0..3302
peak:      80.6% at displayed R7, C13
```

The embedded header SHA-256 matches the current `fsr1.json`, so this is not a
stale calibration file or PC-side double normalization.

Calibration audit:

```text
span range:       3..1858.5 ADC counts
span median:      1544 ADC counts
|span| < 50:      2 cells
|span| < 100:     6 cells
|span| < 200:    11 cells
|span| < 500:    31 cells
negative spans:   6 cells
positive spans: 250 cells
```

Two screenshot cells demonstrate the problem:

```text
displayed R7,C13 -> raw matrix [12][6]
zero=1920, full=1858, span=-62
MCU code 3302 implies current raw about 1870
only a 50-count downward drift was magnified to 80.6%

displayed R5,C8 -> raw matrix [7][4]
zero=807, full=1858, span=+1051
MCU code 865 implies current raw about 1029
the unloaded baseline moved +222 counts, producing 21.1%
```

The first implementation deliberately supported both increasing and decreasing
cell responses and accepted `abs(full-zero) >= 50`. That is too permissive for
this FSR1 hardware, where 250/256 cells show the expected increasing response.
Small inverted spans are calibration noise/nonuniform loading, not valid
reverse-response cells. The firmware also has no zero deadband, so every
post-calibration baseline drift is converted directly into pressure.

Recommended correction before the next normalized image:

```text
accept only full > zero;
raise the minimum span from 50 to at least 200 counts;
add a per-cell unloaded deadband before scaling;
repeat zero after the assembled FSR has mechanically settled;
apply full pressure uniformly and without moving/reseating the stack afterward.
```

The zero deadband should be subtracted before normalization and the remaining
range rescaled to 0..4095. A longer unloaded capture or a stored zero-noise
envelope is preferable to a fixed global deadband.

### Calibration capture extended to approximately 10 seconds

The default Heatmap calibration length was increased:

```text
previous:  40 frames, approximately 1.6 seconds at 25 fps
current:  250 frames, approximately 10 seconds at 25 fps
```

This applies to both `Calibrate zero` and `Calibrate full`. Each cell still
uses the median of all captured frames. The longer window reduces sensitivity
to brief ADC noise and short mechanical transients, but the sensor must remain
completely unloaded or uniformly fully loaded for the entire progress count
from `0/250` to `250/250`.

The value can still be overridden explicitly, for example:

```powershell
python live_fsr_heatmap.py --port COM9 --region fsr1 --calibration-frames 500
```

At 25 fps, 500 frames is approximately 20 seconds. Python syntax validation
and the updated command-line help both passed.

### MCU calibration pipeline now enforces the 10-second captures

The MCU does not measure calibration during SWD programming; it embeds the
zero/full endpoint maps captured immediately before the final build. To ensure
that the MCU image cannot be generated from an older short capture, the
calibration JSON now records:

```json
"zero_frames": 250,
"full_frames": 250
```

`build_flash_normalized_fsr1.py` now requires both fields to be at least 250
frames by default. Old JSON files without frame-count metadata, or captures
shorter than 250 frames, are rejected before the C header is generated:

```text
Calibration generation failed: zero calibration was not captured with the
required 250 frames; recapture Calibrate zero
```

The generated STM32 header also records the zero and full capture counts in
its metadata comment. This makes the 10-second requirement auditable in the
final firmware source.

The threshold can be raised explicitly:

```powershell
python build_flash_normalized_fsr1.py `
  --minimum-calibration-frames 500 `
  --flash
```

It should not normally be lowered. Python syntax validation and the expected
rejection of the previous metadata-free calibration both passed.

The existing generated header from the earlier short calibration was also set
back to `FSR_MCU_CALIBRATION_VALID = 0`. Consequently, an accidental manual
build now falls back to raw ADC output instead of reusing the stale endpoint
tables. Only a successful new 250-frame zero/full generation run will enable
MCU normalization again.

### Peak-cell 3 x 3 neighbourhood test Heatmap

A separate diagnostic viewer was added:

```text
teensy41/ESKIN_SPI_USB_BRIDGE/test_peak_neighborhood.py
```

It leaves the STM32 firmware, Teensy bridge, 515-byte protocol, and main
Heatmap unchanged. For every received 16 x 16 frame it:

1. transposes the transport `[MUX column][ADC row]` matrix into the physical
   `[R][C]` convention;
2. finds the global maximum of that frame;
3. retains the complete 16 x 16 physical grid and all R1--R16/C1--C16 labels;
4. colours only the maximum and its eight immediate physical neighbours,
   leaving every other cell black;
5. marks the maximum at its true 16 x 16 location with a cyan border;
6. writes values only inside the visible neighbourhood;
7. naturally shows fewer than eight neighbours when the maximum lies on an
   array edge;
8. reports sequence, frame rate, gaps, and peak coordinates.

Raw-ADC launch:

```powershell
python -u test_peak_neighborhood.py --port COM9
```

MCU-normalized launch:

```powershell
python -u test_peak_neighborhood.py --port COM9 --mcu-normalized
```

Only one program may own COM9, so close the main Heatmap and Arduino serial
monitor before launching this diagnostic. Python syntax, a centre-peak 3 x 3
case, an edge-peak four-valid-neighbour case, and command-line help were
validated successfully.
## Firmware workspace reorganisation (2026-07-29)

The firmware root was reorganised by function without deleting any project:

- `FSR_left` moved to `stm32/applications/fsr1`.
- `FSR_right` moved to `stm32/applications/fsr2`.
- `FSR1_ADC_CHANNEL_TEST` moved to `stm32/tests/fsr1_adc_channel`.
- the older generic `ESKIN_STM32` workspace moved to
  `stm32/archive/legacy_workspace`.
- `teensy41/ESKIN_SPI_USB_BRIDGE` moved to
  `teensy/applications/ESKIN_SPI_USB_BRIDGE`.
- `teensy41/FSR1_ADC_CHANNEL_TEST_BRIDGE` moved to
  `teensy/tests/FSR1_ADC_CHANNEL_TEST_BRIDGE`.
- `SPI_PATTERN_TEST` moved to `integration_tests/SPI_PATTERN_TEST`.
- this record moved to `docs/WORKFLOW.md`.

The default project path in `build_flash_normalized_fsr1.py` was updated to
the new FSR1 application location. Existing STM32 build caches were preserved
for traceability, but they contain absolute paths and must be regenerated
after the move. Historical sections below retain their original paths because
those paths describe the state at the time of each experiment.
## FSR1 raw acquisition rebuild and programming (2026-07-29)

The reorganised production FSR1 application was prepared for a new hardware
test:

```text
stm32/applications/fsr1/
```

The acquisition path was compared with the archived original
`ESKIN_STM32` workspace. Both use the same MUX1 selection, MAX11633
AIN0-to-AIN15 scan, 3.3 V external ADC reference, 515-byte Host SPI frame,
sequence counter, HOST_IRQ handshake, and SPI3 transmit/receive recovery
logic. The important functional difference was that the current FSR1 source
contained a valid MCU zero/full calibration table.

For this hardware test, `FSR_OUTPUT_NORMALIZED` was added and set to `0U`.
The calibration table remains preserved, but each frame now contains the
unmodified 12-bit MAX11633 result (`0..4095`), matching the original
ESKIN raw-acquisition behaviour. This prevents an old calibration from
masking or amplifying current FSR, MUX, FPC, or ADC faults.

The source was configured and built from a fresh short-path directory:

```text
D:\study\programming\builds\ESKIN_FSR1_RAW_20260729
```

Build result:

- configuration and Ninja build: PASS
- RAM: 3,392 bytes
- Flash: 16,880 bytes
- ELF SHA-256:
  `A3FC21C717554409268A991CEFF036D7D60A5B41CDCB290877FFF57D7E348706`

The first flash command included the obsolete `-m hw` option and was rejected
by the installed pyOCD version before any target access. The corrected
programming command was:

```powershell
pyocd flash `
  -t stm32g474cetx `
  -f 10k `
  -M under-reset `
  -e sector `
  "D:\study\programming\builds\ESKIN_FSR1_RAW_20260729\ESKIN_STM32.elf"
```

Programming result:

- erased 18,432 bytes in 9 sectors
- programmed 17,408 bytes in 17 pages
- pyOCD exit code 0

The Teensy appeared as COM9. A 100-frame binary protocol test was then run
with `read_teensy_fsr.py`:

- 100/100 valid 515-byte frames
- sequence 450 through 549
- sequence gaps: 0
- raw sample range: 0 through 2729
- elapsed time: 4.007 seconds
- measured rate: 24.96 frames/s

Result: STM32 programming, MUX1/ADC1 acquisition, Host SPI transport, Teensy
USB forwarding, and PC frame parsing all passed. The system is ready for an
FSR1 pressure/response test using raw ADC display mode; the heatmap must not
be started with `--mcu-normalized` for this firmware.
## Confirmed Stage 9 and Stage 10 root-cause correction (2026-07-29)

The final fault attribution for the ninth and tenth FSR investigation stages
is clarified below.

### Stage 9: FFC connector fault

The main Stage 9 fault was poor electrical contact at the mainboard FFC
connector. The intermittent contact produced missing or unstable complete
rows/columns even though the SPI transport and GUI continued to operate.

The connector fault was distinguished from a software fault because:

- continuity and signal measurements were not repeatable at the connector;
- the affected readings changed with the physical connector/contact condition;
- the STM32 frame sequence and approximately 25 frames/s transport remained
  valid, so the missing data originated before frame packing;
- the MUX, ADC, Teensy bridge, parser, and Heatmap logic passed their isolated
  tests.

The connector was resoldered and then replaced with a new connector. This
resolved the intermittent FFC contact fault and restored a stable electrical
path.

### Transition from Stage 9 to Stage 10

After the connector repair, the board-side hardware path was operating
correctly, but several specific FSR rows still could not display a normal
pressure response. Because these residual failures remained in fixed sensor
lines after the connector, MUX, ADC, SPI, USB, protocol, and GUI paths had
been verified, they were treated as a separate fault rather than evidence
that the connector repair had failed.

### Stage 10: FSR sensor fault

The Stage 10 isolation tests traced the remaining non-responsive rows to the
FSR itself. Direct resistance/pressure checks showed that the affected sensor
rows did not develop the expected conductive response even when the external
electronics and connections were sound.

The confirmed causal sequence is therefore:

```text
Stage 9 widespread/intermittent missing data
  -> poor FFC connector contact
  -> resoldered and replaced connector
  -> intermittent board connection fault resolved
  -> a smaller set of fixed rows still failed
  -> board, MUX, ADC, SPI, USB, parser, and GUI excluded
  -> Stage 10 isolation identified the FSR itself as the residual cause
```

Result: Stage 9 was solved by connector rework/replacement. Stage 10 established
that the remaining fixed-row failures were sensor-side defects and could not
be corrected by further STM32, Teensy, protocol, or Heatmap changes.

## ACC SPI array bring-up: 2026-07-29

An independent ACC test was created by retaining the proven STM32-to-Teensy
transport used for FSR testing and replacing only the sensor acquisition
stage. This keeps the 515-byte frame, sequence counter, `HOST_IRQ` handshake,
SPI3 slave recovery, Teensy USB bridge, and PC frame-lock parser unchanged.

### Hardware mapping

The mainboard schematic and netlist identify a shared SPI2 accelerometer bus
and a CD74HC154 active-low chip-select decoder:

- `PB13`: `SPI2_SCK` / ACC `SCK1`
- `PB14`: `SPI2_MISO` / ACC `MISO1`
- `PB15`: `SPI2_MOSI` / ACC `MOSI1`
- `PC13`: decoder enable `ACC_EN` (active low)
- `PB12`, `PF0`, `PF1`, `PA9`: decoder address bits A0 through A3
- ACC FPC pin 1: `+3.3V`
- ACC FPC pins 2, 3, 4: MISO, SCK, MOSI
- ACC FPC pins 6 through 21: `nCS16` through `nCS1`
- ACC FPC pins 22 through 24: GND

The fitted LIS2DH12/LIS3DH devices share the same required registers for this
test: `WHO_AM_I` at `0x0F` must return `0x33`; `CTRL_REG1=0x57` selects
100 Hz with X/Y/Z enabled; `CTRL_REG4=0x88` selects block-data update,
high-resolution mode, and the +/-2 g range. The data-sheet timing requires
SPI mode 3: clock idle high, data changed on the falling edge, and data
captured on the rising edge.

### Test implementation

The STM32 project is:

```text
stm32/tests/acc_spi_array/
```

The PC receiver is:

```text
teensy/tests/ACC_SPI_ARRAY_TEST/read_acc_array.py
```

No Teensy reflash is required because the production
`ESKIN_SPI_USB_BRIDGE` forwards the same fixed-width binary frame. Within the
16 x 16 transport matrix, each row represents one decoder position:

- column 0: `WHO_AM_I`
- column 1: status code
- columns 2 through 4: signed 12-bit X, Y, and Z
- column 5: MISO idle level
- column 6: command-phase receive byte
- column 7: STM32 HAL SPI2 error code
- columns 8 and 9: `CTRL_REG1` and `CTRL_REG4` readback
- columns 10 through 15: raw XL/XH, YL/YH, and ZL/ZH bytes

The build directory and image are:

```text
D:\study\programming\builds\ESKIN_ACC_SPI_ARRAY_20260729
D:\study\programming\builds\ESKIN_ACC_SPI_ARRAY_20260729\ESKIN_STM32.elf
```

The latest build passed. It uses 3,728 bytes of RAM and 16,564 bytes of
Flash. Two harmless warnings remain because the copied FSR/MAX11633 helper
functions are preserved but not called by this isolated ACC test.

The image was programmed successfully using:

```powershell
pyocd flash `
  -t stm32g474cetx `
  -f 10k `
  -M under-reset `
  -e sector `
  "D:\study\programming\builds\ESKIN_ACC_SPI_ARRAY_20260729\ESKIN_STM32.elf"
```

### Results and fault isolation

The first all-zero test was performed while the ACC assembly was not
connected. It is retained as a procedural observation only and must not be
used as evidence of an ACC hardware fault.

After the ACC assembly was connected, a 100-frame test produced 100 valid
frames, zero sequence gaps, and approximately 21 frames/s. Fourteen decoder
positions returned the correct `WHO_AM_I=0x33`, proving that STM32 SPI2,
the STM32-to-Teensy link, USB forwarding, and PC framing were operating.
Positions ACC1 and ACC2 did not provide a valid identity.

The first code version accepted a successful HAL transfer as successful
configuration. Its XYZ output contained repeated values such as decimal 819
(`0x333`), which are not credible acceleration data. Configuration readback
was therefore added. It showed:

- ACC1/ACC2: `WHO_AM_I=0xFF` in the repeat test, indicating an undriven MISO
  response for those decoder positions;
- ACC3 through ACC16: `WHO_AM_I=0x33`;
- expected `CTRL_REG1=0x57` and `CTRL_REG4=0x88` were not read back;
- observed readbacks repeated `0x33/0x33` or `0x77/0xBB`;
- HAL SPI2 error code remained zero;
- no sensor was allowed to report XYZ after the failed configuration
  verification.

SPI2 was then reduced from approximately 1 MHz to 62.5 kHz while retaining
mode 3. The same identity and configuration-readback pattern remained across
100 valid frames at approximately 22.8 frames/s. Therefore excessive SPI
frequency and long-FFC edge quality are not the primary cause.

Current conclusion: the host transport and most ACC read paths are proven,
but ACC register writes are not arriving correctly. The highest-priority
remaining checks are the common `PB15/MOSI1` route and decoder/CS behaviour.
The repeated correct identity does not prove MOSI write data is correct,
because all supported accelerometers return the same identity value. ACC1
and ACC2 additionally require continuity/selection checks on their individual
`nCS` paths.

The next isolation test should hold `PB15/MOSI1` low and high for one second
at a time and compare the voltage at the STM32 pin, mainboard ACC connector,
and ACC FPC endpoint. After MOSI continuity is confirmed, repeat the same
static-level test for the four decoder address inputs and observe the
selected `nCS` output. Do not interpret the current XYZ values as physical
acceleration until both control-register readbacks pass.

### ACC 3D GUI

A separate live visualization was added at:

```text
teensy/tests/ACC_SPI_ARRAY_TEST/live_acc_3d.py
```

It decodes the same verified 515-byte stream and displays all 16 logical ACC
positions as a 4 x 4 three-dimensional vector field. Each valid arrow shows
the signed X/Y/Z direction and acceleration magnitude in g. Clicking a marker
selects one sensor and reports its identity, status, X/Y/Z values, magnitude,
frame sequence, rate, gaps, and data age. Rows that do not have
`WHO_AM_I=0x33` and status `OK` are shown as red crosses rather than valid
vectors.

The parser and signed 12-bit conversion self-test passed:

```powershell
python .\live_acc_3d.py --self-test
```

The synthetic 25 Hz animation was launched successfully with:

```powershell
python .\live_acc_3d.py --demo
```

This demo mode permits GUI development while the ACC register-write fault is
debugged separately. Live mode is started with:

```powershell
python .\live_acc_3d.py --port COM9
```

## 2026-08-01 — ACC schematic-count correction and nine-device retest

Review of ACC revision 2.2 confirmed that the flex contains nine LIS2DH12TR
devices, not sixteen. Decoder addresses 0..8 select `nCS_1`..`nCS_9`; the ACC
FFC pins corresponding to decoder addresses 9..15 are not populated. The
earlier conclusion that ACC3..ACC16 returned valid identities is therefore
superseded: responses reported for ACC10..ACC16 could not have come from
independent installed sensors.

The STM32 test was changed from `ACC_COUNT=16` to `ACC_COUNT=9`. The command-
line reader and 3D GUI were also limited to the nine installed devices, while
the 16x16 STM32-to-Teensy frame was retained for transport compatibility.

The firmware rebuilt successfully (FLASH 16,564 bytes; RAM 3,648 bytes). The
first 10 kHz under-reset flash reached programming but failed because the core
was not halted. A hardware reset followed by the same flash command succeeded:
two sectors were erased, four pages were programmed, and thirteen pages were
identical. The STM32 was then hardware-reset to run the new image.

A 100-frame COM9 test produced zero sequence gaps at 20.58 frames/s:

- PASS: ACC2, ACC4, ACC5, ACC7, ACC8 (`WHO_AM_I=0x33`, status `OK`,
  `CTRL1=0x57`, `CTRL4=0x88`, and live XYZ values);
- FAIL: ACC1, ACC6, ACC9 returned `0xFF` and status `BAD_ID`;
- FAIL: ACC3 returned `0x00` and status `BAD_ID`.

This proves the shared SPI2 clock/data path and STM32–Teensy–USB frame path are
working. The remaining fault is restricted to the four physical sensor-select
branches or their local sensor/power/interconnect paths. Their physical
locations are ACC1 upper-right, ACC3 lower-right, ACC6 lower-centre, and ACC9
lower-left.

### Follow-up: blank GUI and reset-dependent ACC result

The 3D GUI later showed only its empty axes. Its nine-device rendering path was
verified independently with a synthetic frame and correctly drew all nine
vectors. COM9 could be opened, but no complete ACC frame arrived until the
STM32 was reset, so the blank display was caused by a stopped STM32 data stream
rather than a 3D plotting or layout error.

After the hardware reset, frames resumed with zero sequence gaps, but the ACC
initialisation result changed: ACC1 returned `0xFF`, while ACC2..ACC9 mostly
returned `WHO_AM_I=0x33` but failed control-register readback (`0x77/0xBB` or
`0x33/0x33` instead of `0x57/0x88`). Therefore the earlier fixed set of four
failing physical locations is not sufficient evidence of four permanent open
circuits. Reset-dependent SPI2/CS/MOSI electrical or initialisation behaviour
must be resolved before assigning the remaining faults to individual sensors.

## 2026-08-01 — Permanent ACC startup and runtime recovery

The all-red-cross GUI state was traced to a transient ACC initialisation
failure that became permanent in software. The old `ACC_InitAll()` attempted
each sensor only once and returned `HAL_OK` even when no sensor passed identity
and configuration readback. Consequently `acc_ready[]` remained zero and the
main loop never retried failed sensors until another reset or flash.

The ACC test firmware now implements the following recovery state machine:

1. Disable the CD74HC154 decoder so all nine `nCS` outputs are inactive.
2. Wait 1000 ms after STM32 startup for the Teensy-powered 3.3 V rail and all
   LIS2DH12 devices to complete power-on.
3. For ACC1..ACC9, attempt initialisation up to five times. Every attempt
   reads `WHO_AM_I`, writes `CTRL_REG1=0x57` and `CTRL_REG4=0x88`, and reads
   both registers back before declaring the sensor ready.
4. Continue sending the 515-byte diagnostic frame even when some sensors are
   not ready, so the GUI reports red crosses instead of becoming blank.
5. Once per second, retry only one failed sensor in round-robin order. This
   prevents nine simultaneous retry loops from collapsing the transport to
   approximately 1 frame/s.
6. If a previously ready sensor later fails an XYZ transaction, clear its old
   XYZ bytes, mark it `DATA_SPI_ERROR`, and return it to the background retry
   queue.
7. Keep successfully initialised sensors running while failed branches recover;
   a sensor fault no longer resets or suppresses the complete array.

New compile-time recovery flags are `ACC_POWER_UP_DELAY_MS=1000`,
`ACC_INIT_ATTEMPTS=5`, `ACC_INIT_RETRY_DELAY_MS=20`, and
`ACC_BACKGROUND_RETRY_MS=1000`.

The firmware built successfully with 17,164 bytes Flash and 3,664 bytes RAM.
It was programmed at 10 kHz with under-reset connection; six sectors were
erased, eleven pages programmed, and six pages were identical.

Verification results:

- first 100 frames: zero gaps, 18.40 frames/s;
- following 300 frames: zero gaps, 18.43 frames/s;
- after a further hardware reset, 100 frames: zero gaps, 18.50 frames/s;
- stable PASS: ACC1, ACC2, ACC4, ACC7, ACC8 (`WHO_AM_I=0x33`, status `OK`,
  `CTRL1=0x57`, `CTRL4=0x88`, and live XYZ);
- persistent `BAD_ID`: ACC3 returned `0x00`; ACC5, ACC6, and ACC9 returned
  `0xFF` despite repeated recovery attempts.

The common reset-dependent all-ACC failure is therefore resolved in firmware.
The four persistent branches are now isolated from that software fault and
should be checked individually for decoder/nCS continuity, local power and
ground, FFC/solder integrity, OR-gate output, MISO continuity, or a damaged
LIS2DH12.

## Command and flag reference (2026-08-01)

This appendix is the current copy-and-paste reference for the active firmware
layout. Earlier chronological entries are retained for traceability, but their
old paths may no longer match the reorganised workspace. Commands below use
PowerShell. Run only one program that opens the Teensy COM port at a time.

### Common paths and reusable PowerShell variables

```powershell
$FirmwareRoot = "D:\OneDrive - Imperial College London\study\Imperial College\BIOE70025 - MSc Major Individual Project 2025-2026\E-SKIN\hardware\new\firmware"
$PythonExe = "D:\study\programming\Anaconda\python.exe"
$Stm32Target = "stm32g474cetx"
$DaplinkUid = "LU_2022_8888"
$TeensyPort = "COM9"
```

If Windows assigns a different COM port, change `$TeensyPort`. Check the
current ports with:

```powershell
Get-CimInstance Win32_SerialPort | Select-Object DeviceID, Description
```

### Tool and dependency checks

```powershell
cmake --version
ninja --version
arm-none-eabi-gcc --version
pyocd --version
& $PythonExe --version
& $PythonExe -m pip show pyserial numpy matplotlib
```

Install missing PC visualization dependencies with:

```powershell
& $PythonExe -m pip install pyserial numpy matplotlib
```

### CMake configure and build

Use a short external build directory because the OneDrive source path is very
long. Configure once after changing project location or deleting the build
cache, then use `cmake --build` for subsequent code changes.

```powershell
$Stm32Project = Join-Path $FirmwareRoot "stm32\tests\acc_spi_array"
$Stm32Build = "D:\study\programming\builds\ESKIN_ACC_SPI_ARRAY_20260729"

cmake `
  -S $Stm32Project `
  -B $Stm32Build `
  -G Ninja `
  -DCMAKE_BUILD_TYPE=Debug `
  --toolchain "$Stm32Project\cmake\gcc-arm-none-eabi.cmake"

cmake --build $Stm32Build --parallel
```

Current source/build mappings:

| Purpose | `$Stm32Project` relative to `$FirmwareRoot` | Recommended `$Stm32Build` |
|---|---|---|
| FSR1 raw/normalised application | `stm32\applications\fsr1` | `D:\study\programming\builds\ESKIN_FSR1_RAW_20260729` or `ESKIN_FSR1_NORMALIZED` |
| FSR2 application | `stm32\applications\fsr2` | `D:\study\programming\builds\ESKIN_STM32_FSR2_NORMAL` |
| ACC 9-device test | `stm32\tests\acc_spi_array` | `D:\study\programming\builds\ESKIN_ACC_SPI_ARRAY_20260729` |
| FSR1 AIN/MUX isolation test | `stm32\tests\fsr1_adc_channel` | `D:\study\programming\builds\FSR1_ADC_CHANNEL_TEST` |
| Host SPI `0x55` integration test | `integration_tests\SPI_PATTERN_TEST\ESKIN_STM32_PATTERN` | Create a fresh short build directory |

CMake flags used here:

| Flag | Meaning |
|---|---|
| `-S PATH` | STM32 source/project directory |
| `-B PATH` | out-of-source build directory |
| `-G Ninja` | generate Ninja build files |
| `-DCMAKE_BUILD_TYPE=Debug` | debug symbols and debug optimisation level |
| `-DCMAKE_BUILD_TYPE=Release` | optimised release build |
| `--toolchain FILE` | select the ARM GNU embedded toolchain file |
| `--parallel` | compile independent files in parallel |

### DAPLink and pyOCD: normal workflow

Before SWD operations, close Cortex-Debug and stale pyOCD processes. Closing
the Heatmap is not required for SWD itself, but it is recommended while the
STM32 is being reset.

```powershell
Get-Process pyocd -ErrorAction SilentlyContinue | Stop-Process -Force
pyocd list --probes --no-config -v
```

Normal low-speed reset, flash, and post-flash reset:

```powershell
$Elf = Join-Path $Stm32Build "ESKIN_STM32.elf"

pyocd reset `
  -u $DaplinkUid `
  -t $Stm32Target `
  -f 10k `
  -M under-reset `
  -m hw

pyocd flash `
  -u $DaplinkUid `
  -t $Stm32Target `
  -f 10k `
  -M under-reset `
  -e sector `
  $Elf

pyocd reset `
  -u $DaplinkUid `
  -t $Stm32Target `
  -f 10k `
  -M under-reset `
  -m hw
```

pyOCD flags:

| Flag | Meaning |
|---|---|
| `-u`, `--uid` | select one DAPLink probe by unique ID |
| `-t`, `--target` | target MCU/CMSIS-Pack name; currently `stm32g474cetx` |
| `-f`, `--frequency` | SWD clock, for example `10k`, `100k`, or `100000` Hz |
| `-M under-reset` | assert NRST while initially connecting |
| `-m hw` | request hardware reset through NRST |
| `-e sector` | erase only Flash sectors needed by the new ELF |
| `--no-config` | ignore local pyOCD configuration files during probe listing |
| `-v` | verbose diagnostic output |

### SWD recovery after `No ACK`, `Unexpected ACK`, or `core is not halted`

Verify that STM32 VDD is stable, NRST is normally high, SWDIO/SWCLK are
continuous, and DAPLink GND is connected to mainboard GND. If the probe pin is
an output labelled `3V3`, do not connect it in parallel with Teensy power. If
it is explicitly a `VTref/Vref` input, connect it to the target 3.3 V rail.

```powershell
Get-Process pyocd -ErrorAction SilentlyContinue | Stop-Process -Force
pyocd list --probes --no-config -v

pyocd reset `
  -u $DaplinkUid `
  -t $Stm32Target `
  -f 1k `
  -M under-reset `
  -m hw

pyocd flash `
  -u $DaplinkUid `
  -t $Stm32Target `
  -f 1k `
  -M under-reset `
  -e sector `
  $Elf
```

Do not treat `pyocd list` reporting `Target: n/a` as proof that the STM32 is
absent: probe enumeration does not always open the target. A successful
target connection must show Cortex-M4 discovery or complete a reset/flash.

### Optional pyOCD GDB server for Cortex-Debug

Run this in a dedicated terminal and leave it open:

```powershell
pyocd gdbserver `
  -u $DaplinkUid `
  -t $Stm32Target `
  -f 10k `
  -M under-reset `
  --port 50000 `
  --telnet-port 50001
```

`GDB server listening on port 50000` means the server is running. The terminal
will not behave like a normal PowerShell prompt until the server is stopped.

### COM-port ownership and recovery

Only one process can open COM9. `PermissionError(13)` means another GUI,
serial monitor, or Python process owns it.

```powershell
Get-Process python -ErrorAction SilentlyContinue |
  Select-Object Id, ProcessName, MainWindowTitle

Get-CimInstance Win32_Process |
  Where-Object { $_.CommandLine -match 'COM9|live_fsr|live_acc|read_teensy|read_acc' } |
  Select-Object ProcessId, Name, CommandLine
```

Stop only the confirmed stale process:

```powershell
Stop-Process -Id <PROCESS_ID> -Force
```

After reconnecting Teensy, wait for Windows to recreate the COM port, reset
the STM32 if the GUI opens but no complete frames arrive, and then reopen one
reader or GUI.

### Teensy 4.1 Arduino programs

The current system uses Arduino IDE/Teensyduino for Teensy uploads. Select:

- Board: `Teensy 4.1`
- USB Type: `Serial`
- Correct Teensy port
- One of the following sketches:

| Purpose | Sketch |
|---|---|
| Normal FSR/ACC binary bridge | `teensy\applications\ESKIN_SPI_USB_BRIDGE\ESKIN_SPI_USB_BRIDGE.ino` |
| FSR1 ADC-channel test bridge | `teensy\tests\FSR1_ADC_CHANNEL_TEST_BRIDGE\FSR1_ADC_CHANNEL_TEST_BRIDGE.ino` |
| Host SPI `0x55` test receiver | `integration_tests\SPI_PATTERN_TEST\teensy_spi_pattern_receiver\teensy_spi_pattern_receiver.ino` |

The `.ino` files have no runtime command-line flags. Their important compile-
time constants are:

| Sketch | Constants |
|---|---|
| Normal bridge | `STM32_CS_PIN=10`, `STM32_IRQ_PIN=2`, `USB_BAUD=2000000`, `SPI_HZ=100000`, `FRAME_MAGIC=0xA5`, `FRAME_BYTES=515` |
| ADC-channel bridge | pin 10 CS, pin 2 IRQ, `SPI_HZ=100000`, `EXPECTED_AIN=10`, `FRAME_MAGIC=0xA7`, `FRAME_BYTES=8` |
| Pattern receiver | pin 10 CS, pin 2 IRQ, `SPI_CLOCK_HZ=100000`, `EXPECTED_BYTE=0x55` |

Changing these constants requires recompiling and uploading the Teensy sketch.

### FSR Heatmap: `live_fsr_heatmap.py`

Working directory:

```powershell
Set-Location (Join-Path $FirmwareRoot "teensy\applications\ESKIN_SPI_USB_BRIDGE")
```

Typical commands:

```powershell
& $PythonExe -u .\live_fsr_heatmap.py --port $TeensyPort --region fsr1
& $PythonExe -u .\live_fsr_heatmap.py --port $TeensyPort --region fsr2
& $PythonExe -u .\live_fsr_heatmap.py --port $TeensyPort --region fsr1 --mcu-normalized
& $PythonExe -u .\live_fsr_heatmap.py --demo
& $PythonExe -u .\live_fsr_heatmap.py --help
```

Flags:

| Flag | Default | Purpose |
|---|---:|---|
| `--port PORT` | required outside demo | Teensy serial port |
| `--baud N` | `2000000` | serial baud value |
| `--timeout S` | `0.5` | serial read timeout |
| `--refresh-ms N` | `10` | GUI polling interval; hardware frame rate still limits redraw rate |
| `--vmin N`, `--vmax N` | `0`, `4095` | fixed colour limits |
| `--region fsr1|fsr2` | `fsr1` | calibration/raw-data folder and region mapping |
| `--calibration-file PATH` | `data/calibration/<region>.json` | override calibration JSON |
| `--raw-output-directory PATH` | `data/raw_frames/<region>` | override Save raw frame folder |
| `--calibration-frames N` | `250` | zero/full capture length, about 10 s at 25 fps |
| `--min-calibration-span N` | `50` | minimum valid per-cell full-minus-zero span |
| `--autoscale` | off | colour limits follow each live frame |
| `--mcu-normalized` | off | interpret STM32 values directly as normalised 0..4095 pressure |
| `--transpose` / `--no-transpose` | transpose on | exchange raw MUX and ADC axes |
| `--flip-rows` | off | reverse displayed rows only |
| `--flip-columns` / `--no-flip-columns` | flip on | control default left/right reversal |
| `--demo` | off | generated data without COM port |

### Raw FSR frame reader: `read_teensy_fsr.py`

```powershell
& $PythonExe .\read_teensy_fsr.py --port $TeensyPort --frames 100 --summary-only
& $PythonExe .\read_teensy_fsr.py --port $TeensyPort --frames 1 --print-matrix
```

Flags: `--port` is required; `--baud` defaults to 2000000; `--timeout` to 2 s;
`--print-matrix` prints all 256 cells; `--summary-only` suppresses per-frame
lines; `--frames 0` runs indefinitely; `--max-time 0` disables the time limit.

### Selected-row response test: `test_adc_rows.py`

```powershell
& $PythonExe .\test_adc_rows.py --port $TeensyPort
& $PythonExe .\test_adc_rows.py --self-test
```

Flags: `--port`; `--baud 2000000`; `--timeout 2`; `--rows 1,12,13,14`;
`--baseline-frames 50`; `--test-frames 75`; `--threshold 50`; `--output PATH`;
and `--self-test`. Without `--output`, JSON is saved under
`data/row_tests/adc_row_test_<timestamp>.json`.

### Peak plus eight neighbours: `test_peak_neighborhood.py`

```powershell
& $PythonExe -u .\test_peak_neighborhood.py --port $TeensyPort
& $PythonExe -u .\test_peak_neighborhood.py --port $TeensyPort --mcu-normalized
```

Flags: required `--port`; `--baud 2000000`; `--timeout 0.5`;
`--refresh-ms 10`; and `--mcu-normalized` for STM32-normalised 0..4095 input.

### Build/flash calibrated FSR1: `build_flash_normalized_fsr1.py`

First capture both zero and full calibration in the Heatmap, close every COM9
owner, then run:

```powershell
& $PythonExe .\build_flash_normalized_fsr1.py --flash
```

Flags:

| Flag | Default | Purpose |
|---|---|---|
| `--calibration-file PATH` | `data/calibration/fsr1.json` | zero/full calibration source |
| `--project PATH` | `stm32/applications/fsr1` | STM32 source project |
| `--build-dir PATH` | `D:\study\programming\builds\ESKIN_FSR1_NORMALIZED` | output build directory |
| `--minimum-span N` | `50` | invalid below this per-cell ADC span |
| `--minimum-calibration-frames N` | `250` | required frames in each endpoint capture |
| `--require-all-valid` | off | stop if any cell is invalid |
| `--build` | off | generate header and build |
| `--flash` | off | generate, build, and pyOCD flash |
| `--probe UID` | `LU_2022_8888` | DAPLink selector |
| `--target NAME` | `stm32g474cetx` | pyOCD target |
| `--frequency RATE` | `10k` | SWD frequency |

### ACC command-line test: `read_acc_array.py`

```powershell
Set-Location (Join-Path $FirmwareRoot "teensy\tests\ACC_SPI_ARRAY_TEST")
& $PythonExe .\read_acc_array.py --port $TeensyPort --frames 100 --summary-only
& $PythonExe .\read_acc_array.py --port $TeensyPort --sensor 2 --frames 100
```

Flags: `--port COM9`; `--baud 2000000`; `--timeout 2`; `--frames 0` for
continuous capture; `--sensor 1..9` for one installed ACC; and
`--summary-only` for aggregate output.

### ACC 3D GUI: `live_acc_3d.py`

```powershell
& $PythonExe -u .\live_acc_3d.py --port $TeensyPort
& $PythonExe -u .\live_acc_3d.py --demo
& $PythonExe -u .\live_acc_3d.py --self-test
```

Flags: `--port COM9`; `--baud 2000000`; `--interval-ms 40`;
`--vector-scale 0.9`; `--demo`; and `--self-test`. A red cross means the row
failed identity/configuration validation; an empty plot means no complete ACC
frame has reached the GUI yet.

### FSR1 ADC-channel binary capture: `read_adc_channel_test.py`

This reader requires the matching STM32 `fsr1_adc_channel` image and matching
Teensy ADC-channel bridge, both using frame magic `0xA7`.

```powershell
Set-Location (Join-Path $FirmwareRoot "teensy\tests\FSR1_ADC_CHANNEL_TEST_BRIDGE")
& $PythonExe .\read_adc_channel_test.py --port $TeensyPort
```

Flags: `--port COM9`; `--baud 2000000`; `--timeout 15`; and
`--output-directory PATH`. The output is a raw CSV containing 16 AIN channels
and 100 conversions per channel.

### STM32 and protocol compile-time flags

These are C preprocessor constants, not terminal arguments. Edit them only in
the matching `Core/Src/main.c`, rebuild, and flash the resulting ELF.

| Project | Important constants and current meaning |
|---|---|
| FSR1 application | `FSR_ROWS=16`, `FSR_COLUMNS=16`, `FSR_OUTPUT_NORMALIZED=0` for raw ADC; set to `1` only with a valid generated calibration header; `HOST_FRAME_MAGIC=0xA5`, frame size 515 bytes |
| FSR2 application | 16x16 raw ADC acquisition through MUX2/ADC2; `HOST_FRAME_MAGIC=0xA5` |
| ACC test | `ACC_COUNT=9`, `ACC_WHO_AM_I_EXPECTED=0x33`, `ACC_CTRL_REG1_100HZ=0x57`, `ACC_CTRL_REG4_HR_BDU=0x88`, `ACC_SPI_TIMEOUT=10 ms`, `ACC_POWER_UP_DELAY_MS=1000`, `ACC_INIT_ATTEMPTS=5`, `ACC_INIT_RETRY_DELAY_MS=20`, `ACC_BACKGROUND_RETRY_MS=1000`, 16x16 compatibility transport |
| FSR1 ADC-channel test | `MUX_CHANNELS=16`, `TEST_ADC_CHANNEL=10`, `MUX_STEP_PERIOD_MS=1000`, `HOST_FRAME_MAGIC=0xA7` |
| Host SPI pattern test | `SPI_PATTERN_BYTE=0x55`, `SPI_PATTERN_TIMEOUT_MS=1000` |

Shared binary frame flags:

- normal FSR and ACC diagnostic transport: magic `0xA5`, sequence `uint16`,
  256 little-endian `uint16` values, total 515 bytes;
- ADC-channel isolation transport: magic `0xA7`, total 8-byte live frame in
  the current fixed-AIN stepping firmware;
- raw ADC range: 0..4095 for 12-bit samples;
- MCU-normalised range: 0 means stored zero-pressure baseline and 4095 means
  stored full-pressure baseline.

### Fast end-to-end checks

Check normal binary transport without opening a GUI:

```powershell
Set-Location (Join-Path $FirmwareRoot "teensy\applications\ESKIN_SPI_USB_BRIDGE")
& $PythonExe .\read_teensy_fsr.py --port $TeensyPort --frames 25 --summary-only
```

Check the current nine-device ACC firmware:

```powershell
Set-Location (Join-Path $FirmwareRoot "teensy\tests\ACC_SPI_ARRAY_TEST")
& $PythonExe .\read_acc_array.py --port $TeensyPort --frames 100 --summary-only
```

If either command reports `PermissionError(13)`, close the other serial
program. If it opens COM9 but waits indefinitely, confirm Teensy power and USB,
then reset the STM32 and retry.

## 2026-08-01: fixed ACC1/ACC5 WHO_AM_I isolation test

The complete nine-device ACC firmware was preserved as the active application
at `stm32/applications/acc_array/`. The existing
`stm32/tests/acc_spi_array/` copy remains unchanged for traceability.

A new test project was created at `stm32/tests/acc_a1_a5_whoami/`. It never
selects the other seven decoder outputs. Each frame performs exactly 100
independent `WHO_AM_I` reads at decoder address 0 (physical ACC1), deselects
all devices, then performs exactly 100 reads at decoder address 4 (physical
ACC5). nCS is released after every two-byte register transaction.

The 256-word compatibility payload is:

- words 0..99: raw ACC1 results;
- words 100..199: raw ACC5 results;
- words 200..215: magic `0xA15`, result counters and SPI diagnostics;
- words 216..255: reserved zero values.

Normal identity is `0x033`. Values `0x100..0x10F` denote STM32 HAL SPI errors
rather than bytes returned by the sensor. The normal Teensy application is
still used. Monitor the test using:

```powershell
Set-Location (Join-Path $FirmwareRoot "teensy\tests\ACC_A1_A5_WHOAMI_TEST")
& $PythonExe .\read_a1_a5_whoami.py --port $TeensyPort
& $PythonExe .\read_a1_a5_whoami.py --port $TeensyPort --frames 1 --show-samples
```

This test distinguishes per-device instability from interaction during the
full nine-device scan. If both devices pass 100/100 in isolation but alternate
between pass and fail in the full application, investigate decoder selection,
shared MISO contention, supply stability and dynamic nCS waveforms.

### Flash and measured result

The test ELF was successfully programmed with pyOCD at 10 kHz using
connect-under-reset. The loader erased 12288 bytes, programmed 11264 bytes and
reported 6144 identical bytes. A hardware reset then started the test image.

The first captured test frame produced:

- ACC1: `WHO_AM_I=0x33` for 100/100 transactions;
- ACC5: `WHO_AM_I=0xFF` for 100/100 transactions;
- combined STM32 SPI peripheral error: `0x000`.

Five further frames reproduced exactly the same result, giving another
500/500 successful identity reads for ACC1 and 500/500 `0xFF` reads for ACC5.
After another DAPLink hardware reset, ACC1 again passed 100/100 and ACC5 again
returned `0xFF` 100/100. No exchange of the logical ACC1 and ACC5 results was
observed in this fixed-address image.

Interpretation: the STM32 SPI peripheral completed every transaction, but at
decoder address 4 the shared MISO remained high and no `0x33` response was
received. This result removes the full nine-device initialization and scan
order from the immediate failure mechanism. It does not by itself distinguish
between the physical ACC5 branch, dynamic decoder/nCS selection, local supply,
SDI gate output, SCK arrival, and ACC5 SDO/MISO drive.

### Complete ACC application restored

The fixed A1/A5 test image was subsequently replaced by the complete
nine-device ACC application from build
`D:\study\programming\builds\ESKIN_ACC_SPI_ARRAY_20260729\ESKIN_STM32.elf`.
The first three upload attempts before reconnecting the hardware failed at
`DebugPortSetup` with SWD `No ACK`; no erase was started. After power/DAPLink
reconnection, pyOCD at 10 kHz under reset successfully erased 12288 bytes,
programmed 11264 bytes and reported 6144 identical bytes. Hardware reset then
started the full application.

One command-line validation frame reported:

- valid: ACC1, ACC2, ACC4, ACC7 and ACC8 (`WHO_AM_I=0x33`, `CTRL1=0x57`,
  `CTRL4=0x88`, valid XYZ data);
- ACC3: `WHO_AM_I=0x00`, `BAD_ID`;
- ACC5, ACC6 and ACC9: `WHO_AM_I=0xFF`, `BAD_ID`;
- STM32 SPI error code for all positions: `0x000`;
- Host frame sequence was continuous with zero gaps.

The `live_acc_3d.py` GUI was then launched on COM9 using the complete ACC
protocol.

## 2026-08-01: selectable single-ACC build and flash

The existing `stm32/tests/acc_spi_array` firmware now accepts the CMake cache
variable `ACC_SELECTED`. Value `0` preserves the original A1..A9 array scan;
values `1`..`9` restrict initialization, background recovery, and acquisition
to the selected physical ACC. The fixed Host SPI frame format is unchanged,
and rows belonging to unselected devices remain zero, so the normal Teensy
USB bridge and `read_acc_array.py` remain compatible.

The helper script performs configuration, compilation, and DAPLink flashing:

```powershell
Set-Location (Join-Path $FirmwareRoot "stm32\tests\acc_spi_array")

# Compile and flash only ACC5. The CMD wrapper avoids local PowerShell policy
# restrictions without changing the global execution policy.
.\flash_selected_acc.cmd 5

# Prompt for 0..9 interactively.
.\flash_selected_acc.cmd

# Restore the complete nine-device scan.
.\flash_selected_acc.cmd 0
```

The selection is made at compile time and cannot change until a different
image is built and flashed. This prevents activity on every unselected nCS
branch during an isolation test.

### ACC3 single-device upload and result

Direct invocation of `flash_selected_acc.ps1` was initially blocked by the
Windows PowerShell execution policy (`PSSecurityException`). This was not an
STM32, compiler, DAPLink, or firmware error. The upload was completed using a
child PowerShell process with `-ExecutionPolicy Bypass`; the global user and
machine policies were not modified. `flash_selected_acc.cmd` was then added as
the permanent project-local launcher for this restricted environment.

The `ACC_SELECTED=3` image compiled and linked successfully, then entered the
pyOCD erase/program sequence at 10 kHz under reset. A subsequent COM9 frame
confirmed the selected-image behaviour:

- only the ACC3 transport row was populated;
- all eight unselected ACC rows remained zero;
- ACC3 returned `WHO_AM_I=0x00` and `BAD_ID`;
- the STM32 SPI error code was `0x000`;
- frame sequence 119 was received with zero gaps.

Therefore the single-device selection and transport are working. The ACC3
failure is a sensor/branch response failure rather than an attempt to scan a
different decoder address.

## 2026-08-01: single-ACC mode promoted to the main application

The compile-time selection mechanism was promoted from
`stm32/tests/acc_spi_array` to the maintained production source at
`stm32/applications/acc_array`. With `ACC_SELECTED=1..9`, the main application
initialises, retries, selects, acquires, and transmits only the chosen ACC.
Every other decoder output remains disabled. `ACC_SELECTED=0` remains the
explicit full-array mode.

The fixed 515-byte transport envelope is intentionally retained so the Teensy
bridge does not require a second firmware variant. In single mode only the
chosen logical ACC row contains diagnostics and XYZ samples; all other rows
are zero. Use the application-local launcher:

```powershell
Set-Location (Join-Path $FirmwareRoot "stm32\applications\acc_array")
.\flash_selected_acc.cmd 3  # only ACC3
.\flash_selected_acc.cmd 0  # complete A1..A9 stream
```

The production application was built with `ACC_SELECTED=3`, flashed through
DAPLink at 10 kHz under reset, and started with a hardware reset. Three COM9
frames (sequence 112..114) arrived with zero gaps at approximately 17.5 fps.
Only the ACC3 row was populated; ACC1, ACC2, and ACC4..ACC9 remained zero in
every frame. ACC3 consistently reported `WHO_AM_I=0x00`, `BAD_ID`, and STM32
SPI error `0x000`. This runtime capture verifies that the main application no
longer accesses the other eight decoder outputs in the selected build.

## 2026-08-01: ACC hardware branch verification

The ACC connector, decoder/nCS branches, OR-gate network, and local passive
components were checked systematically before further firmware diagnosis.

### Connector and nCS-to-OR-gate continuity

- Continuity was checked from every CD74HC154 nCS output used by ACC1..ACC9,
  through the connector, to its corresponding OR-gate branch.
- An unreliable connector joint was found during this process.
- The connector was re-soldered.
- Repeated continuity measurements after rework confirmed that every required
  nCS branch reaches the intended OR gate reliably.

Result: **passed after connector rework**. The earlier connector contact fault
is considered repaired.

### Short-circuit and component checks

- The relevant connector pins, adjacent nCS branches, SPI nets, supply, and
  ground were checked for unintended shorts.
- No short circuit was detected in the tested ACC signal network.
- Every OR-gate branch passed the electrical checks.
- The associated local capacitors passed the component/connection checks.

Result: **passed**. No OR-gate, capacitor, or measured inter-net short fault
was found.

### Fixed single-ACC address verification

- The STM32 was built in single-ACC mode so that only one decoder address was
  selected continuously; the other eight ACC branches were not scanned.
- The four decoder address inputs were measured and compared with the
  CD74HC154 truth table.
- The active-low nCS output corresponding to the requested ACC was also
  checked.
- The measured address and selected output matched the requested ACC.

Result: **passed**. The STM32 GPIO-to-decoder address mapping and the
single-device firmware selection are correct.

### Current conclusion

The following candidate faults have now been excluded by direct hardware
tests: connector discontinuity after rework, incorrect nCS-to-OR-gate routing,
measured shorts, failed OR-gate branches, failed checked capacitors, and an
incorrect decoder address generated by the STM32. Any remaining `BAD_ID`
response must be investigated farther downstream on the selected sensor
branch, including local sensor power/ground, SCK, gated SDI/MOSI, SDO/MISO,
device soldering, and the LIS2DH12 itself.

## 2026-08-02: complete nine-ACC application restored

The main ACC application was rebuilt with `ACC_SELECTED=0` to restore the
original ACC1..ACC9 scan. `compile_commands.json` was checked before flashing
and confirmed that `main.c` was compiled with `-DACC_SELECTED=0`.

Initial programming attempts did not replace the previous single-ACC image.
The DAPLink session stopped during erase, later reported `No ACK`, and one
1 kHz pyOCD process remained in the background. That task-owned process was
terminated. The DAPLink USB connection was then unplugged and reconnected.

After reconnection, pyOCD at 10 kHz under reset completed successfully:

- erased 4096 bytes in 2 sectors;
- programmed 4096 bytes in 4 pages;
- verified 13312 identical bytes in 13 pages;
- reported no loader or verification error.

A hardware reset started the new image. Five consecutive COM9 frames
(sequence 84..88) arrived with zero gaps at approximately 16.0 fps. Unlike the
previous single-device image, all nine logical rows contained their own
diagnostic results:

- ACC1, ACC2, ACC4, ACC7, ACC8: `WHO_AM_I=0x33`, status `OK`,
  `CTRL1=0x57`, `CTRL4=0x88`, and live XYZ samples;
- ACC3: `WHO_AM_I=0x00`, `BAD_ID`;
- ACC5, ACC6, ACC9: `WHO_AM_I=0xFF`, `BAD_ID`;
- STM32 SPI error code: `0x000` for every ACC.

This confirms that the complete nine-device firmware is now running. The
remaining failures reproduce the previously established low-response ACC3
branch and high-response ACC5/ACC6/ACC9 branches.

## 2026-08-02: selectable ACC OR-gate static test

A separate STM32 project was created at
`stm32/tests/acc_or_gate_static` to test the per-ACC SDI OR gate with a
multimeter. It does not modify or replace the maintained ACC application
source.

At build time the user selects ACC1..ACC9 and a static MOSI level. The firmware
then disables SPI2, configures PB13/SCK as a constant low GPIO, configures
PB15/MOSI as a constant GPIO level, applies the chosen decoder address, and
keeps the selected nCS low continuously. No SCK edge is generated, so the
LIS2DH12 cannot interpret the static condition as an SPI transaction.

For the selected branch, the expected local gate equation is
`SDI = nCS OR MOSI`. With nCS held low, the gate output must follow MOSI:

- `MOSI=1`: output approximately 3.3 V;
- `MOSI=0`: output approximately 0 V.

Commands:

```powershell
Set-Location (Join-Path $FirmwareRoot "stm32\tests\acc_or_gate_static")
.\flash_or_gate_static.cmd 5    # ACC5, nCS=0, SCK=0, MOSI=1
.\flash_or_gate_static.cmd 5 0  # ACC5, nCS=0, SCK=0, MOSI=0
```

Both ACC5/MOSI-high and ACC5/MOSI-low configurations compiled and linked
successfully. The static test intentionally produces no Host SPI or USB
frames. After measurement, restore normal acquisition from
`stm32/applications/acc_array` with `flash_selected_acc.cmd 0`.

### Hardware result

The static OR-gate test was completed successfully. The measured behaviour
fully matched the expected truth table for the selected ACC branch:

| Test point | MOSI-high test | MOSI-low test |
| --- | ---: | ---: |
| Selected `nCS` | approximately 0 V | approximately 0 V |
| `SCK` | approximately 0 V | approximately 0 V |
| STM32 `MOSI` | approximately 3.3 V | approximately 0 V |
| OR-gate output / selected ACC `SDI` | approximately 3.3 V | approximately 0 V |

Because `nCS=0`, the equation `SDI = nCS OR MOSI` reduces to `SDI = MOSI`.
The output followed both the high and low MOSI states correctly. Therefore,
the tested OR gate, its static logic function, and the associated selected
signal path are considered verified and are not the cause of the current ACC
communication failures. MISO was not used as a pass/fail criterion because
SCK remained static and no valid SPI read transaction was generated.

### Attempt to restore the complete ACC application

The maintained `stm32/applications/acc_array` project was configured with
`ACC_SELECTED=0` and built successfully, producing the complete nine-ACC ELF
image. DAPLink was enumerated as `jixin.pro CMSIS-DAP_LU`, UID
`LU_2022_8888`. Flashing was attempted under reset at both 10 kHz and 1 kHz.
Both attempts stopped before erase/programming at pyOCD `DebugPortSetup` with
`SWD/JTAG communication failure (No ACK)`. Consequently, this attempt did not
replace the firmware already in STM32 Flash. The compiled application image is
ready, but SWDIO, SWCLK, NRST, target power/reference, and common ground must be
restored before flashing is repeated.

The missing SWD cable was then identified and reconnected. DAPLink enumeration
subsequently completed normally in about one second, confirming that the probe
and its USB connection were accessible. However, both a 10 kHz under-reset
flash and a 10 kHz normal-connect flash stalled before any erase/program
message. The stalled pyOCD child processes were terminated so the probe was not
left occupied. This shows that the full application image remains ready but was
still not programmed; the remaining fault is on the powered target/SWD path
rather than in the build, pyOCD enumeration, or NRST connection mode alone.

## 2026-08-03: one-second ACC SCK continuity test

A separate diagnostic project was created at `stm32/tests/acc_sck_slow` so the
shared accelerometer clock can be measured with a multimeter. It disables
SPI2, configures PB13 as a GPIO, holds SCK low for one second and high for one
second, and repeats continuously. PC13/ACC_EN remains high so every CD74HC154
nCS output stays inactive; PB15/MOSI remains low. Consequently no LIS2DH12 is
selected and the slow SCK edges cannot form a valid sensor transaction.

Expected values at STM32 PB13, mainboard ACC connector pin 3, ACC FPC pin 3,
and every LIS2DH12 SCL/SPC pin are approximately 0 V for one second followed
by the board I/O rail (normally 3.2-3.3 V) for one second. All nCS lines should
remain approximately 3.3 V throughout the test.

The project compiled and linked successfully (RAM 1912 bytes, Flash 11540
bytes). DAPLink enumerated normally as UID `LU_2022_8888`, but both the scripted
and direct 10 kHz under-reset flash attempts stalled before erase/programming.
Residual pyOCD processes were terminated. Therefore the diagnostic is ready
but was not programmed in this attempt; the existing STM32 firmware remains
unchanged until SWD target communication is restored.
# ACC3 slow nCS continuity test (2026-08-03)

An isolated `stm32/tests/acc_ncs_slow` diagnostic was added for meter-visible
chip-select testing. By default, SCK and MOSI remain at 0 V, and
only ACC3 nCS alternates between high for 1 second and low for 1 second. All
other nCS outputs remain high. With no SCK edge, no SPI command is transferred.
Use `.\flash_acc_ncs_slow.cmd 3`, then restore the acquisition firmware with
`.\flash_selected_acc.cmd 3` after testing.
# ACC OR-gate and selection-path verification (2026-08-03)

Hardware testing confirmed that all ACC OR gates operate correctly. Each gate
produced the expected truth-table outputs with its selected nCS held low and
MOSI tested at both static logic levels. The shared MOSI path, each local SDI
path after the OR gate, and the individual active-low nCS selection paths were
also verified and found correct.

**Conclusion:** the present ACC communication failures are not caused by the
OR gates, MOSI-to-SDI routing, or nCS routing/selection. Further diagnosis
should focus on SCK arrival at each device, VDD/VDD_IO, the shared SDO/MISO
return path, LIS2DH12 solder joints, and the sensors themselves.
# Selectable slow-MOSI ACC test (2026-08-03)

Added `stm32/tests/acc_mosi_slow`. The user selects ACC1..ACC9 at build/flash
time. SCK stays at 0 V, the selected nCS stays at 0 V, all other nCS outputs
stay high, and MOSI alternates between 0 V for 1 second and approximately
3.3 V for 1 second. With the selected nCS low, its local OR-gate SDI output
must follow MOSI; unselected SDI outputs must remain high. No SCK edge is
generated, so no valid SPI command is transferred. Example:
`.\flash_acc_mosi_slow.cmd 3`.
# ACC3/ACC5 removal and exposed-pad verification (2026-08-03)

The LIS2DH12 devices at physical ACC3 and ACC5 were removed to expose their
pads. Measurements at the unpopulated locations confirmed correct nCS, SCK,
and MOSI/SDI behaviour. The corresponding OR-gate paths were also verified as
correct. Despite the devices being absent, the live ACC summary continued to
report different open-device signatures:

- logical ACC3: `MISO=0`, `command RX=0x00`, `WHO_AM_I=0x00`, `BAD_ID`;
- logical ACC5: `MISO=1`, `command RX=0xFF`, `WHO_AM_I=0xFF`, `BAD_ID`.

ACC5 is the expected absent-slave signature: no device drives MISO and the
shared line remains high. ACC3 is not the expected absent-slave signature.
Because the removed sensor, OR gate, nCS, SCK, and SDI paths have been
excluded, follow-up diagnosis must target the SDO/MISO return network, confirm
logical-to-physical decoder mapping at the exposed CS pads, and exclude stale
or mismatched firmware/frames. In particular, compare the exposed ACC3 and
ACC5 SDO pads to STM32 PB14 and test for bridges from ACC3 SDO to GND, SDI, or
nCS with power removed.
# ACC3 short repair and ACC5 intermittent recovery (2026-08-03)

A physical short between ACC3 SDO and nCS was identified and repaired. This
explains the earlier ACC3 low-bus signature (`MISO=0`, `command RX=0x00`,
`WHO_AM_I=0x00`), because asserting the active-low nCS could simultaneously
pull SDO/MISO low. After removing the short, ACC3 still did not return to
normal operation.

During subsequent testing, ACC5 became operational for a brief instant and
then immediately stopped working again. The stable devices remained ACC1,
ACC2, ACC4, ACC7, and ACC8; ACC3, ACC6, and ACC9 remained unavailable, while
ACC5 showed this new intermittent behaviour.

**Current interpretation:** repairing the ACC3 bridge removed one confirmed
fault but does not prove that its SDO continuity, supply pads, LGA solder
joints, or sensor survived the original short. ACC5's momentary response is
strong evidence for an intermittent connection, supply, or solder-joint
problem, although a single `0x33` must be confirmed by repeated WHO_AM_I and
configuration-register reads. The current ACC firmware also caches a sensor
as ready after one successful initialisation and does not periodically
revalidate WHO_AM_I, so a brief contact can leave a misleading displayed state
after the connection is lost again.
# ACC3 result after SDO-nCS short repair (2026-08-03)

A fresh ACC summary after repairing the ACC3 SDO-nCS bridge changed the ACC3
signature from all-low to all-high: `MISO=1`, `command RX=0xFF`, and
`WHO_AM_I=0xFF`. This confirms that the selection-dependent low clamp was
removed successfully. ACC3 is still not communicating, but it now behaves as
an absent or non-driving SPI slave rather than a line shorted low. ACC5, ACC6,
and ACC9 also reported the same `0xFF` / `BAD_ID` signature. ACC1, ACC2, ACC4,
ACC7, and ACC8 continued to return `0x33` with `CTRL1=0x57` and `CTRL4=0x88`.

**Updated conclusion:** the confirmed ACC3 bridge repair was effective. Any
remaining ACC3 fault is now in device presence, power/VDD_IO, SCK/SDI arrival
at the actual package connection, SDO continuity, LGA soldering, or the sensor
itself; the former low-bus short is no longer present.
# ACC5 cached-ready / live-data-loss evidence (2026-08-03)

The latest live summary reported ACC5 as `ID=0x33`, `status=OK`,
`CTRL1=0x57`, and `CTRL4=0x88`, but its current raw axis bytes were
`FF/FF/FF/FF/FF/FF` and X/Y/Z all decoded to `-1`. This is direct evidence
that ACC5 successfully completed WHO_AM_I and register configuration at least
once, after which its live SDO/MISO data path was lost or the device stopped
responding.

The apparent `OK` state is a firmware limitation rather than proof of current
health. WHO_AM_I and CTRL values are cached after initialisation. A master SPI
transaction still returns `HAL_OK` when no slave drives MISO, so the firmware
accepts six pulled-high bytes as a successful axis read. Each `0xFFFF` axis
word shifts to signed value `-1`, producing the exact displayed result. The
firmware currently does not periodically revalidate a ready device or reject
all-`0xFF` axis frames.

**Interpretation:** ACC5 has an intermittent hardware connection, supply, or
LGA solder issue unless it is physically absent, in which case the displayed
ID/configuration are necessarily cached. The transport status must be fixed to
detect all-`0xFF`/all-`0x00` reads and periodically repeat WHO_AM_I/configuration
checks before `OK` can mean currently operational.
# ACC 3D GUI left-right mirror (2026-08-03)

The default `live_acc_3d.py` display was mirrored left-to-right. Both sensor
positions and the displayed X component of each acceleration vector are
reflected, while ACC IDs remain bound to their original data records. The
built-in GUI self-test passed after the change.
# ACC3/ACC8 anomalous values and transport-corruption evidence (2026-08-03)

The latest summary showed major recovery: ACC3 and ACC5 both returned
`WHO_AM_I=0x33` with `CTRL1=0x57` and `CTRL4=0x88`; only ACC6 and ACC9
remained `0xFF/BAD_ID`. However, the same capture contained values that cannot
all be valid sensor data:

- ACC3 `MISO` history contained both `1` and `1793`. The firmware field is a
  single GPIO sample and can only be 0 or 1, so `1793` proves at least one
  16-bit transport word was corrupted after frame construction.
- ACC3 Y remained at `2040 mg`, close to positive full-scale saturation, while
  Z remained approximately `984..997 mg`; this gives an impossible stationary
  magnitude above 2 g and may be either corrupted transport data or a damaged/
  stressed sensor axis.
- ACC8 reported X=`0`, Y=`254`, and Z=`-253 mg`, giving only about 0.36 g total
  magnitude. This is also inconsistent with a stationary, correctly configured
  accelerometer and requires validation before being treated as physical data.

The PC frame reader verifies frame boundaries, consecutive sequence numbers,
and that every word is at most 12 bits, but the current 515-byte protocol has
no checksum/CRC. A corrupted word such as 1793 is still within 0..4095 and is
therefore accepted. Consequently, the impossible MISO value is direct evidence
that the STM32-to-Teensy/USB transport can corrupt individual payload words
without losing frame lock.

**Current interpretation:** communication recovery of ACC3 and ACC5 is real at
the identity/configuration level, but ACC3 and ACC8 axis anomalies cannot yet
be assigned solely to LGA/MEMS damage. First add end-to-end frame integrity
(CRC or checksum), reject impossible diagnostic fields, and repeat raw captures.
If the same axes remain abnormal in CRC-valid frames, then investigate package
stress, LGA soldering, supply/decoupling, or sensor damage.

# Combined FSR1 + FSR2 + ACC application (2026-08-04)

A new non-destructive application was created at
`stm32/applications/combined_system`; the existing `fsr1`, `fsr2`, and
`acc_array` applications were retained unchanged. One acquisition cycle now:

1. scans all 16 MUX1 positions and all 16 ADC1 channels for FSR1;
2. scans reversed MUX2 and ADC2 using the verified FSR2 transpose mapping;
3. revalidates and reads all nine LIS2DH12 accelerometers; and
4. sends one versioned 1188-byte frame to Teensy through Host SPI3.

The new `ESK1` frame includes flags, a 32-bit sequence number, the STM32 tick,
both 256-word FSR matrices, nine 16-byte ACC records, and an IEEE CRC32. This
directly addresses the earlier unprotected 515-byte protocol in which a corrupt
but numerically valid word such as the impossible `MISO=1793` was accepted.

ACC live validation was also corrected: `WHO_AM_I` is reread in every frame,
all-`0x00` and all-`0xFF` axis blocks are rejected, and one failed sensor is
retried each second. A disconnected device can no longer retain a cached OK
state indefinitely.

The STM32 cross-build completed successfully on 2026-08-04. Result:
`ESKIN_STM32.elf`, Flash 19,788 bytes (3.77%), RAM 5,472 bytes (4.17%). The only
compiler warnings are unused legacy static functions inherited from the copied
ACC CubeMX application; the new combined module itself compiled without error.
A prebuilt ELF was saved under `combined_system/prebuilt`.

A matching Teensy application and GUI were created at
`teensy/applications/ESKIN_COMBINED_BRIDGE`. The bridge validates magic, version,
declared length, and CRC before forwarding a frame to USB. The Python monitor
shows FSR1 and FSR2 as separate 16 x 16 heatmaps and the nine accelerometers in
one mirrored 3D view. Its synthetic parse/CRC self-test passed, including a
deliberately corrupted frame that was correctly rejected. Teensy compilation
was not executed because no Arduino CLI executable was found; upload through
Arduino IDE with the Teensy 4.1 board package.

## Combined flash-script path fix and SWD result (2026-08-04)

The first `flash_combined.cmd` run failed because `%~dp0` ends with a backslash.
When the long project path was placed inside the CMake `-S` quotes, Windows argv
parsing consumed the closing quote and split the later toolchain argument near
the path's ` - ` characters. `SOURCE` was changed to `%~dp0.` and the toolchain
path now inserts an explicit separator. The exact user command then configured
and rebuilt all 24 objects successfully, proving the CMake path issue fixed.

Flashing subsequently failed at the separate SWD stage with
`ResetHardwareAssert: expected DAP_SWJ_PINS`. A 10 kHz `halt` fallback also
timed out without attaching to the STM32. The generated ELF is valid; the
remaining blocker is the current DAPLink/SWD/nRESET target connection or probe
state, not compilation. The two pyOCD processes left after the timeout were
stopped. `-W/--no-wait` was added to future flash/reset commands so a missing or
busy probe returns an error instead of indefinitely blocking the terminal.

## DAPLink USB endpoint hang after successful combined build (2026-08-04)

A later run correctly reported `ninja: no work to do`, meaning the combined ELF
was already current. It then stopped producing output because pyOCD remained
blocked. The exact pyOCD/Python processes were confirmed and terminated.
`pyocd list --probes --verbose` also timed out, so the failure occurs even
before connecting to the STM32 target.

Windows still enumerated the probe as the started USB composite device
`USB\VID_C251&PID_F001\LU_2022_8888`, with COM10 and CMSIS-DAP HID interfaces.
Selecting `LU_2022_8888` explicitly did not remove the timeout. This isolates
the current fault to a wedged jixin CMSIS-DAP HID endpoint/driver/firmware state,
rather than CMake, the ELF, probe selection, or the STM32 application. A Windows
PnP restart was attempted but denied because the session was not elevated.
Physically unplugging the DAPLink USB cable for several seconds, reconnecting it
(preferably to another USB port), and then retesting `pyocd list --probes` is
required before another flash attempt. A clear `[3/3] Flashing...` marker was
added to the script so future runs visibly distinguish build completion from
the pyOCD stage.

# Central command organization (2026-08-04)

All active Windows `.cmd` launchers were consolidated under
`firmware/tools/commands`. Duplicate `flash_selected_acc.cmd` names were replaced
by the unambiguous `flash_acc_array.cmd` and `flash_acc_spi_test.cmd`. Central
wrappers now resolve their STM32/Teensy projects from the firmware root, while
the implementation `.ps1` files remain beside their source projects.

`docs/COMMAND_REFERENCE.md` was added as the concise operational source of
truth. It covers the combined system, standalone FSR/ACC firmware, diagnostic
burn commands, Teensy sketches, every argparse flag and default, CMake/pyOCD
flags, expected test behaviour, and fast fault isolation. Active project
READMEs were updated to point to the centralized commands. A generic
`flash_stm32_project.cmd` provides safe aliases for FSR1, FSR2, FSR1 ADC,
ACC WHO_AM_I, and SPI-pattern firmware. The combined CMake project no longer
advertises the inherited `ACC_SELECTED` cache setting because combined mode
always scans all nine accelerometers.

## FSR2 build success and invalid SWD ACK (2026-08-04)

The centralized command `flash_stm32_project.cmd fsr2` configured and linked
FSR2 successfully (Flash 16,848 bytes; RAM 3,392 bytes). The yellow
`CMAKE_TOOLCHAIN_FILE` unused warning came from reusing an already-configured
CMake build cache and did not affect the ELF. Central combined/generic scripts
were changed to `cmake --fresh` so the toolchain is consumed during a clean
configuration and the misleading cache warning is removed.

The subsequent flash failed with `SWD/JTAG communication failure (Unexpected
ACK '5')`. SWD ACK values are encoded responses and `5` is not a valid single
OK/WAIT/FAULT response; it indicates a corrupted sampled response or unstable
debug connection. Given the already confirmed cable-contact fault and the
10 kHz clock already in use, the primary action is to reseat and mechanically
stabilize SWDIO, SWCLK, GND, target reference, and nRESET before retrying. This
failure occurred after a valid ELF build and is not an FSR2 code error.

## Combined Host SPI 500 kHz trial (2026-08-04)

The Teensy combined bridge Host SPI clock was increased from 100 kHz to
500 kHz. STM32 SPI3 remains a Mode-0 hardware-NSS slave and requires no baud
setting change. The 1188-byte frame wire time falls from approximately 95 ms to
19 ms. End-to-end CRC32 remains mandatory: a frame with any high-speed link
corruption is discarded by Teensy before USB transmission. The expected total
rate is now governed mainly by the two FSR scans and ACC transactions rather
than Host SPI, and should increase from roughly 5 Hz toward 10-15 Hz depending
on ADC/ACC response and failed-device retries. Arduino IDE upload is required
before the new speed is active on hardware.

## Combined flash reached erase but CMSIS-DAP transfer failed (2026-08-04)

After physically restoring the probe connection, the combined script found the
DAPLink, loaded the correct ELF, and began sector erase. The first attempt then
reported `cannot write registers pc, r0, lr because core #0 is not halted`.
The remaining pyOCD/Python processes were stopped and the command was retried
with explicit probe UID `LU_2022_8888`, 10 kHz SWD, `under-reset`, and
`reset_type=hw`.

The explicit hardware-reset attempt passed the earlier core-halt point and
again entered erase, but failed with `DAP_TRANSFER response error: response is
for command 00` during board cleanup/disconnect. This is a malformed or
out-of-sequence CMSIS-DAP HID response, indicating instability in the jixin
probe firmware, its USB cable/port, or the Windows HID endpoint rather than a
compile/link error. No DAPLink mass-storage drive was exposed, so drag-and-drop
flashing is unavailable. Because sector erase had already started, the previous
STM32 application may now be partially erased and must not be expected to run
until a complete flash succeeds. The script now permanently selects this probe
UID and requests explicit hardware reset; the next attempt should follow a full
physical USB power cycle (and preferably another USB cable/port).

### Confirmed root cause and resolution

The user found that one of the DAPLink/SWD cable connections had poor physical
contact. Reseating the cable restored the connection. This confirms that the
earlier intermittent probe enumeration, failure to halt the Cortex-M4, and
malformed `DAP_TRANSFER` response were all consequences of an unstable physical
debug connection rather than the combined firmware, CMake build, CMSIS-Pack, or
pyOCD command configuration. The cable/header connection should be treated as
the first inspection point if the same symptoms return.
# 2026-08-04 — Selectable combined GUI and restored physical layouts

- Updated `live_combined_monitor.py` so the operator can select **All**,
  **FSR1**, **FSR2**, or **ACC** while the stream remains open.
- Added `--view all|fsr1|fsr2|acc`; `start_combined_monitor.cmd` now accepts
  the same value as its optional second argument.
- Replaced the combined monitor's rectangular FSR images with the established
  hexagonal PCB cell geometry. Both FSR views now use the proven transport
  transpose and physical left-right mirror, labelled `C16..C1` horizontally
  and `R1..R16` vertically.
- Restored the ACC monitor's established 3 x 3 mirrored physical placement,
  3D view angle, axis directions, validity marks, and acceleration vectors.
- Kept the combined `ESK1` parser and CRC32 validation unchanged. Added a GUI
  mapping assertion to the self-test and verified all four view transitions
  by offline rendering.
# 2026-08-05 — Combined acquisition first-stage speed increase

Objective: raise the complete FSR1 + FSR2 + nine-ACC source rate well above the
measured approximately 9 frame/s without changing the 1188-byte `ESK1` protocol.

## Implemented changes

| Path | Previous implementation | New implementation | Expected saving |
|---|---|---|---:|
| FSR MUX settling | 32 separate `HAL_Delay(1)` calls per frame | DWT cycle-counter delay, 100 us per shared MUX address | approximately 30 ms/frame |
| FSR scanning | complete FSR1 scan followed by complete FSR2 scan | both independent MUX enables active for one shared address; read ADC1 then ADC2 | 16 settle events instead of 32 |
| ACC SPI2 | 62.5 kHz, prescaler `/256` | 500 kHz, prescaler `/32` | approximately 8x ACC bus speed |
| ACC `WHO_AM_I` | all ready ACCs checked every frame | one ACC health/recovery slot every 100 ms; all nine checked in approximately 0.9 s | removes repeated identity traffic |
| Host SPI | 500 kHz | 1 MHz | 19.0 ms to approximately 9.5 ms/frame |
| Teensy handshake | IRQ settle 1000 us, CS setup 1000 us, CS hold 100 us | 100 us, 100 us, 50 us | approximately 1.85 ms/frame |

FSR2 keeps the established reverse-MUX/transposed logical storage. CRC32, frame
length, magic, timestamps, flags, and all GUI decoding remain unchanged.

## Verification before formal integration

- Arm GNU Debug build: PASS.
- Linked image: FLASH 19,872 B / 512 KiB; RAM 5,472 B / 128 KiB.
- Combined protocol and GUI mapping self-test: PASS.
- Synthetic old-versus-new FSR1/FSR2 logical mapping equivalence: PASS.
- The first attempted full build under the long OneDrive path failed only
  because Windows exceeded the object-file path limit. Repeating the identical
  build in a short system temporary path linked successfully.
- Arduino CLI is not installed in the command-line environment; upload the
  revised Teensy sketch with Arduino IDE/Teensyduino before hardware testing.

## Hardware acceptance sequence

1. Upload the revised Teensy bridge, then flash the revised STM32 combined ELF.
2. Start the combined GUI and record `source fps`, `FSR flags`, `ACC x/9`, and
   whether `CRC OK` remains continuous for at least 60 s.
3. Press isolated cells near the corners and centre of both FSRs. Reject this
   speed if the response moves to an adjacent row/column, becomes unstable, or
   retains the preceding MUX value.
4. Move the board and confirm every currently healthy ACC changes plausibly.
5. If unstable, fall back in this order: Host SPI 500 kHz; ACC SPI 250 kHz;
   FSR settle 200 us, then 500 us; Teensy waits 1000/1000/100 us.

## Further stepwise values after the first stage passes

| Phase | FSR settle | ACC SPI | Host SPI | Teensy IRQ/CS/hold | Expected complete source rate |
|---|---:|---:|---:|---:|---:|
| Current verified-by-build stage | 100 us | 500 kHz | 1 MHz | 100/100/50 us | approximately 25-50 Hz |
| Next conservative test | 50 us | 1 MHz | 2 MHz | 50/50/20 us | approximately 40-80 Hz |
| Aggressive blocking test | 25 us | 2 MHz | 4 MHz | 20/20/10 us | hardware-dependent; validate CRC and analogue settling |
| DMA/double-buffer phase | measured minimum | 2-5 MHz | 4-8 MHz | measured minimum | approximately 80-150 Hz target |

The final phase also allows increasing the STM32 system clock from the present
16 MHz to 80 MHz first, then up to the device-specific validated maximum. All
SPI prescalers, ADC timing, DWT delay conversion, power, and signal integrity
must be revalidated after a system-clock change. Never use GUI `display fps` to
judge the acquisition result; use `source fps` based on STM32 sequence/timestamp.
# 2026-08-05 — No-valid-frame LED after first Host-link speed trial

Observed after correctly flashing the paired combined firmware: COM9 was
present and opened by the GUI, but the GUI remained on `Waiting for combined
frames` and the Teensy status LED stayed off. Connections were reported correct.

The existing LED expression becomes true only when all of the following pass:
STM32 IRQ detected, 1188 bytes clocked, `ESK1` magic/version/length valid, CRC32
valid, and USB `Serial.write()` accepts the full frame. Therefore LED off does
not by itself distinguish no IRQ from a corrupted Host SPI frame.

Isolation action: retain all STM32-side speed work (100 us shared FSR settle,
500 kHz ACC SPI, periodic ACC health checks), but restore the Teensy Host link
to the last known stable values:

| Parameter | Failed trial | Isolation fallback |
|---|---:|---:|
| Host SPI | 1 MHz | 500 kHz |
| IRQ settle | 100 us | 1000 us |
| CS setup | 100 us | 1000 us |
| CS hold | 50 us | 100 us |

Expected interpretation after uploading the fallback Teensy sketch:

- LED on and GUI frames return: Host SPI clock/setup margin caused the failure.
- LED still off, Teensy GPIO2 always 0 V: STM32 does not reach/assert HOST_IRQ;
  investigate STM32 execution before `send_frame()`.
- LED still off, GPIO2 goes high or pulses: Host transfer occurs but magic/CRC
  is invalid; retain 500 kHz and instrument the bridge error category.
- GPIO2 remains high continuously: STM32 is waiting in the blocking SPI3 slave
  transfer and Teensy did not recognize/clock the request.

Do not change STM32 sampling parameters during this isolation test. After the
link is restored, test Host timing in this order: 500 kHz with 100/100/50 us,
then 1 MHz with 1000/1000/100 us, then shorten the 1 MHz waits.
# 2026-08-05 — Teensy onboard LED is shared with Host SPI SCK

After the 500 kHz Host-link fallback, the onboard Teensy LED remained generally
off but flashed periodically. Code inspection identified that Teensy 4.1
`LED_BUILTIN` is GPIO13 and the default SPI SCK is also GPIO13. The observed
flash is therefore SPI clock activity, not a valid/invalid frame indication.
It proves that at least some Host SPI transactions are being clocked, but it
does not prove magic, header, CRC, or USB success.

Correction:

- Removed `pinMode(LED_BUILTIN, OUTPUT)` and all status `digitalWrite()` calls.
- Kept GPIO13 exclusively under the SPI peripheral.
- Added cumulative result categories and a once-per-second `#ESKDBG` USB line:
  IRQ count, valid frames, bad magic, bad header, bad CRC, USB unavailable,
  USB short write, IRQ-release timeout, and current IRQ level.
- The binary `ESK1` protocol is unchanged. The GUI synchronizer ignores the
  diagnostic text while searching for the next `ESK1` magic.

Diagnosis procedure: close the GUI, open Arduino Serial Monitor, and copy one
complete `#ESKDBG` line. Rising `irq` with rising `magic` indicates byte/frame
alignment or wrong data; rising `crc` indicates signal/timing corruption;
`release_timeout` indicates STM32 HOST_IRQ did not drop after the transaction;
an unchanged `irq=0` indicates STM32 never asserted or Teensy never detected
GPIO2.
# 2026-08-05 — Host SPI diagnostic result: every transaction fails magic

Observed diagnostic counters:

```text
irq=2..5, ok=0, magic=2..5, header=0, crc=0,
usb_off=0, usb_short=0, release_timeout=0, irq_level=0
```

Interpretation: Teensy detects each STM32 HOST_IRQ and completes/release-handshakes
the transfer, but all received frames fail before header, CRC, or USB checks
because bytes 0..3 are not `45 53 4B 31` (`ESK1`). This excludes the Python GUI,
USB port ownership, CRC calculation, and post-transfer IRQ release. Remaining
classes are MISO not driven (usually all `00` or all `FF`), SPI mode/NSS timing,
or a leading-byte/frame alignment offset.

Diagnostic update: added a second once-per-second line, `#ESKFIRST`, containing
the first 16 bytes actually received by Teensy. Interpret the result as follows:

- `00 ... 00`: MISO held low, STM32 SPI3 not driving, or wrong MISO route.
- `FF ... FF`: MISO floating/high, STM32 not selected, or wrong MISO route.
- `45 53 4B 31 ...` at a later position: byte offset caused by CS/NSS/arming.
- changing random bytes: signal integrity, clock phase/polarity, or contention.
# 2026-08-05 — Valid ESK1 header but every long frame fails CRC

New diagnostic output showed stable prefixes such as:

```text
45 53 4B 31 01 07 A4 04 ...
```

`45 53 4B 31` is the correct `ESK1` magic, version is `01`, flags are `07`,
and little-endian length `A4 04` is 1188 bytes. Counters showed `magic=0`,
`header=0`, `crc` increasing with every IRQ, and `ok=0`. Therefore GPIO2 IRQ,
CS/NSS frame start, SPI mode, initial MISO bytes, length, and USB availability
are working. Corruption or disagreement occurs within the remaining long frame
or its final four CRC bytes.

Next controlled isolation:

- Reduce only Teensy Host SPI from 500 kHz to the original 100 kHz value.
- Retain 1000/1000/100 us IRQ/CS/hold waits.
- Add `#ESKCRC wire=XXXXXXXX calc=XXXXXXXX spi_hz=100000`, where `wire` is
  the STM32 CRC carried in the final four bytes and `calc` is Teensy's CRC32 of
  the received preceding 1184 bytes.
- Leave all STM32 acquisition changes untouched.

Interpretation: `ok` increasing at 100 kHz proves long-frame timing/signal
integrity at 500 kHz is inadequate under the current firmware/hardware state.
CRC still failing at 100 kHz, especially with stable but different CRC values,
requires comparison against STM32 `pack_frame()` and a captured raw frame.
# 2026-08-05 — Root cause isolated: fixed two-byte Host SPI prefix

At 100 kHz the raw first bytes repeatedly showed:

```text
92 BC | 45 53 4B 31 ...
26 2D | 45 53 4B 31 ...
69 4D | 45 53 4B 31 ...
39 0F | 45 53 4B 31 ...
```

The valid `ESK1` magic was consistently at byte offset 2. The old Teensy loop
clocked exactly 1188 bytes, so it captured two stale prefix bytes plus only the
first 1186 bytes of the logical frame, necessarily omitting the final two CRC
bytes. The displayed zero CRC values were not measurements: validation returned
at bad magic before assigning/calculating CRC.

Implemented correction on both sides:

- STM32 `send_frame()` now aborts and reinitializes SPI3 before asserting
  HOST_IRQ, clearing stale slave TX/RX/FIFO state before every transaction.
- Teensy keeps nCS low while searching the first eight clocks for `ESK1`.
  When found, it discards only the prefix and clocks the remaining bytes needed
  to obtain one complete 1188-byte logical frame.
- Added `#ESKALIGN discarded_prefix=N` to confirm whether STM32 reinitialization
  removes the offset (`N=0`) or Teensy recovery is still required (`N=2`).
- Host SPI remains at 100 kHz for this root-cause verification. Do not raise
  speed until `ok` increments and `wire == calc` continuously.

Acceptance result to capture after paired reflash/upload: `magic=0`, `crc=0`,
`ok` increasing, matching wire/calculated CRC, and stable alignment count.

# 2026-08-10 — Controlled Host SPI increase to 500 kHz

The Teensy combined bridge Host SPI clock was increased from 100 kHz to
500 kHz. The IRQ settle, CS setup, and CS hold delays remain unchanged at
1000/1000/100 us, so this test changes only the wire clock. STM32 SPI3 remains
an externally clocked Mode-0 slave and does not require reflashing for this
step. One 1188-byte frame now has a wire time of approximately 19.0 ms and a
wire-only ceiling of approximately 52.6 frames/s.

Upload command:

```powershell
& "D:\study\programming\ESKIN\firmware\tools\commands\upload_teensy_sketch.cmd" `
  "D:\study\programming\ESKIN\firmware\teensy\applications\ESKIN_COMBINED_BRIDGE" `
  COM9 TEENSY_COMBINED_BRIDGE
```

Acceptance criteria are continuously increasing `ok`, `magic=0`, `header=0`,
`crc=0`, `release_timeout=0`, matching `wire`/`calc` CRC values, no sequence
gaps in the GUI, and a stable alignment count. If any criterion fails, restore
`SPI_HZ=100000` before changing acquisition or handshake timing.

500 kHz runtime result: **FAIL**. During a six-second DTR-enabled COM9 capture,
the cumulative counters changed from approximately `ok=14, crc=695` to
`ok=63, crc=732`: about 49 accepted frames and 37 new CRC failures. Magic
remained zero, alignment remained `discarded_prefix=0`, IRQ released normally,
and individual frames alternated between matching and mismatched wire/calculated
CRC values. This localises the failure to intermittent corruption within the
long frame rather than frame-start alignment. The active test setting was
therefore reduced to 250 kHz with all waits unchanged.

At 250 kHz, one 1188-byte frame takes approximately 38.0 ms on the wire and has
a wire-only ceiling of approximately 26.3 frames/s. Apply the same acceptance
criteria before retaining it; otherwise restore the verified 100 kHz fallback.

250 kHz runtime result: **FAIL**. A ten-second DTR-enabled COM9 capture initially
showed accepted frames, then CRC failures became continuous and the transfer
rate degraded to approximately one transaction per second. The observed
counters progressed from `irq=310, ok=0, crc=116` through
`irq=331, ok=15, crc=122`, then to `irq=336, ok=15, crc=127`. Throughout the
failure, `magic=0`, `header=0`, `release_timeout=0`, and
`discarded_prefix=0`; wire/calculated CRC values diverged on the failed frames.
This setting was rejected and the active bridge was restored to the 100 kHz
fallback with 1000/1000/100 us IRQ/CS/hold waits.

Final 100 kHz recovery result: **still intermittent CRC failure**. The combined
STM32 image rebuilt successfully (19,884 bytes Flash, 5,472 bytes RAM), pyOCD
confirmed all 20 programmed pages were identical, and a hardware reset
completed. The matching 100 kHz Teensy bridge was also rebuilt and uploaded.
During the following ten-second capture, counters progressed from approximately
`irq=379, ok=31, crc=175` to `irq=517, ok=128, crc=216`: about 97 newly accepted
frames and 41 new CRC failures. `magic=0`, `header=0`, `release_timeout=0`, and
`discarded_prefix=0` remained stable. Therefore Host clock alone is not the
root cause; current evidence points to corruption within the 1188-byte transfer
or the STM32/physical SPI path. Leave the device at 100 kHz, but do not mark the
current long-frame acceptance as passed.

# 2026-08-10 — Host SPI low-frequency CRC isolation

Objective: determine whether the intermittent 1188-byte CRC failures depend on
Host SPI clock frequency. Change only the Teensy SCK setting; keep the STM32
image, Mode 0, 1188-byte `ESK1` protocol, CRC32 implementation, and
1000/1000/100 us IRQ/CS/hold waits unchanged.

First isolation point: reduce Host SPI from 100 kHz to 50 kHz. One frame takes
approximately 190.1 ms on the wire, for a wire-only ceiling of approximately
5.26 frames/s. A new capture utility saves every test instead of retaining data
only in terminal memory:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File `
  "D:\study\programming\ESKIN\firmware\tools\capture_combined_diagnostics.ps1" `
  -Port COM9 -DurationSeconds 20 -Label host_spi_50khz
```

Outputs are written under `docs\test_results`:

- `.bin`: complete raw USB serial capture, including accepted binary frames and
  ASCII diagnostics;
- `.txt`: test parameters, counter deltas, acceptance/CRC percentages, transfer
  rate, and extracted `#ESK...` lines.

Decision rule: if 50 kHz materially lowers the CRC error rate relative to the
approximately 29.7% 100 kHz sample, frequency/signal timing is involved. If it
does not, test 25 kHz; persistent errors at both lower rates make a pure SCK
frequency limit unlikely and shift priority to STM32 SPI3 FIFO/HAL state,
buffer integrity, or a physical intermittent connection.

The first 50 kHz hardware-SPI capture is **invalid as a frequency comparison**.
Although firmware reported `spi_hz=50000`, 20 seconds produced 362 transactions
(18.1/s), which is physically impossible for 1188 bytes at 50 kHz. Inspection
of Teensyduino 1.62.0 `SPI.h` showed that Teensy 4.x `beginTransaction()` limits
the LPSPI clock divisor to 257. Requests below the resulting hardware minimum
are therefore clamped rather than generated exactly. The capture is retained at
`docs/test_results/20260810_140929_host_spi_50khz.*` for audit, but its 39.503%
CRC result must not be labelled an actual 50 kHz result.

For the controlled low-frequency test only, the bridge now uses GPIO11 MOSI,
GPIO12 MISO, and GPIO13 SCK as a software Mode-0 SPI master with a 10 us
half-period. This generates approximately 50 kHz independently of the LPSPI
divisor clamp. CS, IRQ, frame parsing, CRC, and all handshake delays are
unchanged. Diagnostics identify this build with `soft_spi=1`.

Valid software-SPI 50 kHz result: **preliminary PASS**. The saved 20-second
capture contained 85 measured transactions, all 85 accepted, with zero CRC,
magic, header, or IRQ-release errors. Measured transaction rate was 4.25/s,
which is physically plausible for a 1188-byte frame at approximately 50 kHz.
Every sampled `wire` and `calc` CRC matched, and diagnostics confirmed
`spi_hz=50000 soft_spi=1` and `discarded_prefix=0`.

Saved evidence:

- `docs/test_results/20260810_141218_host_spi_50khz_soft.bin`
- `docs/test_results/20260810_141218_host_spi_50khz_soft.txt`

Compared with the approximately 29.7% CRC error observed in the earlier
hardware-SPI run labelled 100 kHz, this strongly associates the failures with
Host SCK timing/signal integrity. It is not yet proof of frequency alone because
software SPI also changes GPIO edge shape and the sampling instant. Retain the
50 kHz software test build while collecting a longer run; then test controlled
software-SPI steps such as 75 and 100 kHz to locate the boundary.

## Controlled software-SPI 100 kHz point

After the valid 50 kHz result, change only `SPI_HZ` from 50000 to 100000 while
retaining `USE_SOFTWARE_SPI=true`, Mode 0, the 1188-byte frame, CRC32, and
1000/1000/100 us IRQ/CS/hold waits. The software half-period changes from 10 us
to 5 us. The expected wire time is approximately 95.0 ms per frame and the
wire-only ceiling is approximately 10.5 frames/s. Save the test as
`host_spi_100khz_soft` and compare its CRC error rate directly with the saved
50 kHz software result.

100 kHz software-SPI result: **CRC PASS, complete-system FAIL**. The saved
20-second capture contained 13 measured transactions and all 13 were accepted:
`delta_crc=0`, `delta_magic=0`, `delta_header=0`, and
`delta_release_timeout=0`. Diagnostics confirmed `spi_hz=100000 soft_spi=1`,
so the observed CRC error rate was 0%.

However, the measured transaction rate was only 0.65/s. Consecutive STM32 frame
timestamps increased by approximately 1556 ms, closely matching
`HOST_TIMEOUT_MS=1500`. Teensy therefore received a complete CRC-valid frame,
but STM32's blocking `HAL_SPI_TransmitReceive()` did not finish normally and
waited for its timeout/recovery path before producing the next frame. This point
is rejected for normal operation even though its received CRC acceptance was
100%.

Saved evidence:

- `docs/test_results/20260810_142110_host_spi_100khz_soft.bin`
- `docs/test_results/20260810_142110_host_spi_100khz_soft.txt`

Interpretation: raw SCK frequency alone does not explain the earlier CRC
corruption, because software Mode-0 at 100 kHz delivered CRC-clean frames. The
hardware-LPSPI edge/sampling behaviour is implicated in the CRC errors, while
the STM32 full-duplex polling completion path is separately implicated in the
software-100-kHz throughput collapse. Restore the active bridge to the
50 kHz software-SPI point, which passed both CRC and throughput acceptance.

# 2026-08-10 — STM32 SPI3 full-duplex DMA stage 1

The STM32 Host SPI3 transport was changed from blocking CPU polling to
full-duplex DMA. DMA1 Channel1 uses the `SPI3_RX` DMAMUX request to drain all
Teensy MOSI dummy bytes into `host_rx`; DMA1 Channel2 uses the `SPI3_TX` request
to feed the 1188-byte `ESK1` frame from `host_tx` to MISO. Both channels use
byte alignment, incrementing memory, fixed peripheral address, normal mode,
and very-high priority. DMA and SPI3 error interrupts are enabled.

The ordering is deliberately:

1. abort/reinitialize SPI3 to clear stale state;
2. arm `HAL_SPI_TransmitReceive_DMA()` for both directions;
3. assert `HOST_IRQ` only after DMA is ready;
4. let Teensy assert NSS and supply SCK;
5. complete through `HAL_SPI_TxRxCpltCallback()` or recover through the DMA/SPI
   error and timeout paths;
6. lower `HOST_IRQ` and reuse the buffers only after completion.

This is DMA stage 1: it removes CPU FIFO polling but intentionally waits for DMA
completion before starting the next acquisition. It therefore validates FIFO,
IRQ, timeout, alignment, and CRC behaviour without simultaneously introducing
double-buffer ownership. Stage 2 will add two frame buffers so CPU acquisition
of frame N+1 can overlap DMA transmission of frame N.

Initial validation target remains the accepted Teensy software Mode-0 50 kHz
configuration. Required result: increasing DMA completion count, no DMA errors
or timeouts, `crc=0`, `release_timeout=0`, `discarded_prefix=0`, and normal
approximately 4–5 frame/s throughput before testing higher SCK rates.

## Full-duplex DMA hardware validation

Build and flash completed successfully. The DMA image uses 24,300 bytes Flash
and 5,672 bytes RAM. PyOCD erased/programmed 24 pages and performed a hardware
reset.

At the initial software-SPI 50 kHz point, the saved 20-second capture reported:

```text
delta_irq=87
delta_ok=87
delta_crc=0
acceptance_percent=100
crc_error_percent=0
transfer_rate_hz=4.35
```

Evidence:

- `docs/test_results/20260810_145656_host_spi_50khz_soft_stm32_dma.bin`
- `docs/test_results/20260810_145656_host_spi_50khz_soft_stm32_dma.txt`

The same DMA image was then tested with a true software Mode-0 100 kHz SCK. The
saved 20-second result was:

```text
delta_irq=147
delta_ok=147
delta_crc=0
delta_magic=0
delta_header=0
delta_release_timeout=0
acceptance_percent=100
crc_error_percent=0
transfer_rate_hz=7.35
```

Evidence:

- `docs/test_results/20260810_145806_host_spi_100khz_soft_stm32_dma.bin`
- `docs/test_results/20260810_145806_host_spi_100khz_soft_stm32_dma.txt`

This replaces the pre-DMA 100 kHz result of 0.65 frame/s and approximately
1556 ms between STM32 timestamps. Full-duplex DMA therefore removed the primary
blocking-completion timeout at this clock while preserving zero CRC errors.
The active accepted configuration is now software Mode-0 100 kHz on Teensy and
full-duplex SPI3 DMA on STM32. It is still stage 1 (DMA completion is awaited);
double buffering and hardware-SPI frequency increases remain separate steps.

# 2026-08-10 — STM32 DMA software ping-pong buffers

Stage 2 introduces two independent 1188-byte TX buffers and two matching RX
dummy buffers. `host_send_index` is owned exclusively by SPI3 DMA, while the CPU
scans FSR1, FSR2, and all ACC devices and packs the next CRC-protected frame into
`host_fill_index`. After DMA completion, the indices swap. The CPU never writes
the buffer marked for transmission.

The first loop iteration primes one complete frame. Steady-state operation is:

```text
DMA sends frame N from buffer A
    concurrently with
CPU acquisition/CRC packing of frame N+1 into buffer B

DMA completion -> A becomes free -> swap A/B
```

DMA completion and error callbacks immediately lower `HOST_IRQ`, so Teensy does
not wait for the CPU acquisition running in parallel. If DMA start fails, the
ready frame is retained for retry. If an active transfer fails or times out, the
newly filled alternate frame becomes the next send candidate after SPI3
recovery. SPI3 reinitialization remains in the per-transfer start path for this
validation stage because it previously removed stale-prefix state; removing
that reinitialization is deferred until long-run alignment remains zero.

Expected 100 kHz result: wire time remains approximately 95 ms, but acquisition
and CRC work should overlap most of it. Complete source rate should rise from
7.35 fps toward the wire ceiling of 10.5 fps, with zero CRC, magic, header,
alignment, DMA, and IRQ-release errors.

## DMA ping-pong validation result

Build and flash passed. The ping-pong image uses 24,624 bytes Flash and 8,056
bytes RAM; the additional RAM is the expected second 1188-byte TX buffer and
second 1188-byte RX dummy buffer. PyOCD programmed the image and performed a
hardware reset.

The saved 20-second 100 kHz software-SPI capture reported:

```text
delta_irq=209
delta_ok=209
delta_crc=0
delta_magic=0
delta_header=0
delta_release_timeout=0
acceptance_percent=100
crc_error_percent=0
transfer_rate_hz=10.45
```

Independent parsing of the saved raw stream found 215 complete binary `ESK1`
frames, sequence 95 through 309, with zero sequence gaps. This confirms that
the throughput gain did not come from retransmitting an old buffer or
overwriting the DMA-owned buffer.

Evidence:

- `docs/test_results/20260810_151704_host_spi_100khz_soft_stm32_dma_pingpong.bin`
- `docs/test_results/20260810_151704_host_spi_100khz_soft_stm32_dma_pingpong.txt`

Result: **PASS**. Relative to DMA stage 1, throughput increased from 7.35 to
10.45 frame/s and reached 99.3% of the 100 kHz wire-only ceiling of 10.52
frame/s, while CRC and sequence integrity remained perfect for this short run.
The active configuration is now 100 kHz software Mode-0 Host SPI, STM32 SPI3
full-duplex DMA, and two software ping-pong frame buffers.

# 2026-08-10 — Raise Host SPI to the acquisition-matched point

The pre-overlap single-buffer measurements imply approximately 41 ms of FSR,
ACC, frame-pack, and CRC work per complete frame. A 1188-byte frame contains
9504 wire bits, so the Host clock that makes transport time approximately equal
to acquisition time is `9504 / 0.041`, or about 232 kHz. Select the standard
250 kHz test point rather than increasing directly to 500 kHz.

Only the Teensy software Mode-0 `SPI_HZ` changes from 100000 to 250000. STM32
full-duplex DMA, ping-pong ownership, 1188-byte protocol, CRC32, IRQ/CS/hold
waits, and all sensor acquisition settings remain unchanged. At nominal
250 kHz, wire time is 38.016 ms and the wire-only ceiling is 26.3 frame/s. The
expected complete rate is approximately 20–25 frame/s because acquisition now
becomes the similar or slower pipeline stage.

Acceptance requires zero CRC/magic/header/release errors, zero sequence gaps,
and a substantial increase over the accepted 10.45 frame/s at 100 kHz. If it
fails, restore the accepted 100 kHz DMA ping-pong configuration.

The first two 250 kHz captures each showed zero SPI/CRC/header/IRQ errors and
21.3 transactions/s, but exactly one `USB short write` immediately after DTR
opened. Each capture therefore reported 425 accepted frames out of 426 IRQs;
the first raw stream contained one matching sequence gap. The short write was a
USB connection-start race, not Host SPI corruption: `crc`, `magic`, `header`,
and `release_timeout` remained unchanged, and all later frames were accepted.

The Teensy USB forwarding path now retries partial `Serial.write()` operations
until all 1188 bytes are queued or a 100 ms timeout expires. It never starts a
diagnostic/result decision after merely writing a prefix of a binary frame.
Revalidate 250 kHz after upload; acceptance now requires no new
`usb_short` count and zero binary sequence gaps as well as zero CRC errors.

The partial-write retry alone still observed exactly one 100 ms USB timeout
after each DTR connection, while all Host SPI/CRC counters remained clean. This
confirms a USB CDC endpoint-start race rather than sustained bandwidth pressure.
The bridge now tracks the disconnected-to-connected transition and waits 500 ms
before forwarding the first binary frame. Frames acquired during this settling
window are counted as `usb_off` and discarded whole; no partial frame enters the
USB stream. Diagnostic text may flow during the window to let the endpoint
become ready. The existing full-frame retry remains active after settling.

## 250 kHz final validation

With the 500 ms USB connection-settling window, the saved 20-second capture
reported 460 acquisition/Host transactions. Twelve complete frames were
deliberately classified `usb_off` during connection settling; after that, all
448 measured frames were accepted. There were no new `usb_short`, CRC, magic,
header, or IRQ-release errors.

```text
delta_irq=460
delta_ok=448
delta_usb_off=12 (intentional connection settling)
delta_usb_short=0
delta_crc=0
transaction_rate=23.0 Hz
USB output rate=22.4 frame/s
USB-ready acceptance=448/448 = 100%
```

Independent parsing found 457 complete binary frames, sequence 9610 through
10066, with zero sequence gaps. The differing binary/count window endpoints are
caused by diagnostics being sampled inside the raw-capture interval, not by
lost frames.

Evidence:

- `docs/test_results/20260810_152754_host_spi_250khz_soft_dma_pingpong_usb_settle.bin`
- `docs/test_results/20260810_152754_host_spi_250khz_soft_dma_pingpong_usb_settle.txt`

Result: **PASS**. The active configuration is nominal 250 kHz software Mode-0
Host SPI, STM32 full-duplex DMA, and ping-pong buffers. At 23.0 transactions/s,
one complete pipeline cycle is approximately 43.5 ms. The nominal Host wire
time is 38.0 ms, so sensor acquisition/packing is now the slightly slower stage.
Further Host-frequency increases alone should give little improvement until the
FSR/ACC phase timings are instrumented and reduced.

## 2026-08-10 documentation update and next optimisation flow

The measured progression, line chart, bilingual summary and full acceptance
criteria are consolidated in:

```text
docs/updates/2026-08-10/PROGRESS_UPDATE.md
```

The next work keeps the `ESK1` frame and CRC32 unchanged and changes only one
timing variable per experiment:

1. Instrument FSR1, FSR2, ACC, packing/CRC, DMA and total cycle time.
2. Start ADC1 and ADC2 conversions together so conversion time overlaps.
3. Test MUX settling at 100, 75, 50 and 25 us, validating analogue accuracy and
   crosstalk in addition to CRC.
4. Test ACC SPI2 at 1 and 2 MHz while preserving valid data from all positions.
5. Revalidate Teensy hardware LPSPI at 500 kHz, 1 MHz, 2 MHz and 4 MHz now that
   the STM32 full-duplex DMA receiver is active.
6. Raise the STM32 clock from 16 MHz to a validated 80 MHz configuration, then
   consider 170 MHz only after recalculating timing and peripheral dividers.
7. Require zero CRC errors, sequence gaps, DMA errors and post-settle USB short
   writes for 1000 frames, followed by 10,000 frames or at least 60 seconds.

Target cycle limits are 20 ms for 50 complete frames/s and 10 ms for 100
complete frames/s. Additional buffers may reduce jitter, but will not raise
sustained throughput unless acquisition or transfer time is reduced.

## 2026-08-10 — STM32 combined-system clock raised to 80 MHz

The combined STM32 application now derives an 80 MHz SYSCLK/HCLK from the
internal 16 MHz HSI through PLLM=4, PLLN=40 and PLLR=2. Voltage scale 1 remains
active and Flash latency is increased from zero to two wait states.

SPI1 and SPI2 only provide power-of-two baud prescalers, so their exact former
8 MHz and 500 kHz rates cannot be retained from an 80 MHz peripheral clock.
For the first CPU-clock validation, both sensor buses are deliberately kept
conservative:

```text
SPI1 ADC: 80 MHz APB2 / 16 = 5 MHz
SPI2 ACC: 20 MHz APB1 / 64 = 312.5 kHz
SPI3 Host: external Teensy software Mode-0 SCK remains nominal 250 kHz
```

The 100 us MUX settling delay uses DWT cycles calculated from
`SystemCoreClock`, so its real-time duration remains 100 us after the clock
change. HAL millisecond timeouts continue to use the reconfigured SysTick.

Initial 80 MHz tests exposed a timing race that the slower 16 MHz acquisition
had hidden. The STM32 could finish the next acquisition and start aborting and
reinitialising SPI3 before Teensy released NSS/CS after its 100 us hold. This
produced alternating incomplete transfers, stale prefix bytes, approximately
50% CRC failures and about 0.6 transactions/s. Reducing APB1 to 20 MHz and
replacing `__WFI()` with polling did not change the failure, ruling out the
peripheral clock and CPU sleep as root causes.

The accepted fix waits for hardware NSS on PA15 to return high before SPI3 is
aborted, reinitialised and armed for the next DMA transaction. The wait has a
10 ms diagnostic timeout and increments `host_dma_timeout_count` if Teensy
does not release CS.

The final 60-second capture is:

```text
docs/test_results/20260810_195415_stm32_80mhz_nss_release_guard_soft250_60s.txt
STM32 IRQ / completed: 1450 / 1438
CRC / magic / header errors: 0 / 0 / 0
post-settle USB short writes / NSS release timeouts: 0 / 0
transaction rate: 24.167 Hz
effective USB output: 23.967 frames/s
USB-ready acceptance: 1438 / 1438 = 100%
```

Independent raw parsing found one capture/USB-startup boundary gap, followed
by 1427 consecutive CRC-valid frames (sequence 3756 through 5182) with zero
steady-state gaps. This passes the required 1000-frame hardware gate. Compared
with the accepted 16 MHz result of 23.0 transactions/s, the transaction rate
increased by 5.1%; Host SPI wire time at nominal 250 kHz is now the dominant
limit rather than the STM32 CPU clock.

## 2026-08-10 — 700 Hz packet transport achieved

The next optimisation cycle reached stable 700 Hz complete-packet transport.
The final architecture uses 10 MHz hardware Host SPI, one shared FSR MUX
address per packet, 100 Hz ACC refresh, table-driven CRC32, STM32 SPI3
full-duplex DMA ping-pong buffers, a 16-frame Teensy USB queue, and explicit
700/s pacing. STM32 is built at 80 MHz; the accepted Teensy 4.1 build uses its
default 600 MHz `F_CPU` setting.

The Release build initially exposed an NSS race that caused alternating DMA
timeouts and stale prefixes. Increasing only Teensy CS timing did not fix it.
Requiring PA15/NSS to remain high for 50 us before STM32 abort/reinitialise/
rearm fixed the race. An unpaced test then reached 883 transactions/s but
overflowed sustained USB output, so the final producer is paced at 700/s.

Final 60-second result: 41,331/41,331 diagnostic-window transfers accepted,
700.181 packets/s, and zero CRC, magic, header, USB-short, sequence-gap, or NSS
release-timeout errors. Because one of 16 MUX addresses is refreshed per
packet, complete FSR matrix freshness is 43.76 Hz; ACC freshness is 100 Hz.

Full bilingual attempt table, flow diagram, profile timings, and evidence:

```text
docs/updates/2026-08-10-700hz/PROGRESS_UPDATE.md
```
