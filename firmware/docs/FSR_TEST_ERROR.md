# FSR 测试错误记录

## FSR1 原始 ADC 数据仅为 0/1

- 记录日期：2026-08-09
- 测试对象：FSR1 16×16 矩阵
- 串口：COM9
- 现象：GUI 和保存的原始帧中，256 个 ADC 数值仅为 `0` 或 `1`。
- 传输状态：帧序号持续增加，速率约 `25.1 fps`，`gaps = 0`。
- 显示状态：GUI 已取消校准和归一化，直接显示 STM32 发送的 12-bit 原始 ADC 值。
- 当前结论：STM32→Teensy→USB→GUI 的帧传输连续，FSR1 原始采集值异常接近零。
- 状态：未解决；暂停 FSR1，下一步切换到 FSR2 进行对比测试。
